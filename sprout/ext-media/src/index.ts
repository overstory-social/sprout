// @overstory/sprout-ext-media — pictures for Sprout, as an extension
// (MIT). Install it on a language instance and a source may say
// `use media`; the engine records `show` as an effect the host acts on.
export * from './media.js';
