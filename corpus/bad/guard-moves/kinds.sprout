// A guard may not move anything: the engine asks it in the middle of a
// move, and the world must not change underneath it.
kind Crate {
  contains

  accept (item, from) {
    move item to from
  }
}
