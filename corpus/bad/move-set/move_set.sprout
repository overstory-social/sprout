world move_set is sprout.World {
  contains actors
  visitors are Person
  visitors arrive at hall

  object hall is sprout.Place
}

verb sort { role target  role pieces many  "sort [pieces] into [target]" }
