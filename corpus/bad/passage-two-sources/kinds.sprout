// Two kinds each write a line called `taken`, neither of them a default.
// A thing speaks each line in one voice, so composing both is refused
// until the composer writes its own.
kind Plain {
  passage taken { You take it. }
}

kind Terse {
  passage taken { Taken. }
}

kind Porter is Plain, Terse { }
