cabinet.sprout:3:24  `Cabinet` gets `pass any` from both `Glass` and `Oak`, and a container has one rule for each message.
                     Write `pass any (…)` in `Cabinet` to say which applies.

refused: 1 problem
