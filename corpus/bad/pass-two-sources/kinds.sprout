// Two composed kinds that each say what passes cannot both be the
// container's policy.
kind Glass { contains pass any (true) }
kind Oak   { contains pass any (false) }

kind Cabinet is Glass, Oak { }
