// `:gust` carries nothing, so there is no value for a handler to bind.
message :gust

kind Candle {
  :lit true
  on :gust (from, strength) { self.set(:lit, false) }
}

kind Bellows {
  on :spawned (from) { broadcast :gust }
}
