world arrival_world_in_path is sprout.World {
  visitors are Creature

  // The world's name may be a path's first step and no other: written
  // after a step, it is refused at the step it is written as.
  visitors arrive at hall.arrival_world_in_path.nook

  object hall is Room {
    object nook is Room
  }
}
