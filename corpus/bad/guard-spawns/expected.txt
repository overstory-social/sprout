kinds.sprout:7:5   `spawn` makes a new thing, and a guard only reads and decides.
                   Move it to a handler or a `do`; a guard ends in `allow` or `refuse`.

kinds.sprout:10:5  `destroy self` removes something, and a guard only reads and decides.
                   Move it to a handler or a `do`; a guard ends in `allow` or `refuse`.

refused: 2 problems
