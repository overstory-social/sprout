// A kind the visitor kind composes may play and guard as any kind may.
kind Creature is sprout.Actor {
  as actor for wave { permit { if (self.count >= self.get(:capacity)) { refuse "Your hands are full." } } }
}
