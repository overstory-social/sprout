chest.sprout:3:23  `Chest` gets `:open` from both `Lidded` and `Futon`, which are two claims on one slot.
                   If they are meant to be one property, restate it in `Chest`: `:open true`.

refused: 1 problem
