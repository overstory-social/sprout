// `SafetyLamp` composes `LightSource`, but nothing in `LightSource` is
// a `changed :lit` hook, so there is nothing to leave out.
kind LightSource {
  :lit false
}

kind SafetyLamp is LightSource {
  without changed :lit from LightSource
}
