painting.sprout:4:10  `media.Image` is a type of the extension `media`, which this file does not name.
                      Write `extension media 1` at the top of the file.

painting.sprout:8:7   `media.show` is a statement of the extension `media`, which this file does not name.
                      Write `extension media 1` at the top of the file.

sprout.json:7:28      This host does not provide the extension `media`.
                      Ask whoever runs this host to install `media`, or take it out of the manifest and every file that names it.

refused: 3 problems
