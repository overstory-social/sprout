chance_in_when.sprout:10:45  An exit's `when` may not use `chance`: it is asked to show a visitor the ways out, so a roll would offer a way that vanishes when taken.
                             Roll in a `do`, a handler or a tick, keep what it gave on a property, and read that here.

refused: 1 problem
