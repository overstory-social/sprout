world actor_in_what_holds_no_actors is sprout.World {
  visitors are Person
  visitors arrive at hall

  object hall is sprout.Place {
    object basket is Basket {
      // An actor stands only in a place, and a basket holds things, not people.
      object cat is Creature
    }
  }
}
