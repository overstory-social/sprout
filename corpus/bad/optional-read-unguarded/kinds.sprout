// `"unlock [target]"` leaves the tool out, so a body reads it only once
// `bound` has said it is there.
kind Key { }

kind Warded {
  as target for unlock {
    permit {
      if (!tool.is(Key)) { refuse "That is not a key." }
    }
  }
}
