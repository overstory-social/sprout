// The world is declared in the file named for it, never in a kind's file.
kind Person is sprout.Visitor { }

world kind_file_world is sprout.World {
  visitors are Person
  visitors arrive at hall

  object hall is sprout.Place
}
