person.sprout:4:7  The world `kind_file_world` is declared in `person.sprout`, and the world is declared in the file named for it.
                   Move `world kind_file_world` to a file called `kind_file_world.sprout`, and name that file in the manifest's files.

refused: 1 problem
