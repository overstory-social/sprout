kind Cell is sprout.Place {
  grammar { link onward "deeper into the dark"  link back "back" }
  as target for dig {
    do {
      let pebble = spawn Pebble in self
      connect onward to meadow
      connect back to pebble
      say "The wall gives."
    }
  }
}
