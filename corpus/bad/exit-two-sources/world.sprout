// One place says where a direction leads: two kinds that each lead out
// cannot both, and the composer says which.
world exit_two_sources is sprout.World {
  visitors are Person
  visitors arrive at yard

  object yard is Porch
  object street is sprout.Place { }
  object lane is sprout.Place { }
}
