// A verb is named alone: this world's own is reached first, then the
// standard library's.
kind Cat is Creature {
  as target for pet {
    do {
      act sprout.take (target: actor)
      say "The cat settles."
    }
  }
}
