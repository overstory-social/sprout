kind Cell is sprout.Place {
  grammar { link onward "deeper into the dark" }
  as target for dig {
    do {
      let cell = spawn Cell in self
      connect down to cell
      say "The floor gives."
    }
  }
}
