world move_no_thing is sprout.World {
  contains actors
  visitors are Person
  visitors arrive at hall

  object hall is sprout.Place
}

kind Person is sprout.Visitor { }

verb pull { role target  "pull [target]" }
