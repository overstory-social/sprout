world object_names_container is sprout.World {
  visitors are Creature
  visitors arrive at composing_room

  object composing_room is sprout.Place {
    object bench is Bench
  }

  // An object is written inside what holds it, and never names it.
  object cushion is Bench in composing_room.bench
}
