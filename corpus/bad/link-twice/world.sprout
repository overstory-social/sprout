// `connect` names a link by its direction, so one body writes one link
// in each.
world link_twice is sprout.World {
  visitors are Person
  visitors arrive at yard

  object yard is sprout.Place {
    grammar {
      link north "deeper into the dark"
      link north "further on"
    }
  }
}
