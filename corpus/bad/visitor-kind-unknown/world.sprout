// `visitors are` names a kind the world declares.
world visitor_kind_unknown is sprout.World {
  visitors are Creture
  visitors arrive at hall

  object hall is sprout.Place
}

kind Creature is sprout.Actor { }
