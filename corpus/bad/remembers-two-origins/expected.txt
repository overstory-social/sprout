remembers_two_origins.sprout:6:29  `bell` gets `:seen` from both `Watched` and `Greeted`, which are two claims on one slot.
                                   If they are meant to be one property, restate it in `bell`: `remembers { :seen false }`.

refused: 1 problem
