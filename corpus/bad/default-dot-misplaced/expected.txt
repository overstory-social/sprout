sprout.json:2:3    This world has no `world` declaration.
                   Write one, in one of its files: `world <name>: sprout.World { … }`.

world.sprout:4:16  `:wear` writes a dot after a type that has no options.
                   Only an enum names its option after a dot, as in `:ward Ward.iron`. Write `default` and the value instead.

refused: 2 problems
