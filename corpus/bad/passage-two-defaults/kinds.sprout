// Two kinds each write a default line called `shrug`. A default gives
// way only to a line that is not one, so two defaults collide as any two
// sources do.
kind Plain {
  passage shrug default { {self} gives nothing away. }
}

kind Terse {
  passage shrug default { Nothing. }
}

kind Mirror: Plain, Terse { }
