// A passage never closed runs to the end of the file, and is refused
// once, at the brace that opened it.
kind Lamp {
  :lit false
}

kind Mirror {
  :mood 0
  passage greeting {
    The glass is older than the frame, and older than
