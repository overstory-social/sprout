# Legacy corpus

Entries for the previous language, moved here by the PR that made them
wrong and deleted by the issue that replaces them (CLAUDE.md, The legacy
fence). `check-corpus` reads only `corpus/good` and `corpus/bad`.
