verbs.sprout:4:6   `take` hides `sprout.take`: a bare `take` in this world is now yours.
                   Where the library's `take` is still meant, give yours another name.

verbs.sprout:4:6   Nothing that takes part in `take` ever `say`s anything, so typing it is answered with the world's `nothing_happens`.
                   Say what happens in a role's `do`, as in `as target for take { do { say "…" } }`.

verbs.sprout:12:6  Nothing that takes part in `wave` ever `say`s anything, so typing it is answered with the world's `nothing_happens`.
                   Say what happens in a role's `do`, as in `as target for wave { do { say "…" } }`.

verbs.sprout:19:6  Nothing that takes part in `greet` ever `say`s anything, so typing it is answered with the world's `nothing_happens`.
                   Say what happens in a role's `do`, as in `as target for greet { do { say "…" } }`.

ok: 6 declarations in 3 files, 4 warnings
