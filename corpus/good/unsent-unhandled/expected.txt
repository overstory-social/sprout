cat.sprout:2:6    Nothing sends `:stir`, so `on :stir` never runs.
                  Send it with `send <thing> :stir` or `broadcast :stir`, or take the handler out.

kinds.sprout:2:9  Nothing handles `:creak`, so sending it does nothing.
                  Write `on :creak { … }` in the kind that should hear it, or take the message out.

ok: 6 declarations in 4 files, 2 warnings
