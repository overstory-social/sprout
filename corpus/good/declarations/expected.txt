kinds.sprout:5:9   Nothing handles `:stir`, so sending it does nothing.
                   Write `on :stir { … }` in the kind that should hear it, or take the message out.

kinds.sprout:6:9   Nothing handles `:illuminating`, so sending it does nothing.
                   Write `on :illuminating { … }` in the kind that should hear it, or take the message out.

kinds.sprout:7:9   Nothing handles `:ward_seen`, so sending it does nothing.
                   Write `on :ward_seen { … }` in the kind that should hear it, or take the message out.

kinds.sprout:11:9  Nothing handles `:ward_seen_again`, so sending it does nothing.
                   Write `on :ward_seen_again { … }` in the kind that should hear it, or take the message out.

ok: 11 declarations in 4 files, 4 warnings
