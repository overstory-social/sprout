# Sprout — working notes

2026-09-19 · @Someone

The inputs behind the design spec: four reviews reconciled, the prior art weighed, and the questions still open. This doc goes stale as the spec settles — the spec is the artifact, this is the reasoning behind it.

## Reviewer positions

Three reviews converge on eight findings, conflict on six, and between them correct two claims each of them made. Eric's own material is forward-looking — federating Sprout instances among other things — and is a separate input, not part of this reconciliation.

One decision governs everything below: **Sprout is unreleased, so backwards compatibility is not a constraint.** Every "do this before `LANGUAGE_LEVEL` 1 ships" argument collapses into "design it correctly." The policy-refusal versus added-syntax distinction stops governing sequencing; work is ordered by dependency alone. `LANGUAGE_LEVEL` remains as a forward mechanism for after launch.

### Where they converge

| Finding | Raised by | Status |
| --- | --- | --- |
| No prose interpolation | Claude, Gemini | Settled. Gemini sharpens it: with 16 statements per body and nesting capped at 8, an author cannot print a number above about 8 even by if-chain. |
| `:hidden` and `:scenery` are declared and ignored | Claude, Gemini | Settled. Gemini locates the consequence at `turn.ts:213` — scenery is regurgitated as "You can see bench, window, shelf here." |
| No time, clock, or daemons | Claude, Gemini, GLM | Unanimous. The only unanimous finding. |
| No local variables or intermediate computation | Claude, Gemini | Settled. Gemini adds that arithmetic is `+` and `-` only. |
| Single inheritance is too weak | Claude (no `super`), Gemini (no traits) | Same root, two proposed fixes. Resolved under Conflicts. |
| Multiplayer is architectural, not actual | Claude (no `tell`), Gemini (no push outbox), GLM (no ambient, coarse lock) | Three angles on one gap. |
| Author-facing tooling is effectively absent | GLM, Gemini | Settled, and Claude under-weighted it. GLM wants `lint`, `parse`, a test format; Gemini wants LSP, `fmt`, `test`. |
| Flat global namespace for rooms and objects | Gemini | Matches the repository's own "rooms as scopes" proposal. Settled as a real problem. |

### Where they conflict

**1. Is the parser a strength or the weak link?** GLM calls it "the weakest technical link — opaque, hard to extend, prone to surprising misses" and ranks making it inspectable fourth. Claude called grammar-as-data a top strength. Both are right about different things: the *design* — grammar authored in Sprout, one match function behind parse, complete and help — is sound and should survive. The *implementation* is unverifiable to the author, who cannot see what their world accepts without playing it. We take GLM's fix and reject GLM's framing: keep the design, add `sprout parse` and a grammar-table dump.

**2. Is the opaque actor a feature or a defect?** Gemini says it stifles storytelling — NPCs cannot greet by name, descriptions cannot acknowledge the player. Claude treated opacity as a privacy invariant on which the interpolation safety argument depends. This is a values conflict, not a technical one. The opacity is what lets the host promise a visitor that the memory panel shows everything any object knows about them. Position: keep the actor opaque by default; if a name is ever exposed it is a host-supplied presentation the visitor chose and the host can moderate, never an inventory or an identity the world can fingerprint. Carried to Open questions.

**3. Is the evaluator bounded?** Gemini calls it "mathematically sound, bounded, and secure by construction" and lists totality as strength 1.1. Claude found that `engine.ts:727` runs `each` with no accounting, so nested `each` is O(nᵏ) with no fault. Verified: the budget counts events, spawns and live instances, never statements. Gemini's claim is true of events and false of work. **Sprout is total but not cost-bounded**, and Gemini's strength 1.1 is half a strength.

**4. What does "everything is strings" mean?** GLM means the internals — properties, messages, grammar lines and effect kinds are stringly typed, and every consumer re-derives maps from raw definition arrays. Claude means the source language — symbols evaluate to their bare names and `==` is unchecked. Not a conflict; two distinct problems that share a smell. Both are in scope: source-level typing, and compiler-built typed maps the runtime consumes.

**5. Is the extension model right-sized or unfinished?** Gemini lists it as strength 1.5; GLM calls it "an interface, not a framework" and ranks clarifying it second. Both hold. The *invariant* — an extension records an effect and never performs one — is correct and stays. The *interface* is under-specified, which the repository's own `review-dx.md` finding 7 already said: four of its five fields are undefined. Keep the invariant, specify the interface.

**6. Is the per-microworld write lock right?** GLM says globally serialized write turns are wrong. Claude says the lock is a blast-radius control, so a pathological world only harms itself. Both are correct about their own objective. This is core's decision, not the language's, but the two interact: a step budget is what makes a coarse lock tolerable, because it bounds how long any one turn can hold it. Deferred to the spec's limits section.

### Found by one review only, and surviving

| Finding | Source | Why it holds |
| --- | --- | --- |
| All semantic errors report at line 1, column 1 | Gemini | Verified in `sprout-lang.ts`: `checkTree` defaults `at` to `{line: 1, column: 1}` and stamps every semantic problem with it. Decisive, and a prerequisite for any type checking. |
| Message arguments can only be `object` | Gemini | `SproutArg` is `z.literal('object')`. No `turn dial to 3`, no `type 1042 on keypad`, no `ask guard about rumors`. Claude missed this entirely. |
| Comparisons are untyped; symbols are strings | Claude | `engine.ts:1063` returns a symbol's bare name; `==` is `l === r`. A typo'd symbol compiles and is false forever. |
| `each` is uncharged | Claude | See conflict 3. |
| `send` to an unresolvable target or an unhandled message is a silent no-op | Claude | `engine.ts:723`. The repository's own worked example carries a live instance of the bug. |
| No author test format | GLM | `testWorld()` is a TypeScript helper. There is no way to write "after `light torch`, expect this text" in something an author reads. |
| `Runtime` does too much | GLM | Loading, turns, projection, parsing, store IO, logging, presence and view construction in one class. |
| Two-sided doors, supporters, wearables, equipment slots | Gemini (Claude had doors and supporters) | The standard IF world model, largely absent. |
| Dialogue and topics | Gemini | `ask X about Y` has no shape in the language. |

### What this review corrects

- **Claude's**, that every refusal names line and column. Half true: syntax errors carry real positions from the lexer, semantic problems do not.
- **Gemini's**, that the evaluator is bounded by construction. True of cascades, false of iteration.
- **Gemini's phasing** ("Weeks 1–2", "Weeks 3–5") does not apply. This is a design project before it is a build project, and the ordering that matters is dependency, not calendar.

## The reconciled critique

Everything wrong with the language as it stands, consolidated from four reviews and verified against the source, with where the spec answers it. This is the bridge: the backlog is this list ordered by dependency.

### Correctness

| defect | evidence | answered by |
| --- | --- | --- |
| Comparisons are untyped; symbols evaluate to bare strings, so a misspelled symbol is false forever | `engine.ts:1063`, `:1101`; the two shipped examples use different idioms | Properties and types |
| `each` is uncharged, so a total program can run effectively forever | `engine.ts:727`; the budget counts events, spawns and instances only | Limits — the step budget |
| A `send` to an unresolvable target or an unhandled message is a silent no-op | `engine.ts:723`; the worked example's `:struck` never depletes the matches | The compiler — symmetric warning |
| `isContainer` decides containment by comparing a kind name to a literal string | `engine.ts` | World model — `contains` |
| The spec said pass rules gate range; `contents()` performs no pass check, so `each` sees inside a shut chest | `engine.ts:1053` | World model — Range, which keeps the spec's version and adds the self-and-own-contents exception |

### Expressiveness

| defect | answered by |
| --- | --- |
| No prose interpolation at all; integers and counts cannot be printed | Prose |
| No bystander narration — `say` reaches the actor only, and engine notices cover arrivals and departures alone | Other people — `tell` |
| Nothing happens without a player action; no clock, no scheduled work | Time — ticks and wakes |
| No variation of any kind | Chance |
| Single inheritance, whole-member override, no `super`, no composition | Kinds — composition |
| Message arguments may only be `object`, and at most two | Verbs — roles, set roles, value roles |
| No local bindings, so a property read is repeated and re-read | Properties and types — `let` |
| Exits are free-text labels, so `go north` works by string coincidence | World model — directions and conditional exits |
| No way to reach space that does not exist yet | World model — `link` and `connect` |
| Flat global namespace for every identifier | Names — scope by container |

### Things that lie

| defect | evidence | answered by |
| --- | --- | --- |
| `:hidden`, `:scenery` and `:illuminated` are declared, stored, and consulted by nothing | half the well-known table | Kinds — no magic properties at all |
| Every semantic problem reports at line 1, column 1 | `sprout-lang.ts` `checkTree` defaults `at` | The compiler — a stated requirement |
| A false `when` guard makes a typed command produce nothing at all | `engine.ts:1277` | Verbs — `permit` refuses in the author's words; `when` and `otherwise` are gone |
| The statement cap exists to bound cost but is enforced where only review effort belongs |  | Limits — removed |

### Author experience

| defect | answered by |
| --- | --- |
| The extension interface is under-specified and lets extensions inject properties globally | Extensions |
| No author-facing test format; `testWorld()` is a TypeScript helper | **Unresolved** |
| No way to inspect what the parser will accept | **Unresolved** |
| No dialogue or topic model | Verbs — value roles, narrowed per object with `from` |
| No doors, supporters or wearables | Partly — the standard library; doors parked with federation |

### Not the language's

`Runtime` doing loading, turns, projection, parsing, store IO, logging, presence and view construction in one class is GLM's finding and a real one, but it is host-side. It belongs in the backlog, not the spec.

### What the spec adds that nobody asked for

Worth listing separately, because each is a new surface rather than a repair: per-world nicknames, passages as a member kind, static linking with content-hashed vendoring, library namespacing, world-level verbs with roles and their two passes, conjunctive composition, places as ordinary objects under a world root, and the explicit separation of what a client may render from what the log records.

## Prior art

Organised by the decision each system speaks to. Two of these confirm choices we made, two say we underpriced a cost, and one hands us a number.

### Combining multiple opinions: Inform 7

Inform sorts rules into rulebooks **by specificity** and runs them until one ends in success or failure; a rule ending in "no decision" lets the next one run. More specific beats less specific — a rule about an open container beats one about a container.

The cost of that convenience is documented at length. Inform's manual carries a chapter called *The Laws for Sorting Rulebooks*, admits that the mechanism "will sometimes get it wrong," and supplies `is listed before`, `is listed instead of` and `is not listed in any rulebook` so authors can override the sort. There are forum threads about rules that refuse to order correctly.

**This confirms refusing on collision.** Automatic precedence looks kinder than a compile error right up to the point where an author has to learn a sorting algorithm to find out which of their rules ran. Our answer — every contribution runs, any refusal decides, and a genuine conflict is a compile error the composer resolves — has no sort order to get wrong.

One thing to borrow: Inform's third outcome. Its "no decision" is our `allow`, so the shapes already match; what Inform has and we do not is a *name* for the common case of a rule that simply has no opinion.

### Who decides an action: TADS 3

TADS puts action handling on the objects, through `dobjFor(Verb)` and `iobjFor(Verb)` groups — so both the direct and indirect object get a say. Each group has phases: `verify()` first, then `check()`, then `action()`, then `report()`.

The split between the first two is the interesting part, and TADS has a whole technical-manual article on when to use which. `verify()` is side-effect-free and *ranks* — `illogical`, `illogicalNow`, `illogicalAlready`. `check()` vetoes with a message. `action()` performs.

**This confirms the read-only guard.** Our `depart`/`release`/`accept` are TADS's `verify` and `check` merged, and read-only for the same reason theirs is: a decision procedure that can change the world is a decision you cannot trust.

**But it names a capability we gave up.** `verify()` ranks candidates, so TADS disambiguates on *state*: with an open box and a closed box present, the open one wins for an action that needs one. Our typed slots disambiguate on *type* only. Ours is static, checkable and free; theirs is dynamic and strictly more expressive. If disambiguation turns out to need state, that is where the pressure will come from.

TADS also writes `preCond = (inherited() + touchObj)` to add to an inherited set and `preCond = (inherited() - objHeld)` to remove from it. We built the subtraction half as `without … from …` and dropped the addition half, because all-run composition makes adding implicit. Worth knowing the pair exists together in a mature library.

### Bounding cost: LambdaMOO

This is the direct precedent, and it is almost exactly what we designed.

LambdaMOO counts **ticks**: one for every expression evaluation other than a variable or literal, one for every `if`, `fork` or `return`, and **one for every iteration of a loop**. Foreground tasks get 30,000; background tasks get 15,000. There is also a seconds limit — five foreground, three background — which the manual notes is "in practice, rarely reached," because the tick count binds first. Nested verb calls are capped at 50 levels, and players have quotas on queued tasks.

So: a deterministic operation count as the primary bound, a wall clock as the rarely-reached backstop, a stack depth cap, and per-player quotas on scheduled work. Four for four.

It also hands us a starting number. We proposed 50,000 steps; LambdaMOO has run on 30,000 for decades, against a language with loops and recursion that Sprout does not have.

And a cautionary detail worth keeping: there is a real LambdaMOO bug report of a player whose *command parsing* exhausted the tick budget, because it had to check too many of their installed features. Type-constrained disambiguation makes our parse more expensive, on every keystroke for completion. Parsing must be charged too.

### Text variation: Ink

Ink has **four** kinds of alternative where we have one. Sequences run through their entries and stick on the last. Cycles loop back to the first. Once-only alternatives run through and then show nothing. Shuffles pick at random. They nest, and they can contain blank entries and diverts.

Our `{one of}` is Ink's shuffle, and it is the only one of the four that needs no state. The other three each require a counter — which element are we on — and that is a real design question we have not asked: **per object, or per visitor?** In a single-player medium the question does not arise. In a shared room it decides whether the second visitor to look sees the first line or the second.

This is the most actionable finding here, because "the first time, and thereafter" is arguably a more useful pattern in interactive fiction than randomness, and we have built the random one and not the sequential one.

### Composition: Evennia, and a warning

Evennia's typeclasses are Python classes, so composition is Python's multiple inheritance, and its own tutorial reaches for a `LivingMixin` while cautioning "one should not over-do multiple inheritance since it can also get confusing to follow the code." That is our legibility cost, observed in the wild.

More pointedly: Evennia's default game template ships **`ObjectParent`, an empty mixin class**, as the designated place to put behaviour common to everything. That is exactly the empty-kind-as-brand smell we rejected — and a mature system shipped one anyway, because the alternative was worse. Worth remembering when our own standard library wants one.

### Ambience in a shared world: Evennia

An Evennia community pattern for ambient messages runs a global script on an interval, determines **which rooms have players in them**, and triggers one message per occupied room; rooms collect candidate messages from their own contents.

We arrived at the same design independently — periodic, occupied rooms only, the room fanning out to what it holds. That is reassuring. What they have and we do not is **weighting**: their selection is weighted, so a common line and a rare one can sit in the same set. Our `{one of}` is uniform.

### The one nobody solves for us

The worked example found that a standard library kind cannot declare a property of a world-defined type. Neither precedent has this problem, and the reason is instructive.

TADS is dynamically typed, so the question never arises. Inform's Standard Rules are compiled together with the story rather than linked as a separate artifact.

And Inform solves lock and key directly: its standard rules give a lockable thing a **`matching key` property that holds another object**. That is precisely the stored object reference we refused, and refusing it is what forced our ward enum, which is what forced the matching logic out of the library and into every world.

So the standard library ceiling is not an independent problem. It is the downstream price of comparison-only identity, and it is larger than we priced it. That does not make the decision wrong — stored references cost dynamic reachability, dangling values and the return of null — but the trade should be re-argued knowing that the entire standard library thins out on the other side of it.

## Open questions

Two kinds: things the spec uses without defining, and decisions genuinely still to make.

### Holes in the spec

How this section works, for the agents that read it. **Open** comes first and holds every question still waiting on Eric, numbered as on his review doc (the "Sprout spec holes and decisions" artifact), which mirrors it. A hole found while building is built the narrow way, recorded under its item below, and, if it needs Eric, added to Open with the next number. When Eric answers, the entry leaves Open, the spec changes in a docs PR, and the item's entry is marked *Decided* with the date. Nothing open lives below Open.

**Open**, awaiting Eric:

- **10. Whether a withheld file changes the bundle's hash.** Deferred until the publish, share, repository and library-versioning story is settled.
- **35. Which kind a register library is composed onto.** A register composed onto the world replaces `sprout.World`'s lines and not `sprout.Place`'s or `sprout.Actor`'s, since a passage is the kind's. Left for the library work (#127).
- **54. The register of the stock lines.** Long-term, not a blocker.
- **132. Whether a dropped tick is logged.** Faults says a tick that faults is dropped, and The log says it holds every tick with its inputs and seed. Built: the tick turn abandons everything, tells nobody in the world, and hands its fault to the host, marked the world's or the engine's as every fault is; whether the log records it, as it records a faulted wake, is B40's, and the spec should say, since a place whose tick faults every time is otherwise invisible to a moderator. (Recorded while building ticks; see below.)
- **162. The place of a teller that holds actors.** Who hears it says a plain `tell` reaches the teller's place, "its nearest ancestor declaring `contains actors`", and Ticks' own example has a place's `on :tick` `tell` the wind to the people in it, which an ancestor strictly above the place would not reach. Built: the teller itself where it holds actors, else its nearest container that does; so a wardrobe that holds actors tells its own occupants and not the room it stands in, from its `do` as from its handlers. The alternative: a place tells its occupants only from a tick or a handler, and an object's role body tells the place around it. *Discussed 2026-09-24, morning:* Eric wants the author to direct a teller's speech inside or outside, as a magic wardrobe that says "get inside me" to the room and then "ahaha I am a people eater... Gulp!" to the one inside; `tell inside` and `tell outside` are proposed in the thread.
- **163. What counts as speaking for a verb.** What it warns about says "a verb with phrases that no participant ever `say`s for, which will fall back to `nothing_happens`". Built: a `say` in any play's `do` for the verb, for any role or the actor, in any composed kind; a `tell actor`, which also reaches the actor, and a `refuse` do not count; only the world's own verbs are warned about; and nothing is said of a refused bundle, which is missing what was refused. The alternative: count anything that reaches the actor, a `tell actor` or a `tell self` in the actor's part among it.
- **190. What a body declares while the world runs.** Identifiers and scope resolves a kind's name from where the instance sits "by the same rule", and the rule is that an identifier belongs to the body it is written in. Built: each container outward from the instance is asked what its body declares, not what it holds now: for a declared object, what the tree places under it; for one made while the world runs, what its kinds give it, and for a copy, what the body it was copied from writes. So a declared lamp carried out of the hall is still the hall's `lamp`, found wherever it is and a target only in range, and a lamp spawned into the hall is nobody's `lamp`; the nearest body declaring the first step wins even where what it declares does not hold the rest of the path, and the name then reaches nothing. The alternative: nearest by what each container holds now, by identifier, which a spawned instance does not have. (Recorded while building instance scope.)
- **191. `is()` on a name that is not a binding.** What the compiler checks says `x.is(K)` takes an object binding and narrows that binding. Built: a name in a kind's body is narrowed through a `let`, `let found = lamp` and then `if (found.is(Lamp)) { … }`, and a passage reads it only through such a binding of the body that says it, since a passage has no `let`; `lamp.is(Lamp)` is accepted as a condition and narrows nothing. The alternative: `if (lamp.is(Lamp)) { lamp.get(:lit) }` narrows the name for its branch, in a body and in a passage, the run binding the object the condition resolved so that a move inside the branch cannot change what the name reaches. (Recorded while building instance scope.)
- **197. What "equally near" is.** Spawning says the nearest of things written alike is meant and a draw decides among equally near ones, and Range orders the walk by ring and then breadth-first within it, each container's contents in its order. Built: nearness is the ring, how far out the container is that a thing is reached through, then how deep inside that container it lies; two things are equally near where both agree, so two pebbles on one floor tie, and one in the visitor's hands is nearer than one on the floor, which is nearer than one in an open chest there. Container order breaks no tie.
- **200. How `here` is typed where a place is not a `sprout.Place`.** The bindings table types `here` as `sprout.Place`, and Places makes a place any object declaring `contains actors`, so an actor may stand in something that does not compose `sprout.Place`. Built: `here` is `sprout.Place` wherever every kind in the world that holds actors composes it, and otherwise the object type, a read through it that needs its kind refused with a remedy naming the first such kind (`corpus/bad/here-not-a-place`); no world is refused for holding actors in a kind of its own. The alternatives: refuse `contains actors` on anything not composing `sprout.Place`, which would refuse the many worlds that write `kind Room { contains actors }`; or type `here` as a kind every place composes in common, which a binding of one kind cannot always say. (Recorded while giving `here` its kind; see below.)
- **210. What `help` offers, and in whose words.** Engine verbs says `help` renders "what the actor can currently do, as text: the same derivation the chips come from", and The view says the chips are "every reading the parser could build from what is in range … with the result of its consent pass". Built: every such reading whose consent pass allows, typed by its verb's first phrase that types every role it fills, a value role as `…`, a tool left out, one line however many things are written alike, in the engine's fixed words, "You can type: look, examine lamp, go north, ….", as a `notice`. So a verb nothing plays a part in for a thing is offered for it, as the parser would build it (`pull hall`, answered by `nothing_happens`), and the list is as long as range is. The alternatives: offer only readings in which some participant besides the actor plays a role, or group them by verb; and say `help` through a world passage a register may replace, binding the offers as a set. (Recorded while building descriptions; see below.)
- **211. What `wait` says.** Engine verbs says `wait` "is a turn in which nothing is done, so a client can show what has changed", and gives it no line, while Silence says a reading that says nothing to the actor is answered with `nothing_happens`. Built: `wait` is answered with `nothing_happens`, "Nothing much comes of that.", as any reading that says nothing is. The alternative: a stock line of its own, `waited default { Time passes. }`, on `sprout.World`. (Recorded while building descriptions; see below.)
- **212. A description that renders nothing.** A `describe` with no `text` is refused, and one whose every `text` names a passage of an absent `.prose` file is refused at publish; but a `describe` whose every `text` sits in an `if` that does not hold, or that renders nothing at load because its file is absent, reads as nothing when it runs. Built: where every line renders nothing, the world's `unremarkable` is read in their place, so looking never reads as silence. The alternatives: read nothing, which is a silent outcome; or refuse at compile time a `describe` with a path through it that runs no `text`. (Recorded while building descriptions; see below.)
- **220. An arrival that faults.** Faults has no row for it, and Admission says only that entry is a move. Built: the arrival turn is abandoned, the visitor is not admitted, and the host says so outside the world in fixed words, "Something went wrong as you arrived, and you have not come in.", the way a crash is told; so an arrival place whose `:entered` always faults admits no one. The alternative: the visitor is admitted quietly in a turn of its own, placed with nothing sent, as a faulted departure goes (221), so a world's fault can never keep a person out. (Recorded while building arrival; see below.)
- **221. A departure, and what the one who leaves is told.** The spec has a visitor leave and keep their state, and says leaving a place is the mirror of arriving, but names no turn for it and gives the world no line for the one who goes. Built: a departure is a write turn of its own: the visitor leaves the tree with what they carry, the place is sent `:left (item, to)`, its range reads `leaves` and is sent `:departed (actor, to)`, and the one leaving is told in the engine's fixed words, "You leave, and take what you carry with you."; one that faults is abandoned and the visitor then goes quietly, with nothing sent, in a turn of its own, since a person is never held in a world. The alternatives: a `gone` passage on `sprout.World` a world may override; or nothing said in the world, and the host's own goodbye outside it. (Recorded while building arrival; see below.)
- **222. What `from` is on arrival, and `to` on departure.** `:entered (item, from)`, `:moved (from, to)`, `:arrived (actor, from)`, `:left (item, to)` and `:departed (actor, to)` all bind an object, and a person arriving from outside the tree came from nowhere in it. Built: the world, in each, so a handler reading `from` or `to` always has an object and `{from}` renders the world's name. The alternative: an arrival sends no `:moved`, and `from` is left unbound, which the engine messages' bindings would need a rule for. (Recorded while building arrival; see below.)
- **223. What a displaced visitor typed.** What absent means moves a visitor whose place is gone "on their next turn". Built: that turn is the displacement alone: the world's `displaced`, then the entry at the arrival place, and what they typed is not read, since it was typed about a place that is no longer there. The alternative: displace, then read the command where they now stand. (Recorded while building arrival; see below.)
- **224. An arrival place that refuses.** Admission has entry ask `accept`, and the absent table covers only an arrival place that is not there. Built: a new visitor the arrival place refuses is not admitted and reads the refusal's words, and nothing is written; a returning visitor whose last place refuses is taken to the arrival place instead, as the spec says, and is not told `displaced`, which is for a place that is gone; a displaced visitor the arrival place refuses stays where they were, told `displaced` and then the refusal. The alternatives: the host's placing is not a proposal, and the arrival place's `accept` is not asked; or a refusal there is a host matter, as a world that admits no one is. (Recorded while building arrival; see below.)
- **225. A visitor stored inside something that no longer holds actors.** Decided 75 makes an actor found there a loud engine error, and says nothing of what then happens to it. Built: load names each one, NPC or visitor, in what it gives back for the host to report, and keeps it where it was stored; a visitor so stored is displaced on their next turn as from a place that is gone, since what they stand in is no longer a place, and keeps no place ticking meanwhile; an NPC stays. The alternative: a visitor so stored stays too, and every turn of theirs faults as an engine error until the source is fixed. (Recorded while building arrival; see below.)
- **230. How an NPC's `move` reaches another place.** Decided 152 has an NPC change place with `move self to <place>`, and Acting now says so, but another place is out of the mover's range ("A destination out of range across places"), so as built a `move` to another place faults and an NPC cannot cross between places at all. Options: (a) a `move` whose mover is an actor may name any place, as `go` asks no range of its destination; (b) only a place reached through one of the mover's place's exits or links; (c) as built, and a world wires the places so they hear each other. Not built.
- **235. Which speech pass rules stop.** Who hears it says pass rules do not apply to speech, "a voice from inside a shut chest is heard", and Decided 161 has `tell <x>` reach `x` only in the teller's range, which a shut container ends. Built: a plain `tell` ignores pass rules as that sentence says, and `tell <x>` asks range the way `get` does (`pass any`), so it stops at a shut container both ways. The sentence would narrow to the plain `tell`.
- **240. Two kinds a place names writing one link.** Decided 151 refuses two kinds a place's `is` names writing one direction, and says a place's own link of a name replaces its kinds'; it does not say what two of its kinds writing one link name do. Built: refused as the direction is, at the second kind as written, with the words "`yard` gets a link `onward` from both `Burrow` and `Tunnel`, and `connect onward` could not say which it means." and the remedy of the place's own link (`corpus/bad/link-two-sources`), since a link's destination is kept under its name and one `connect onward` would set both. The alternatives: both apply, one shared slot under two labels; or the first kind's, as written.
- **241. Which links a `connect` may name.** Links says `connect` "names a link its own body writes". Built: a link among the ways out of the kind whose body holds the `connect`, which for a kind is its own body's and for an object its own and those of the kinds its `is` names, since those are exactly the links its instance has; so `object cell is MazeCell { on :x { connect onward to … } }` is accepted, and a kind's `connect` of a link only a kind it composes writes is refused ("`DeadEnd` has no link `onward`, so there is nothing to connect."). The alternative: an object's `connect` too names only a link its own grammar block writes.
- **250. Which actor an effect carries.** Effects gives each effect "the actor if there was one". Built: the turn's, the same for every effect it says: the visitor who typed, arrived or left, a line a handler said in their turn and an NPC's `act` within it included; none in a tick or a wake, an NPC's `act` there included. The alternative: the actor of the reading the line was said in, so a handler's line carries none and an NPC's reading carries the NPC, and a moderator reading the log asks the turn, not the effect, who caused it. (Recorded while building effects; see below.)
- **251. A line nobody reads.** A handler's refused `move` is said to nobody, and so is a `tell` to a place with no one in it. Built: it is no effect, since an effect has a recipient: it is not rendered, so it draws nothing and costs nothing, and the log (B40) has nothing of it but the turn. The alternative: an effect with no recipient, rendered with nobody as the reader, so a moderator sees in the log what a handler refused while nobody was there. (Recorded while building effects; see below.)
- **252. When a line is rendered.** Effects says "words are true at the time they are said", and Decided 173 has a draw in prose fall when its line is first rendered, after every draw the turn's bodies made. Built: every line of a turn is rendered once the turn's work is done, against the state then, before it commits, so a line whose slot reads a property, said before a `set` of it in the same turn, renders the value the `set` wrote, and a nickname changed later in the turn reads as changed. The alternative: render each line as it is said, against the state at that moment, which reverses 173, since its draws would fall among the bodies'. (Recorded while building effects; see below.)
- **253. A place's notice as an answer.** The two passes answers "a visitor's reading whose effect pass says nothing to them" with `nothing_happens`. Built: what a place says of someone its effect pass moved between places counts, where it reaches the actor, so a `do` that carries an NPC out of the room with no `say` reads "The cat leaves." and not also "Nothing much comes of that."; a notice that reaches only others does not. The alternative: only the author's own lines, said, told or refused, answer a reading. (Recorded while building effects; see below.)
- **270. What someone other than the actor reads past their output.** What this costs and Cost that scales with people say a turn "never faults because of who else was there", and the output budget is per recipient; neither says what a bystander reads once a turn has said more to them than the host's `output` allows, nor whose output faults a turn with no actor. Built: only the turn's actor's output past the figure faults the turn; anyone else is cut short at the first line that would take them past it, reading what was said to them up to that line and nothing after it that turn, and the budget lists who was cut short for the host; a tick, a wake and a maintenance turn have no actor, so nobody's output faults them. The alternatives: a bystander reads the line cut mid-way, or a fixed notice in its place ("More is said here than can be shown."), so they know something was withheld; a turn with no actor faults on output as before, dropping a tick whose line is too long for anyone. (Recorded while building cost with people; see below.)
- **271. Rendering steps that scale with people.** A line is rendered once for each reader, since it says "you" to one and a name to the rest, and each rendering evaluates the line's slots, `{if}`s and `{for}`s against the turn's one step budget; so a `tell` whose passage walks a room costs steps for every person in the place, and enough people fault the turn, which is a fault because of who else was there. Not built: rendering is charged as it is. The options: charge a line's steps once, for its first reader, and render the rest without charge, since they differ only in names; or give each reader other than the actor a step allowance of their own for what they read, cutting them short as output does (270). (Recorded while building cost with people; see below.)
- **272. A place the host says is full.** The host contract › Enforcement has the host bound "how many people may be in one place", and says nothing of what the engine does when a move would pass it. Built: the host's `peoplePerPlace`, unset and so unbounded by default; only people count, directly in the place, the one moving not among them, and never an NPC; a move that would bring one more person in, by `go`, by a `move` in a body, on arrival or on displacement, is refused before any guard is asked, as the engine's other refusals are, in the engine's fixed words "There is no room in {to} for {item}.", said from the world; a returning visitor whose last place is full comes in at the arrival place, as one it refuses does, and a new one the arrival place has no room for is not admitted. The alternatives: the words are a passage on `sprout.World`, `full`, a world may override, as `inside_itself` is; the bound is asked after the guards, so a place's own voice is heard first; or the bound is only the host's at admission, and moves inside a world are never refused for it. (Recorded while building cost with people; see below.)

**Decided 2026-09-24, morning**, now in the spec except where an entry says otherwise:

- 125: a passage nothing says, and the engine does not, is checked with its own `self` alone, as built. No spec change.
- 133: a `:tick` handler is warned at an object, declared or spawnable, whose kind does not declare `contains actors`: "`cat` is not a place, so `on :tick` never runs." (What it warns about). "Sure, this is good." *Built:* see "Found while building ticks" (`corpus/good/untimed`).
- 143: a wake on something out of the tree is not due until it is back, and its `elapsed` then spans the whole time; what an away visitor carries is delivered live once they are admitted, as built (Absence).
- 144: catch-up drops everything a delivered wake says, a refused `move`'s line and an NPC's `act` included, as built (Absence).
- 145: "Agree, warn on both": a `wake` on a kind that answers no `:woke`, and an `on :woke` on a kind whose bodies never write `wake`, "Nothing here asks to be woken, so `on :woke` never runs." (What it warns about). *Built:* see "Found while building wakes" (`corpus/good/untimed`).
- 150: "Link name probably needs to be free form, else how can you enter the wardrobe?" A link's name is a word of the author's, not a direction, as `link onward "deeper into the dark"` writes it; `connect <name> to <binding>` names it (Links). The narrow reading: lower-case letters, digits and underscores beginning with a letter, neither a reserved word nor a word of the closed set, so `go north` never means a link; the name is source's alone, and a visitor takes a link by typing its label, with no `go onward`; two links of one name in one body are refused. Exits' closed set stands. *Built*: a link named by a direction, a reserved word or a word that starts with no letter is refused (`corpus/bad/link-not-a-name`), and `help` offers a link as `go` and its label; see "Found while building exits".
- 151: "Exits don't compose." An exit or a link belongs to the body that writes it: an object's own, and a kind's for that kind's own instances, an object whose `is` names it and a `spawn` of it, never for a kind that composes it (How members combine, Exits, Links). The narrow reading of the rest: a composed kind's exits and links are not refused, and simply do not reach the composer's instances; a place's own exit in a direction replaces its kinds' there, and its own link of a name replaces theirs; two kinds a place's `is` names writing one direction are refused, as `corpus/bad/exit-two-sources` pins; and a `connect` run on an instance without the link it names, as a composed handler on a composer's instance is, faults as a `connect` to what is not a live place does. *Built* (`corpus/good/exits-own`, `corpus/bad/exit-two-sources`), with two kinds a place names writing one link refused as well (Open 240), and a `connect` checked against the writing body's ways out (Open 241).
- 152: "NPCs moving with move instead of act is fine." `act go` stays refused, as built; Acting no longer has the cat enter a room with `act go`, and says an NPC changes place with `move self to <place>` in a handler, a tick or a wake. Another place is out of the mover's range as built ("A destination out of range across places", found while moving a thing through consent), so how an NPC's `move` crosses between places is Open 230.
- 153: a play of `go` runs its `permit` in the consent pass and its `do` after the move, as built (Engine verbs).
- 154: `connect` takes only a binding, as built: "Good for now" (Links).
- 160: `tell <x>` may not name an object of the world, as built (Who hears it).
- 161: "Only in range." `tell <x>` reaches `x` only if `x` is in the teller's range when the `tell` runs, and otherwise goes nowhere (Who hears it). The narrow reading: nothing, not a fault, as a `send` out of range does nothing and a line told to an NPC goes nowhere; so an `on :departed (actor, to)` that tells the one who left reaches nobody. Built differently; issue to follow. *Built:* `tell <x>` asks Range's walk from the teller whether `x` is in range, charged to the step budget as a `get` is; out of range the line is still kept, with nobody to read it; see "Found while building the three audiences".
- 170: `chance` in a pass rule is refused, as built: "Correct decision." (Where chance is forbidden, What it refuses).
- 171: the world's `unseen` and `unremarkable` may not vary wherever they are said, as built (Where chance is forbidden, What it refuses).
- 172: every reader of a line reads the same draw, as built: "Same for all" (The seed).
- 173: a draw in prose falls when its line is first rendered, after every draw the turn's bodies made, as built (The seed).
- 174: "Accepted, good idea." The generator is the language's, so a log replays across engine versions: mulberry32, a seed a whole number from 0 to 2³² − 1, a draw below `n` uniform by rejection, and `chance`, `random` and `{one of}` each in terms of it, as built (The seed).
- 175: "Actually allow. It is likely a warning but not an error." A `{one of}` with one choice is warned, not refused (What it warns about). *Built:* see "Found while building chance" (`corpus/good/one-of-one-choice`).
- 180: "Agreed with proposal." A name in a kind's body that no object in the bundle declares is refused; a read through one is of the object type and needs `is()`; one that reaches nothing when it runs is out of range, a `get` through it faulting and a `send` doing nothing (Identifiers and scope). Built as proposed (`corpus/bad/send-unknown-name`, `corpus/bad/kind-name-unnarrowed`), with two names the compile still fixes, since the rule settles them for every instance: the world's name as a first step, and a name the running instance's own body always declares, typed at its kind; an exit in a kind's body is refused only where nothing its destination could be holds actors (`corpus/bad/kind-exit-not-a-place`). How `is()` narrows such a name is 191.
- 181: `not_here` replaces `unreachable`, as built; the spec already no longer carries it.
- 195: `unlock door with gong` is answered `not_here`, since a phrase did read, taking the rest of the line as its noun, as built: "Good". No spec change.
- 196: a reading, then a `which`, then `not_here`, then `unknown`, as built: "Agreed" (Range).

**Decided 2026-09-24, early morning**, now in the spec except where an entry says otherwise:

- 81: a host may unset only the limits whose default is the host's to say, and the wall clock; a `null` for a figured limit is refused at boot, as built. "Fine for now"; a host unsetting a figured limit is considered for language level 2 and later. No spec change.
- 82: the engine reads a destroyed declared place as it reads an absent one: an exit to it does not apply, and a world whose arrival place is destroyed admits no one (Destroying). Built for exits (B28), where a guard reading through a destroyed declared object does not apply either; and for admission (B42): a world whose arrival place is destroyed admits no one, and a returning visitor whose last place is destroyed is displaced.
- 83: `actor` is `sprout.Actor` in every body that binds it, anything more read through `is()`, as built (the bindings table).
- 84: a restatement by a kind that itself composes the property's origin supersedes that origin wherever both reach one composer, so `kind Visitor is Creature, sprout.Visitor { }` restates no `:capacity`; two restatements neither of which composes the other still collide (Properties merge). Built differently; issue to follow.
- 85: a kind that composes the library kind it hides, `kind Visitor is Creature, sprout.Visitor`, is not warned (What it warns about). Built differently; issue to follow.
- 86: blessing is the host's runtime state: a load honours what the host blesses now, both ways, and a world an un-blessing pushes past the host's caps is the host's to handle, "a host impl problem" (Two decisions, of different kinds). Built differently; issue to follow.
- 90: a body may write several `remembers` blocks, their entries combining as one, as built (Declaring and composing, Per-actor memory).
- 91: a name in a kind's body, and in an object a kind gives, resolves at run time from where each instance sits, nearest wins (Identifiers and scope). Eric: "dynamically scoped variable binding. Not great for engineering languages but great for expressive microworld languages." *Built* (`corpus/good/instance-scope`); what the checker can still know is 180, and what a body declares while the world runs Open 190.
- 92: an event is a delivery that runs a handler or a hook, once however many run; one that runs none costs nothing, as built (Events › Bounds).
- 93: the visitor kind's own body may write `without`, as built (Actors and visitors).
- 94: `finally` is a reserved word (Lexical rules), and `syntax/reserved.ts` holds it; nothing in the corpus used it as a name.
- 95: a kind's file is its name in lower snake case, compared exactly, as built (Objects). Eric: this "implies a global namespace for kinds declared in a single microworld": two kinds of one name in one world would want one file.
- 96: the world is declared in the file named for the world's name, from the manifest, `world ways` in `ways.sprout` (Objects); the worked microworld's `world.sprout` is now `printers_shop.sprout`. Built differently; issue to follow, which also settles, narrowly as for a misplaced kind, what a load does with a world in a file not named for it.
- 97: an export for a visitor carries what objects remember about them, as built (Moderation and takedown).
- 98: forgetting a visitor removes what they held with their instance, all the way down, as built (Moderation and takedown).
- 100: `a` wherever nothing writes an article, `the` and `none` only where written, as built: "no good way to do it really" (Addressing and display). The vowel is 121.
- 101: among things written alike, the nearest is meant, and among equally near ones a draw from the turn's seed, "so people don't depend on unintentional tie breaks" (Spawning, which said the first in its container's order). Built differently: the draw; issue to follow. *Built:* the parser draws among the equally near from the turn's stream, one step, and a line reads the same for the same seed (Open 197 says what equally near is).
- 102: "only what the player names": a noun nothing in range answers to is answered with a new stock line on `sprout.World`, `passage not_here default { You see nothing like that here. }`, bound `actor` and `here` as the parser's other answers are (Range, the bindings table, the standard library). `unreachable` is read as replaced and leaves the standard library (181). Built differently; issue to follow. *Built:* `sprout.World` writes `not_here`, and the parser says it, as a notice to the actor alone, of a line whose phrase reads and whose noun nothing in range answers to (Opens 195, 196).
- 103: the example was wrong: Places inside places now writes `exit in "through the fur coats" -> narnia`, and the closed set stands, as built; B28 refuses `exit through` at the word with that line as the remedy (`corpus/bad/exit-not-a-direction`). Every other `exit` in the spec is in the set; the Links example's `link onward` and `link back` are not either, and are Open 150's, left as they are.
- 104: a world's verb that shadows a library's takes its phrases with it, as built (Libraries and namespaces).
- 110: an engine defect is a fault, told as any fault and marked the engine's for the host, as built (Faults).
- 111: no world version is stored; a committed write turn names the visitors whose views it made stale, as built; "could change when we get more usage reports" (The view).
- 120: `$index` counts from 1, as built (Conditionals and loops).
- 121: the default `a` is written `an` before a name beginning with a, e, i, o or u; a written `article` still decides (Addressing and display). *Built:* the default article is `an` before such a name wherever the engine writes a thing, and a written `article a` stays `a`, as the study's `ukulele` has it (`corpus/good/grammar`).
- 122: each engine line is given exactly what the engine binds where it says it, as built, `fault` included with `actor` and `here`; `not_here` (102) takes `unreachable`'s place (the bindings table).
- 123: a passage a slot renders is given `actor` and `here` where the slot has them, and nothing else, as built (Slots).
- 124: one `prose` line per body and one body per file, as built; "for now, may loosen" (Passages).
- 126: `here` is typed `sprout.Place` (the bindings table). A place is any object declaring `contains actors` (Places), so an actor's place need not compose `sprout.Place`; the issue says how `here` is typed there. *Built:* `sprout.Place` in a role's body, a passage it says and the engine's lines, wherever every kind holding actors composes it, and the object type with a remedy where one does not (Open 200); see "Found while giving `here` its kind".
- 127: the literal cap holds `refuse` in quotes too (Static caps). *Built:* in the words of `say`, `tell` and `text` (`corpus/bad/refuse-too-long`).
- 130: a place's first tick hands `elapsed` 0, as built (Ticks).
- 131: `elapsed` spans a stretch nobody was there, as built and as Ticks already said. No spec change.
- 140: a catch-up part that faults is abandoned and its wake consumed, and the other objects' due wakes are still delivered in the same catch-up (Absence, Turns, Faults). The faulted object's later due wakes wait for live time, as catch-up's one wake per object already has it, so this needs no question of its own. *Built:* see "Found while building wakes".
- 141: oldest first is by when a wake fell due, then by the serial it was asked under, as built (Absence).
- 142: a whole number written out, the plural unit only, and a wait under the host's floor raised to it with nothing said, as built (Wakes).

**Decided 2026-09-23, night**, now in the spec:

- 7: memory is declared in a `remembers` block whose entries are written exactly as properties, `remembers { :visits 0 min 0 max 99 }`; `remembers` is a reserved word, and the colon no longer keys an entry. Built: the block's entries are read by the property reader, and `:remembers [ … ]` is refused with the block its entries make as the remedy (`corpus/bad/remembers-old-form`); see "Found while giving memory its block".
- 67: each kind in a file of its own, named for it; enums, verbs and messages in any file; libraries alike; nothing imported, so nothing circular. Booked until it becomes a problem. Built, the naming and the world's file left open as 95 and 96; see "Found while giving each kind its file".
- 76: a spawned instance's contents are told nothing; the instance gets `:spawned` and its container `:entered`, as built.
- 78: the `objects` cap counts every copy a kind gives a declared instance, as built, and `places` the copies that hold actors.
- 79: a kind's content made of that kind is refused at compile, as built; a loop the compiler cannot see would fault at a cap.
- 80: the visitor kind's own body may declare properties, a `remembers` block, passages, a grammar block and a `describe`, and is refused a handler, hook, tick, wake, play, guard or pass rule. Built: a play and a guard in its own body are refused at the member, and what it composes keeps its behaviour; the other members are refused as each becomes one the grammar reads (see "Found while keeping a person's kind free of behaviour").

**Decided 2026-09-23, late**, now in the spec:

- 50: `allow` in a `permit` ends that `permit`, consenting, as built.
- 66: `finally destroy self`'s details as chosen.
- 68: a kind's contents compose as chosen.
- 69: `:departed` and `:arrived`, as named.
- 70: the world's name may be a path's first step, and no other. Built: `visitors arrive at printers_shop.hall` reaches the hall, the world's name as a later step is refused, and the hidden-name warning names the path through it.
- 71: a refused `move`, and a refused `act`, end the body they stand in: the first refusal ends the work.
- 72: a destroyed declared object is gone for good; its id is never made again, a reference to it is a fault when read, and the compiler warns about `destroy self` on a declared object. Destroying is meant for what was spawned. Built: a tombstone per destroyed declared id, load making none again, the warning, and the fault where a name is read (see "Found while keeping a destroyed declared object gone").
- 73: every composer of `sprout.Actor` is an actor and may `act`; the visitor kind composes `sprout.Visitor`, and every other actor is an NPC. What "no behaviour" covers was decided the same night (80).
- 74: a value role in an `act` is filled only by a binding. A friendly noun form for options a visitor types is B27's to settle. *Built (B27):* an option is typed as its humanised spelling, `old press` for `old_press`, and binds only where a participant's `from` hears it (see "Found while building the command parser").
- 75: an actor found at load where actors cannot stand is a loud engine error.
- 77: a place and a surface are sent the place's messages, as built.
- 7, in part: `Ward.iron` or `:iron`, the author's choice.
- 67, in part: one kind per file, libraries too.

**Decided 2026-09-23, evening**, now in the spec:

- 13: names never change while a world runs; renaming anything in source, the world included, changes the ids under it, as the declared-path id already implies.
- 14: the visit is a UUID the host mints, keyed by the host's opaque id for the person, so a returning person finds it or starts a new one.
- 21: the only actors are visitors and NPCs; an object composing `sprout.Actor` that does not compose the visitor kind is refused, and so is a `spawn` of such a kind. *Superseded by 73:* every other actor is an NPC, and what is refused is an object or a `spawn` composing `sprout.Visitor`.
- 22: what objects remember about a visitor is opaque to that visitor (no memory panel; a future debugging view may show it to the author), and it is erased only when the visitor is deleted.
- 26, 27 and 55: what a destroyed object held is destroyed with it, all the way down, dormant instances included, with no `:entered`; a visitor anywhere inside faults the destroy. The `:entered` contradiction is gone with it.
- 42: the engine's cycle refusal speaks through a new `inside_itself` default on `sprout.World`, "{item} cannot go inside itself."
- 43 and 62: an actor is only ever inside something that declares `contains actors`; declaring an NPC elsewhere is refused, a move there is refused before any guard, a spawn there faults, and an exit to something that does not hold actors is a compile error. *Built (B28):* the exit refused at its destination (`corpus/bad/exit-to-non-place`).
- 44: see 69; the one who moved hears neither notice.
- 49: declared order for the two passes is as built.
- 53: an exit guard that reads out of range does not fault the poll; the exit does not apply. *Built (B28).*
- 56: `finally destroy self` is in the spec (details at 66).
- 57: the world's body is one block in one file, and a world whose file fails to compile admits no one (the kind-file rule is 67).
- 58: a kind's body may hold objects; each instance, declared or spawned, starts with its own copies (details at 68).
- 59: the standard library's default yields to another library's, and the world's own kinds count as another library.
- 60 and 61: an NPC's `say` is framed in fixed engine words, and an NPC's reading that says nothing has no output (`nothing_happens` answers only a person).
- 63: `as actor` only on a kind composing `sprout.Actor`, at level 1; a later level may relax it.
- 64: a `say` in a participant's `do` where the actor is an NPC goes to the NPC's audience, from the NPC.
- 65: the parser's module specs, reaching their module only through `parse.ts`, do not count as exercising it directly; they are to call the module's functions.

The code that differs is an issue each; see the items below.

**Decided 2026-09-23**, now in the spec:

- Range reads the path rule: an object reaches a target when nothing strictly between them on the tree refuses.
- Range goes nearest first, a ring at a time, breadth-first within a ring and each container's contents in its order; a broadcast delivers in that order.
- What a kind leaves out with `without` stays left out in every kind composing it; a copy of the member reaching the composer through another kind still runs.
- `visitors arrive at` never names the world, even one that declares `contains actors`.
- An object hiding one of its name further out is warned about at the inner declaration, naming the outer one's path; objects in sibling containers hide nothing.
- Composition is written with `is`, `kind Creature is sprout.Actor, Fragile`; the colon form is refused with the `is` form as the remedy. Built.
- Pending wakes per object is a host cap, 1 by default, and a `wake` past it faults as a `spawn` past live instances does.
- A library's `version` is semver.
- Objects are declared inside the body of what holds them; the parse tree is the containment tree; `in` and its refusals and the `container` absent row go. Built.
- An identifier belongs to the body it is written in and is seen from inside it at any depth, nearest wins; a dotted path names anything deeper, and the world's `visitors arrive at`, written in the world's body, names a nested place by one. Built for the tree and `visitors arrive at`; bodies resolve identifiers with B32. *Built (B32):* a body resolves an identifier or a path from where it is written.
- Kinds stay at a file's top level; an `object` at a file's top level is refused. The world's body is one block in one file, and a file holding only kinds and enums needs no world. (That a kind's body may hold objects was decided later the same day, above.) Built, a kind's objects refused as not read yet.
- Refused: an object inside something whose kind does not hold things; the world's name as a step of a path other than the first (70), or as an object's name; two objects of one name in one body. Built.
- What `objects`, `places` and `kinds` count, under Static caps.
- A `spawn` of `sprout.World`, or of a kind that composes it, is refused; so is `destroy self` in the world's own body.
- A destroyed object has no effects: messages queued to it, messages it sent that have not been delivered, engine messages naming it as their `from`, and its pending wakes are all dropped. (What it held was decided later the same day, above: destroyed with it.)
- A `spawn` whose target is out of range or does not hold things when it runs is a fault, never nothing, since there is no null to bind; a spawn into the world is allowed where the world is in range.
- The absent table has a row for a kind named in a `spawn`: the `spawn` faults when it runs.
- The host's live-instance bound counts every instance it stores, dormant ones included.

Range's path rule differs from what is built or being built, and the code catches up.

**Recorded since the sweep, awaiting Eric.** Found while building containment (B13), each decided the narrow way:

- **Whether `contains actors` implies `contains`.** The standard library's own `kind Place` declares only `contains actors` and holds a bench, so it implies it, and `contains` is true wherever either was written.
- **Whether writing either of them twice is worth saying anything.** *How members combine* calls both idempotent under composition; one body writing the same line twice is treated the same and nothing is said. A warning for a redundant one is B50's to add.
- **Whether a world may declare `contains actors`, and so be a place itself.** Nothing forbids the line, so it is accepted and the world is a place if it says it is. Decided 2026-09-23: the line is still accepted, but visitors never arrive at the world itself, whatever it declares; `visitors arrive at` naming it is refused in either mode, and the remedy names a place inside the world where there is one.
- **A block comment never closed, or inside another.** Decided 2026-09-22 and now under Lexical rules: refused at its opening; no nesting.
- **How a list of lists keeps its no-duplicates rule.** Decided 2026-09-22 and now under Lists: same elements in the same order, inside `add`, `remove` and `includes` only, never as an `==`.
- **A bundle with no `world` declaration, or two.** Decided 2026-09-22 and now under The manifest and the absent table: refused at publish; at load the world admits no one.
- Not a hole: a world is not refused for holding nothing, since `sprout.World` declares `contains` and every world composes it (explicitly, as of the sweep); a world composes like a kind, so it holds whatever anything it composes holds.

Found while building composition (B19), each decided the narrow way and awaiting Eric:

- **`object` is both a type name and a declaration word.** A type position that meets the start of a declaration (`object bench is …`) says the type is missing rather than reading the object as the type.
- **An object with no kinds, or no body.** One that names no kind is refused ("An object names its kinds and its container"); one with no body is allowed and has nothing of its own.
- **A kind's braces.** Required, even when empty: `kind Marker { }`.
- **An object's own name.** Any lower-case word; the reserved-word rule is read as covering options and bindings only.
- **A bare `World`.** Resolves as any bare kind name does, to the world's own `World` if it declares one and else to `sprout.World`, so on a kind or an object it is refused as `sprout.World` is; on a world it still does not count as writing `sprout.World`.
- **The closure's order.** Depth-first, left to right, each kind at its first appearance, the composer last (post-order): `D is B, C` with `B is A` and `C is A` runs `A, B, C, D`.
- **A kind that composes itself.** Refused once, at the kind as written that closes the loop, naming the kinds it runs through; the other kinds in the loop are said nothing more about.
- **The same kind twice in one composition list.** Refused at the second, rather than read as one path.
- **Who a restatement's origin is.** The restating kind becomes the property's origin, so composing `Crate is sprout.Container { :capacity 40 }` beside `sprout.Container` collides on `:capacity` again. *Decided 2026-09-24 (84), the other way:* a restatement by a kind that composes the origin supersedes it, so `Crate` beside `sprout.Container` no longer collides; the code catches up.
- **What counts as changing a property's type in a restatement.** A different integer range is a change, and so is remembered versus plain.
- **Origins that disagree in type.** Cannot be merged by restating; the refusal says to compose only one of them.
- **An object in a library.** Refused, as a world is: a library holds kinds for the world to make things of.
- **An object's anonymous kind.** Named for the object, in the world's library, and the origin of what its body declares.
- **A kind composing one that is absent at load.** It is not composed and its body is not read; the objects made of it are absent with no gap of their own beyond the `kind-in-composition` one.
- **What `without` may name.** Only the members whose several sources all run: `on :m`, `changed :p`, `depart`, `release`, `accept` and `as <role> for <verb>`. `from` names the kind that declares the member, which must be in the composer's closure and not the composer itself. Guards are read, so `without depart from X` works where `X` writes a `depart` itself; every other form is refused with "has no … to leave out" until B24 reads roles and B32 handlers and hooks. *Built (B32):* `without on :m` and `without changed :p` leave out a composed kind's handler or hook.
- **Where a library lives on disk.** Still unspecified for any library's vendored copy in a world folder, so the CLI reads none from one. The CLI carries the standard library itself and sends it whenever the manifest names `sprout`, and `sprout init` pins it.

Found while building identifier scope (B14), each decided the narrow way and awaiting Eric:

- **Arriving somewhere that is not a place.** What it refuses does not list it. `visitors arrive at` naming an object whose kind does not hold actors is refused in either mode, at the path's last step, since nothing is missing: the spec's Places makes a place whatever declares `contains actors`, and visitors have to be somewhere they can stand.
- **An arrival place whose kind or container is absent.** It is the `place-of-arrival` row, the same as a name nothing answers to: refused at publish, and at load recorded, with the world admitting no one. At publish, where what left it absent has been refused already (its kind, or where it was put), that refusal is all that is said, as the tree says nothing more about what an absent object holds.
- **Identifiers in a named kind's body.** A kind has no place in the tree, so a name inside its body has no vantage until an instance does; B28 and B32 decide how exits and sends inside a kind resolve. *Built (B32):* a kind's body names what the kind gives each instance, and then what the world's body holds; see "Found while building events" below. *Built (B28):* an exit's destination in a kind's body resolves the same way. *Decided 2026-09-24 (91), the other way, and built:* resolved at run time from each instance's place, exits included; Open 180 and 190.
- **A binding and an object of one name.** Whether `let key = …` hides an object called `key` in reach, or is refused, is B23's and B32's. *Built (B32):* a binding hides an object of its name, since the scope is asked first.

Found while building range (B15), each decided the narrow way and awaiting Eric:

- **Crossing into the asker's own contents.** The spec says a container's other contents are crossed into where they pass, and says only that the asker reaches its own contents. Built uniformly: the asker's own contents are always reached, and what they hold only through their own rules, so a pouch in a visitor's hands keeps its gem out of range while it is shut.
- **What `get`, `each` and a command's nouns ask of a pass rule.** They carry no message, so they consult `pass any`.
- **What a broadcast leaves out of "the same walk".** Events, messages and the bus › Sending delivers to neither the sender nor a container outward that refuses, which the walk reaches only as a surface, so a reached node records how it was reached (`self`, `held`, `surface`, `passed`) and B32 drops the first and the third. *Built (B32):* a broadcast is sent to what the walk reaches `held` or `passed`.
- **A visitor standing directly in a world that declares `contains actors`.** The world's rule refuses, so it reaches the world as a surface only, and nothing else in it.
- **"The container's order" for a moved or spawned object.** Declared objects keep their declared order; where a moved or spawned one goes in its container's order is B16's.
- **What a membership test costs.** Whether one object is in range of another is answered by the path between them, one step per node climbed, not by a whole walk.
- **`each` over a container that is in range but refuses, seen from outside.** Its contents are out of range, so it visits nothing.

Found while building the state model (B16), each decided the narrow way and awaiting Eric:

- **The minted id's form.** `<world>#<n>`, as `printers_shop#12`, from one persisted serial counter per world that spawns, arrivals and wakes all draw from, so no id is ever reused. A name never holds `.` or `#`, so a minted id and a declared path cannot collide.
- **A stored value carries the type key it was written under.** A property retyped from `string` to `Ward` falls to its default even when the stored text is one of `Ward`'s options, so a value is never reinterpreted under a type it was not written as.
- **A value outside a narrowed range.** Falls to the default, and is not clamped into the new range: the spec's "no longer fits" is read as no longer fitting, and `adjust`'s clamp is for a write, not for reading state.
- **A list that no longer fits.** Falls to the default whole, when any element misfits or it holds more than the host's current cap, rather than keeping the elements that still fit.
- **Times in stored state.** Whole host seconds, as `elapsed` is.
- **Pending wakes are stored as a list.** Decided 2026-09-23: how many an object may have pending is a cap of the host's, 1 by default, rather than one per object as a rule of the language; the stored form is a list bounded by that cap. `lastTick` is kept on every instance's record and stays null for anything that is not a place.
- **The container's order.** Declared objects still where they were declared come first, in declared order; then everything that arrived, in arrival order, each arrival drawing the world's next serial. So a declared key taken and put back goes last, and a spawn goes after what was there.
- **"Its kinds."** Stored as what the instance was made from: the world, a declared object, a visitor, or a spawn of a kind named by its qualified name. The closure is re-derived from the bundle at every load, never stored, so a kind that composes something new applies to instances already made.
- **A property no longer declared.** Dropped at load, and said so; it is not kept beside the declared ones.
- **A default changed in source.** Does not change existing instances: every instance holds every property it declares, so there is no unset value for a new default to show through. Only instances made after the change start at it.
- **A declared object moved at run time into something now absent.** It is unreachable with its container, and is not returned to its declared container; it comes back where it was when the container does.
- **A visitor's instance after `visitors are` changes.** Decoded against the world's current visitor kind, whatever it was made under, since a visitor records that it is a visitor and not which kind it was.
- **An instance whose container no longer declares `contains`.** Stays where it is; nothing is moved at load.
- **Which item puts the composed world and the visitor kind into the bundle.** *Answered by B17:* `compileBundle` composes the world and resolves `visitors are`, and the bundle carries both, so the world's instance holds its composed properties and a visitor's instance decodes; each is dormant only where a loaded world admits no one for want of it.

Found while building actors (B17), each decided the narrow way and awaiting Eric:

- **How the standard library travels.** From the CLI as one known copy, blessed, and pinned by `sprout init`; every manifest names it, and one that does not is refused for a missing `sprout.World`, with a remedy that names the library. Decided 2026-09-23.
- **What visitors are made of.** The visitor kind must compose `sprout.Actor` and be the world's own kind. `visitors are Hall`, where `Hall` is not an actor, is refused at the kind; so are `visitors are sprout.Actor` and any other library's kind, since the world's own kind is where "whatever this story needs a person to have" is written. Decided 2026-09-23, now under What it refuses. *Decided 2026-09-23 (73), and built:* the visitor kind composes `sprout.Visitor` and is the world's own; `visitors are sprout.Visitor` is refused as any library's kind is.
- **A visitor kind absent at load.** The absent table has no row for it. It is treated as the `world` row's consequence: the world admits no one, and the gap is recorded under `world`. At publish it is refused, and not said while one of the world's own files was refused at the first tier, as for the arrival place. So is a kind the world composes that nothing declares. Decided 2026-09-23: the world admits no one, as for the arrival place, now a row of What absent means.
- **Where an NPC may be declared.** An NPC needs no place among its ancestors, and an actor may be declared inside something that does not hold actors (a cat in a basket); both are accepted, since where an actor may be moved is B22's and B42's. Decided 2026-09-23. *Decided 2026-09-23 (43, 62), the other way, and built:* an actor declared inside something that does not hold actors is refused.
- **Erasing what the world remembers about a visitor.** Not built: the spec gives only the memory panel, which lists everything every object remembers about an actor, dormant objects included, and only what was written, a remembered property never written about them reading as its default. Whether a visitor may erase it, and how, is open; Eric asked for more on the question, 2026-09-23. *Decided 2026-09-23 (22):* no memory panel; memory is opaque to the visitor and erased only when the visitor is deleted.
- **An actor that is not an NPC.** Whether an object composing `sprout.Actor` but not the visitor kind is an actor that cannot `act` is open; `isNpc` answers only whether an object composes the visitor kind. Eric leans to yes and asked for more on the question, 2026-09-23. *Decided 2026-09-23 (21), and built:* there is none; such an object is refused, and so is a `spawn` of such a kind. *Decided 2026-09-23 (73), the other way, and built:* every object composing `sprout.Actor` and not `sprout.Visitor` is an NPC, and `isNpc` is gone; what is refused is an object or a `spawn` of a kind composing `sprout.Visitor`.
- **`sprout.Actor` has no pass rule yet.** It writes its three guards, and until B32 lands it declares no `pass any (false)`, so a pocket is visible: what a visitor carries is in range of the people beside them. Accepted for now, 2026-09-23. *Built (B32):* `sprout.Actor` writes `pass any (false)`, and the engine's pass rule reads what a kind writes.

Found while building spawning and destroying (B18), each decided the narrow way and awaiting Eric:

- **A spawn target of the bare object type.** Accepted at compile and checked when the spawn runs, since the worked microworld writes `spawn Sheet in here` and `here` is the object type. A target known to be a value or a set is refused, and so is one whose kind does not write `contains`.
- **Spawning an actor.** An actor kind, or the world's visitor kind, may be spawned; the spec's Spawning names no exception. *Decided 2026-09-23 (73), and built:* an actor kind may be spawned, and a kind composing `sprout.Visitor`, the visitor kind among them, may not.
- **Where `spawn` and `destroy` are refused.** What it refuses forbids both in a guard, a `permit` and `describe`; the items that read those bodies (B22, B24, B31) refuse them there, with a `let` naming a spawn.
- **The target's grammar.** `in` takes a binding or an identifier, or a dotted path to one, written without spaces around its dots as a dotted path elsewhere is. `destroy` takes only `self`, and anything else after it is refused once.
- **A dotted target.** Refused, as a name nothing in the body answers to, until identifiers inside bodies resolve (B32). *Built (B32):* a dotted target resolves as any path in a body does.
- **A message naming a destroyed object as `from`.** Dropped. Decided 2026-09-23: a destroyed object takes everything pending on it with it — messages queued to or from it, engine sends naming it as `from`, and its pending wakes.
- **The destroying body's own sends.** Dropped. Decided 2026-09-23: a destroyed object has no effects, so what it sent in the body that destroyed it goes with it.
- **What a destroyed object held.** Falls to its container without consent, as Destroying says, each thing placed last in that container in the order it was held, so the fallen keep their order after whatever was already there. Decided 2026-09-23 as the spec is written, until Eric confirms a change: his preference, pending, is that what it held is destroyed with it. *Decided 2026-09-23 (26), the other way, and built:* destroyed with it, all the way down.
- **The `:entered` for what fell.** One per thing, in falling order, `from` the destroyed object. The destroy returns them and the queue drops them under the no-effects rule above, since their `from` is gone, so the container is never told. Destroying still says the container receives `:entered` for each thing that fell, which that rule contradicts; the contradiction is recorded for Eric, and the rule lives in one place, the queue's, whichever way it goes. *Moot, 2026-09-23 (55), and built:* nothing falls, and a destroy sends nothing.
- **A visitor standing directly in a destroyed object.** A fault, whatever the object is. Decided 2026-09-23. A visitor further in — in a box in the room — moves with what holds them. *Decided 2026-09-23 (27), and built:* anywhere inside, now that contents are destroyed, checked before anything is removed.
- **`destroy self` on a visitor's own instance.** A fault. Decided 2026-09-23.
- **Spawning into the world.** Allowed where the world is in range of the spawner, which it is when every container between them passes. Decided 2026-09-23.
- **A spawn target out of range, or holding nothing, when the spawn runs.** A fault. Decided 2026-09-23. A target that is not live, the spawner's own self included, is out of range.
- **A kind absent at load named in a `spawn`.** The `spawn` faults when it runs. Decided 2026-09-23, as a new row of What absent means, which the spec and the compiler's absent table do not carry yet.
- **Dormant contents of a destroyed object.** Left where they are: a dormant record is kept untouched, so it still names the destroyed object as its container. When its kind returns it decodes into a container that no longer exists and is not live; where it goes then is open. *Decided 2026-09-23 (26), the other way, and built:* destroyed with it, its stored record removed.
- **How the host's bound reaches a turn.** As a number per turn, recorded with the turn (B40), not a callback into the host, so a replayed turn faults exactly where it did.
- **What a spawn tells.** The container `:entered (item, from)`, the new object as `item` and the spawner as `from`, then the new object `:spawned (from)`, in that order; the engine is the sender of both.
- **`destroy self` twice.** Is once: it takes effect when the body ends, and a second in the same body changes nothing.
- **Memory keyed by a destroyed NPC.** Kept: what an object remembers about an actor is its own, and a destroyed actor's id is never minted again, so the entry can name no one else.

Found while building passages (B20), each decided the narrow way and awaiting Eric:

- **A composer's own `default` beside a composed non-default of the same name.** How members combine says a composer's own exclusive member always replaces what it composes, and that a default yields to any passage of the same name from any other source; for this case they disagree. Built: the composer's own applies, and stays a default, so it still yields further up. Decided 2026-09-23, now under How members combine.
- **A default rewritten over a default.** `W: K, A` where `K: A` wrote its own default over `A`'s: two defaults from two origins reach `W`, and they collide, as a restated property from two origins does. Decided 2026-09-23. Where `A` is the standard library, the rule below would let `K`'s default win instead if the world's own kinds are read as another library; built as a collision until Eric confirms that reading, and the refusal says the standard library's yields to another library's.
- **Where a library's replacement lines go.** A register library composed onto the world replaces `sprout.World`'s lines but not `sprout.Place`'s or `sprout.Actor`'s, since a passage is the kind's; the spec's "a second library supplying the lines the first left out" says nothing about which kind it is composed onto. And a register that writes its lines as `default` collides with the standard library on every shared name, so under the rule as written a register replaces stock lines only by writing non-defaults. *Decided 2026-09-23, now under How members combine, and built:* the standard library's default yields to another library's, so a register's defaults replace the stock lines and still yield to the world's; two defaults from two other libraries still collide. Which kind a register is composed onto stays open until the library work.
- **A typo in an override.** `passage nothing_happen` in the world overrides nothing and is not refused; no warning is listed. Decided 2026-09-23: a warning, B50's, now under What it warns about.
- **After a refused collision.** The first contender in list order is kept, as the property collision keeps its first, so a kind composing the refused one hears nothing more about that line; and when non-defaults collide the refusal names only them, not the defaults that gave way.
- **`default` on an object's or the world's own passage.** Accepted and recorded, though nothing can compose it.
- **A passage's name.** Any lower-case word, as an object's name may be; only `default` in the name position is read as the keyword. A reserved word as a passage's name is not refused, as it is not for a message or a kind today; a verb's is.
- **A lone `}` in prose.** There is no way to write one: only `\{` is an escape, so an unmatched `}` closes the passage and `\}` is refused, stepped over with the character after it as a bad escape in quoted text is.
- **Quoted text in a slot.** Ends at its line; in prose a quote is a character. *Built (B29):* a slot reads quoted text as a string, which renders as written.
- **Where a passage's header may sit.** Its words on the `passage` line, the brace on that line or the next; a header left without braces then never takes the next member for its own.
- **`{item}` in `arrives` and `leaves`.** Untyped by the binding table. *Built (B29):* the engine says a place's notices with `item` bound to what moved, an object, and nothing else (Open 122). *Decided 2026-09-24 (122):* as built.
- **The standard library's `0.1.0`.** Stays `0.1.0` while its source changes before release. Decided 2026-09-23.
- **A world whose composed kinds lack an engine passage.** A forked standard library without `fault` or `displaced`: what the engine says then is B34's and B48's. *Built (B34), for `fault` and `unseen`:* the stock line in fixed words. *Built (B42), for `displaced`:* the same, "The place you were standing is gone."

Found while building library namespacing (B21), each decided the narrow way and awaiting Eric:

- **A world namespaced as a library it pins.** Its own bare names and the library's could not be told apart, so the manifest is refused. The spec says only that a world's own declarations are unqualified.
- **A library's message written qualified.** Libraries namespace messages, but no syntax writes one qualified (`send x sprout.:m`?): a message name is a bare symbol wherever it is written, so a library's message is reachable only unqualified, and only when the world declares none of that name. Open until B32 reads sends. *Built (B32):* still no qualified form; a send reaches the body's own library's message, then the standard library's, as a handler does.
- **What the shadowing warning covers.** Kinds, enums, messages and verbs, which is everything libraries namespace.

Found while reading, checking and composing the consent guards (B22), each decided the narrow way and awaiting Eric:

- **A guard's parameters.** Positional, named as the author chooses: one for `depart`, two for `release` and `accept`, and any other count is refused. `to` and `from` are reserved words, and the spec's own guards name parameters with them, so those two may name one and no other reserved word may. `_` for a parameter left unnamed waits for the lexer to read it, as a handler's does (B32); until then every parameter is named. *Built (B32):* the lexer reads `_` alone as a name, and a guard's `_` binds nothing, as a handler's does.
- **What follows `allow` or `refuse` in a block.** Accepted, and never runs; a warning for it is B50's.
- **A guard written twice in one body.** Refused at the second, as a passage written twice is, and the first kept.
- **`release` or `accept` on something that holds nothing, and `depart` on the world.** Accepted, and never asked, since nothing can leave or enter it and the world never moves.
- **How a `without` travels.** Decided 2026-09-23: a `without` follows the kind into its composers, and in a diamond it removes only the copy that came through the kind that wrote it; a copy of the same member reaching the composer by another path still runs. `KindRef.suppressed` holds a kind's own lines only, since a composed kind's have already shaped that kind's members.
- **Where `refuse <name>` looks.** At the passages of the kind that wrote the guard, composed ones included; the evaluator looks the passage up on the refusing instance's kind at run time, so a composer's own line replaces a library default.
- **What a guard may say as a statement.** A call that writes or remembers is refused as a write, and any other expression standing as a statement is refused as reading without doing, as everywhere. `say`, `tell`, `send`, `move` and `act` are not read as statements yet; in a guard they are refused as statements this compiler does not read, until the items that read them refuse them there by name. *Built (B30):* `tell` and `text` are read, and refused in a guard by name.

Found while evaluating expressions and running a guard's body (B22), each decided the narrow way and awaiting Eric:

- **Whether `&&` and `||` short-circuit.** They do: a left side that decides leaves the right unevaluated and uncharged. An expression only reads, so the only difference an author can see is the steps a turn is charged.
- **`+` or `-` past the integer range.** The types give every integer the range −2,147,483,648 to 2,147,483,647 and say nothing of a sum outside it. It is a fault, as a `set` out of range is, rather than wrapped or clamped, since either would make a comparison quietly wrong.
- **An integer literal past that range.** Neither the parser nor the checker refuses one standing alone, as in `let n = 3000000000`; it evaluates to what it writes, and faults only when `+` or `-` is applied to it. Refusing it at compile is the checker's to add.
- **What one step is.** Every statement executed, every `else if` tested, and every expression node, which includes the `:p` a `get` or `recall` names and the kind an `is` or `count` names. `count(K)` looks through a container's contents without a step for each thing it looks at, since Runtime budgets lists no such charge.
- **A `get` through a binding out of `self`'s range.** Range is what an object may read with `get`, and a guard's `item`, `to`, `from` and `mover` may be out of range of the party asked. The evaluator reads through any binding without asking; whether such a read faults, as the exit's `when` question above has it for an identifier, is open.

Found while moving a thing through consent (B22), each decided the narrow way and awaiting Eric:

- **Whether composed guards run past a refusal.** Consent under composition and One rule for many opinions say every contribution runs and the first refusal speaks; The three roles says the engine stops at the first refusal. A guard only reads, so the two differ only in the steps a turn is charged and in a fault a later guard would raise. Built: the first refusal stops the poll, within one kind's list as between the three roles.
- **The engine's cycle refusal has no stock line.** After the move says a move that would make a container hold itself is refused before any guard, and gives no words. Built with fixed text, "{item} cannot go inside itself."; an `inside_itself` default on `sprout.World` is proposed, so a world can say it in its own voice. *Decided 2026-09-23 (42):* the `inside_itself` default on `sprout.World`. Built: the refusal speaks through the passage as it applies on the world's composed kinds, rendering with `item`.
- **An actor moved into something that does not hold actors.** Refused by the engine before any guard, as structural, with fixed text, "{item} cannot stand in {to}.", so a person is never carried in a hand or put in a basket. The world holds actors only where it writes `contains actors`. The alternative, allowing it and leaving it to a guard, is Eric's to choose. *Decided 2026-09-23 (43):* refused as structural, as built; and an exit to something that does not hold actors is a compile error.
- **What the engine's refusals name an object by.** A declared object by its identifier, the world by its name, and anything made while the world runs, a visitor's instance included, by its kind's name. *Built (B29):* the refusal is a one-line passage rendered with the objects it names, which a slot names as it names anything.
- **A move that cannot be asked about.** A fault, writing nothing, as a spawn's is: the thing is the world, or a visitor who is away; the thing or the destination is not live or out of the mover's range; the destination holds nothing.
- **A move to the container the thing is already in.** A move like any other: the guards run, it goes last in that container under a new arrival, and all three messages are sent.
- **A place whose kind has no `leaves` or `arrives`.** One that holds actors without composing `sprout.Place` and writes neither: no notice for that side. The description is still sent.
- **When a place speaks.** Only when the thing is an actor and both the container it left and the one it entered hold actors. An actor moved out of a basket onto the floor of a room, or into a wardrobe from a box, is told nothing and nobody hears it, though After the move and Places each say only what happens between two places.
- **The description to the one who moved.** A `described` notice naming the new place. *Built (B31):* the engine describes it to the mover once the queue is empty; see "Found while building descriptions".
- **Who hears `leaves` and `arrives`.** The actors directly in the place, not those inside something in it; the one who moved left out; NPCs included, though prose addressed to one goes nowhere; and a mover other than the thing hears as anyone there would. *Decided 2026-09-23 (44), wider:* every visitor in range of the place reads the text and every other object in range is sent a message (names at Open 69). Built; what it decided on the way is under telling a place's range, below.
- **A destination out of range across places.** A move's destination must be in the mover's range, and while the world refuses one place is out of range of another, so a `move` from one place to another faults. How `go` crosses an exit to another place is B42's. *Built (B28):* an exit is the reach, so a move through one asks no range of its destination, which must still be live; see "Found while building exits".
- **Where `refuse <name>` looks when it runs.** As recorded above, on the refusing instance's kind, so `good/consent`'s visitor kind says `hands_full` in its own words over `sprout.Actor`'s.

Found while reading a verb and checking it against itself (B23), each decided the narrow way and awaiting Eric:

- **`many` beside a kind.** `role tools: Rib many` is accepted, a set of things of one kind; `many` and `optional` follow the filler, in either order.
- **`optional` on a verb that has phrases.** Refused: the phrases decide which tools are optional, and `optional` is written only on a verb with none. Decided 2026-09-23, now under What it refuses.
- **A phrase that leaves out the target.** Refused: every phrase names the first role. Declaring a verb says a phrase "need not fill every tool", which is read as tools only. Decided 2026-09-23, now under Declaring a verb and What it refuses.
- **`many` on the target.** Accepted, as in `verb sort { role targets many  "sort [targets]" }`; the spec restricts `many` on value roles only. Decided 2026-09-23, now under Set roles.
- **`optional` on the target of a verb with no phrases.** Refused: the target is never optional, which is what the rule above means for a verb with phrases. `optional` on a value tool of such a verb is accepted, though every value tool is optional already. Decided 2026-09-23, now under Optional tools.
- **Two phrases that mean the same, and an empty phrase.** Refused. Two are the same when their words and slots are, however they are spaced: a run of words between slots is trimmed and its spaces made single.
- **What a slot holds.** Exactly one role's name in lower case: `[ target ]`, `[the target]` and `[Target]` are refused, and so is a `]` that closes no slot. A slot next to a slot, `"[a][b]"`, is accepted; which noun fills which is B27's. *Built (B27):* the earlier slot takes the longer run first, and the first placement whose every slot fills is the reading.
- **The phrase cap.** Counts the phrase as it means, after escapes, in characters, since that is what a visitor types.
- **A verb with neither roles nor phrases.** `verb dance { }` is accepted: only `act` performs it, and only the actor plays it.
- **`exit` as a filler.** Read on any verb by the parser, and refused past the first tier on any verb but the standard library's `go`, at `exit`, in any library, the standard one included.

Found while resolving verbs across libraries (B23), each decided the narrow way and awaiting Eric:

- **Whose verb may take an engine verb's name.** Reserved names says a world's may not; built so that no library but `sprout` may either, since a pinned library's `look` would reach the world only qualified and would read as the engine's. Adding to or translating an engine verb's words is then only by forking the standard library.
- **A world's verb of a standard library verb's name.** A shadow, warned, as for kinds, enums and messages; not a refusal, since The engine verbs says a world that wants `take` to do something else writes its own. Which of the two a typed phrase then reaches, and whether both are offered, is B27's. *Built (B27):* the world's own; the library's phrases are not typed (Open 104). *Decided 2026-09-24 (104):* as built.
- **Writing a verb with its library.** Libraries and namespaces says the qualified name always reaches the library's, and no syntax writes one: `as target for` and `act` take a bare name. The shadow warning's remedy says to rename the world's verb rather than to write `sprout.take`, until B24 or B26 decides whether `as target for sprout.take` is a form.
- **Two verbs of one name in one library.** Refused at the second, the first kept, as for kinds, enums and messages; the words name the library as theirs do.
- **A role naming an enum.** Refused, with `symbol` and `from` as the remedy: Value roles says `symbol` says only that the role is filled by an option, and which enum is the role-player's to say.
- **A value role in the target's place.** Optional, as a value tool is: Value roles gives the reason for tools, that what a visitor types is never one of a closed set until checked, and it holds for the target as well. The first tier's rule that the target is never optional is about `optional` as written.
- **An `exit` role.** Optional only where a phrase leaves it out, as a thing role is, which the standard library's `go` never does: the directions and labels are a closed set.
- **A role whose kind was declared and could not be composed.** Nothing more is said of it, since the composition's refusal already has; the role fills nothing, as an absent kind's does.
- **`put` in the standard library.** It names `Container`, which no library file declares yet, so it waits for `sprout/container.sprout` (B48). `take`, `drop`, `give` and the engine verbs' phrases are there now, with nothing yet playing them; `ask` is in `sprout/talk.sprout`.

Asked ahead of building the two passes (B24), 2026-09-23:

- **Declared order.** The actor first, then the verb's roles as declared (the target, then the tools; a set role's fillers in typed order), and within one participant its kind's contributions in closure order, the composer's own last. Proposed; awaiting Eric, and built so, as The two passes, Set roles and How members combine read.
- **`allow` in a `permit`, `refuse` in a `do`.** Proposed: the first accepted, since a `permit` has a guard's shape; the second refused. Awaiting Eric.
- **An NPC actor's `say`.** Decided 2026-09-23, now under Acting: it comes from the NPC, heard by whoever would hear its `tell`, as *the cat says "miaow"*. How the line is framed (a stock passage on `sprout.World`, or fixed words), and whether `nothing_happens` is told the same way for an NPC's reading that said nothing, are open; B30 carries who hears it.
- **The warning for a verb no object plays a role for.** Waits for B48, which brings the library's role bodies, so it does not fire for `take`, `drop` and `give` in every world. Decided 2026-09-23.

Found while reading, checking and composing the roles a kind plays (B24), each decided the narrow way and awaiting Eric:

- **Where a verb is known before kinds compose.** A kind's play resolves its verb as it composes, and what fills a verb's role is a composed kind, so the two cannot wait on each other. Which verbs exist, and the roles each declares, are read from the declarations before any kind composes (`VerbNames`, keeping what the verb table keeps); fillers are still the verb table's, built after the kinds.
- **Which verb a bare name in a play reaches.** The play's own library's, then the standard library's, as for a kind. `as target for sprout.take` is still not a form.
- **`allow` in a `permit`, and `refuse` or `allow` in a `do`.** The Two passes calls a `permit` the same shape as a guard, so `allow` is accepted there. A `do` runs once every `permit` has allowed, so both `refuse` and `allow` are refused in one.
- **The role played, by its own name.** Not bound: inside `as target for unlock`, `target` is `self`, and a read of it says to write `self`. A set role is the exception, since Set roles binds the whole set in every participant's body, its own fillers' included.
- **A value tool with no `from`.** Unbound, and refused at a read or at `bound`, not at the play. An integer tool with no `from` is the same, as A role-player narrows its own options says.
- **What `from` may name.** A property of the role-player's, composed ones included, and not a remembered one, which is held about each actor rather than by the role-player. A `from` naming a role the verb lacks, or a role twice, is refused.
- **What may be played.** A role the verb declares, or `actor`. A value role or an exit is named by the visitor and played by nothing, so `as topic for ask` is refused. A play of an engine verb is accepted; what the engine does with one is B34's. *Built (B28), for `go`:* its `permit` runs in the consent pass and its `do` once the move is made (Open 153). *Decided 2026-09-24 (153):* as built.
- **A kind playing a role its verb's filler does not name.** Accepted: the spec's own `sprout.Lockable` plays `target` for `open`, whose target is a `Container`, and composes no container, so its play contributes wherever a composer composes both. `self` in such a play is the kind that wrote it.
- **`as actor` on a kind that does not compose `sprout.Actor`.** Refused, from The actor's own part ("A kind composing `sprout.Actor` may play it"). By the `Lockable` reading above, a library kind adding to what every actor does would want the same latitude; this is the question to answer if it does. *Decided 2026-09-23 (63):* refused at level 1.
- **`actor` where the world names no visitor kind.** Of the object type, since that failure has been said where the world is declared. *Moot, and built (Open 83):* `actor` is `sprout.Actor`, whatever the world names.
- **`permit` or `do` twice in one play.** Refused at the second. A play with neither is refused at its head.
- **A play on the world's own body.** Accepted, though nothing fills a role with the world.
- **`bound`.** Read as a primary, the word and a name, so it binds tighter than any operator; Precedence does not list it. It narrows only as a whole condition, as `is()` does, so `bound tool && tool.is(Key)` reads `tool` unbound on the right.
- **An exit role inside a play.** Not bound; only the engine's `go` has one. *Built (B28):* the exit is the engine's to take, and binds nothing in a play.
- **The two warnings.** A verb with phrases no participant `say`s for waits for B30, which reads what a body says to whom. A verb no object plays a role for waits for B48: it would fire for `take`, `drop` and `give` in every world until the library writes their plays. *Built (B30):* the first, for the world's own verbs (Open 163); see "Found while building the three audiences".
- **A verb nothing declares, at load.** The absent table's `verb` row: a gap at the verb as written, and the play dropped.

Found while running a reading through the two passes (B24), each decided the narrow way and awaiting Eric:

- **How an NPC's line is framed.** Acting has the audience hear an NPC's `say` "as the cat speaking, _the cat says "miaow"_", and does not say whether that frame is a stock passage on `sprout.World` or fixed words. Built: the line is carried as written, to the audience, with the NPC as its speaker, and the framing is left to B29. *Decided 2026-09-23 (60):* fixed engine words; B29 and B30 render them. *B29 renders a line for each reader; the frame an NPC's line is heard in, with who hears it, is B30's.* *Built (B30):* `The cat says "Mrrp."`, rendered for each hearer; see "Found while building the three audiences".
- **`nothing_happens` for an NPC's reading.** Told the same way as its lines: to whoever would hear its `tell`, from the NPC, when its reading said nothing at all. *Decided 2026-09-23 (61), the other way:* no output; the code catches up.
- **A `say` in a participant's `do` that is not the actor's.** Reaches the actor, since Prose says `say` reaches the actor whoever's body it is in, with that participant as `self` when it renders; where the actor is an NPC it goes to the NPC's audience, from the NPC, as the NPC's own lines do. *Decided 2026-09-23 (64):* as built.
- **Who hears an NPC.** The actors directly in its place, NPCs among them, less the NPC and every participant, in contents order. What reaches an NPC and what reaches an occupant of something inside the place is B30's. *Built (B30):* only people read, so an NPC is never among them, and an occupant of a place inside the place hears nothing of it.
- **`here`.** The actor's nearest container that holds actors, so a wardrobe declaring `contains actors` is `here` for whoever stands in it. An actor with no such container, as an NPC in a crate in a world that holds only things, has the world as `here`; the spec does not say. *Moot, 2026-09-23 (62), and built:* every actor has a place, so `here` is the actor's container, and an actor found in something that holds no actors is an engine error.
- **`allow` in a `permit`.** Ends that `permit`, consenting; every other `permit` is still asked, as a guard's `allow` leaves the other parties to be asked.
- **A participant destroyed earlier in the effect pass.** `destroy self` takes effect when the `do` that ran it ends, so one is an object filling two roles, or one whose composed play destroyed it before its own ran. Its later `do`s, composed or own, do not run.
- **What a `do` does at this level.** `let`, `if`, `spawn`, `destroy self`, the four writes, `remember` and `say`. `tell`, `move`, `send` and `act` are not yet statements the parser reads; B30, B25, B32 and B26 bring them. *Built (B32):* `send` and `broadcast` are statements a `do`, a handler and a hook hold. *Built (B30):* `tell` and `tell <x>`.
- **A world composing no `nothing_happens`.** An engine error, which B34 turns into a fault, as for the other engine passages a forked standard library leaves out. *Built (B34):* a fault, told in the world's `fault`.

Found while building `move` (B25), each decided the narrow way and awaiting Eric:

- **Whether a refused `move` ends the body.** Moving something says what happens on a refusal (nothing moves, the text is said) and not whether the body goes on. Built: it goes on, as it does after `destroy self`. This is the one most likely to need Eric: the standard library's own `take`, written as `move target to actor` then `say taken`, would say "You cannot carry any more." and then still say it was taken. The alternatives are that a refused `move` ends the `do` it stands in, or that a `move` yields whether it was made, for an `if` to read. *Decided 2026-09-23 (71), the other way:* the first refusal ends the body, and built.
- **A refusal and `nothing_happens`.** A refused move's words count as said to the actor, so a reading whose only line is a refusal is not also answered with `nothing_happens`.
- **What effect a refusal is.** Said to the actor, marked as a refusal rather than a line a body said, in the one ordered list of what the effect pass says; B38 decides the effect's final shape. *Built (B38):* a `refused` effect from the party that refused, to each reader; see "Found while building effects".
- **Who hears a refusal when the actor is an NPC.** Whoever would hear its `tell`, from the NPC, as its `say` lines are.
- **What a refusal renders with.** A guard's: the refusing party as `self`, with `mover` and the guard's parameters bound; a `let` inside the guard is not carried. The engine's own ("{item} cannot go inside itself.") is fixed text from the world, with nothing bound.
- **What a `move` costs.** One step for the statement and one for each name it evaluates, then the range walks and the guards' own statements; the move charges nothing for itself.
- **A set or a value as the thing.** Both refused at compile, since a value is never moved and a `move` moves one thing; a set is moved one of its things at a time.
- **An identifier on either side.** `move item to cellar` is refused as a name nothing here answers to, as a spawn's dotted container is, until identifiers inside bodies resolve (B32). *Built (B32):* an identifier or a path on either side resolves from where the body is written.
- **`move x to x`.** The engine's refusal that a container may not hold itself, before any guard.
- **The world as the thing.** Refused at compile where the thing is known to be the world, as `self` in the world's own body; a `MoveFault` where it is found only at run time, as `here` in a world that holds no places.
- **A thing no longer live.** A `MoveFault`. `move self to c` before `destroy self` in one body moves, since the destroy takes effect when the body ends.
- **"The actor if there is one."** A `move` in a handler, a tick or a wake has no actor to say a refusal to. Only a `do` reads `move` today; B32 decides. *Built (B32):* a refused `move` in a handler or a hook is said to nobody, the refusal kept with what the drain did, and it ends the body, as in a `do`.
- **Order among a body's effects.** What its spawns, moves and destroys send, and what it says and has refused, keep the order the body did them in.
- **`move` where a value is wanted.** `let x = move a to b` is refused once, as `spawn` and `destroy` are, with both sides stepped over.
- **A move whose mover is not the actor.** `sprout.Actor`'s `depart` refuses when the mover is not itself, so `move actor to self` in a cart's `do` is refused by the actor; a person boards by moving themselves, in the actor's own part, as `good/move` does.

Found while refusing arrival at the world and warning on a hidden name, each decided the narrow way and awaiting Eric:

- **Which place the arrival refusal names.** The first place in the tree, shallowest first and in the order declared within a depth, by its path (`visitors arrive at kiln.back_room`). A world with no place in it is told to declare one.
- **Which hidden object the warning names.** Only the nearest one further out, since that is what the name meant there before; an object that hides several is warned about once.
- **A hidden object directly in the world.** The world's name as a path's first step reaches it from anywhere (Decided 70), so the warning names that path, `printers_shop.lamp`, as the other hidden-name warnings name theirs. Built.
- **An object whose kind is absent.** It is still placed, so it still hides one of its name further out and can be hidden itself, and the warning is said in either mode.

Found while destroying what a destroyed object held, each decided the narrow way and awaiting Eric:

- **The order things are destroyed in.** The object, then what it held, depth-first: each container's decoded contents in contents order, then its dormant records by id, each followed by what it holds. The queue sees them in that order; nothing observable depends on it yet.
- **A binding to something destroyed with its container.** Reads as the object itself does for the rest of the turn, holding the state it had, its container still the one destroyed around it; a participant destroyed so does nothing more in the reading.
- **A visitor kept dormant inside.** Faults the destroy as a live one does: a record kept for want of the visitor kind is still a person's. An away visitor is inside nothing and faults nothing.
- **Which visitor the fault names.** The first found in destroy order; the fault is about the object destroyed, and names one visitor in its detail.
- **Stored records that hold each other.** Records a store left holding one another in a loop are each destroyed once.
- **A destroyed declared object at the next load.** Not decided here: load makes every declared object it finds no record for, so one destroyed comes back at its defaults. It is Open 72. *Decided 2026-09-23 (72):* gone for good, never re-made; a reference to it faults; a compiler warning for destroying a declared object. Built.

Found while building `act` (B26), each decided the narrow way and awaiting Eric:

- **Where `act` stands today.** Only in a `do`, since handlers, hooks, ticks and wakes are not yet read; the spec's own cat, which acts in `on :stir`, waits for B32. In a guard or a `permit` it is refused as the other effects are. *Built (B32):* `act` stands in a handler and a hook too, where the kind that wrote it composes `sprout.Actor`; the spec's cat is built.
- **Whose kind must compose the visitor kind.** The kind that wrote the body, as What it refuses words it ("a body whose kind does not compose the visitor kind"), not the object running it. So a behaviour kind written to be composed into an NPC's kind, `kind Purring { as target for pet { do { act purr () } } }` with `kind Cat: Creature, Purring`, is refused, though every object running it could act. A real question for Eric: it decides whether reusable NPC behaviour can live in a kind of its own. Open 73. *Decided 2026-09-23 (73), the other way:* any composer of `sprout.Actor` may `act`. Built.
- **What fills a role.** A name in scope: `self`, `actor`, `here`, a role, a `let`. An identifier or a dotted path is refused as a name nothing here answers to, as `move`'s are, until bodies resolve identifiers (B32). A value written out, `act ask (target: guard, topic: :toll)` or `number: 7`, is refused: a value role is filled only by a binding that holds a value. A real question for Eric: a symbol role names no enum (the role-player's `from` does), so a literal cannot be checked where it is written, and the only way to hand one on today is a value tool already bound. Open 74. *Decided 2026-09-23 (74):* as built. *Built (B32):* an identifier or a path fills a role, typed at its object's kind.
- **What each role takes.** A role naming a kind takes a binding known to be one, so the bare object type is refused with the `is()` narrowing to write; an open role takes any thing; a set role takes a set whose kind composes its own, or one thing as a set of one; a `symbol` role takes any value of an enum, and an `integer` role any integer, which the role-player's `from` then hears or not; an exit role takes nothing an author can name, so `act go` waits for B28.
- **What may be left out.** An optional tool, every value tool, and a set role, which is then the empty set; never the target or a tool that is not optional, as Acting says. A set target may be left out, since a set role is never missing.
- **How the verb is named.** By its name alone, reached from the body's own library and then the standard library's, as a play reaches one; `act sprout.take (…)` is refused as `as target for sprout.take` is. The brackets are always written, `act purr ()` for a verb with no roles; a comma may follow the last role, as after an enum's last option; `target:p`, which the lexer reads as a name and a symbol, means `target: p`.
- **What the actor may name.** Every thing an `act` fills a role with must be live and in the actor's range, or the turn faults (`ActFault`), as a `move` of something out of range does: a person names only what is in range, and Acting says every rule that governs a person governs an NPC. The range walk is charged to steps.
- **Cascade depth.** An `act` runs one deeper than the reading it stands in, a typed command's being 0, so at the default of 20 a turn's `act`s nest twenty deep; it is checked with the same meter a message's depth will be. How deep an `act` in a handler runs, one deeper than the event it handles, is B32's. *Built (B32):* an `act` in a handler runs one deeper than the event it handles, and its reading's sends are that handler's.
- **What an `act`'s reading says, and `nothing_happens`.** Its lines, heard from the NPC by whoever would hear its `tell`, less the participants of its own reading, join the reading it stands in, in body order, with what it sends and destroys. It is never answered with `nothing_happens` itself; only a person's own command is, so a person who acts through `act` in their own `do` is answered once, by their command. Decision 61, that an NPC's reading which says nothing has no output, is built here with it.
- **A refused `act`.** Its consent pass's refusal is said as its actor's reading's whole output: from the NPC, to whoever would hear its `tell`, which leaves out the participant that refused. The body that ran it goes on, as after a refused `move`; whatever Open 71 decides for `move` should decide this too. The refusal carries the refusing play's `actor`, `here` and roles for its slots, a `let` inside the `permit` not among them, and a typed command's refusal now carries them too. *Decided 2026-09-23 (71), the other way:* a refused `act` ends the body, and built.
- **An actor its own `act` destroyed.** The body that ran the `act` ends there, since its `self` is gone; nothing after it runs, and a `destroy self` it asked for is not done twice.
- **What an `act` costs.** One step for the statement and one for each name it evaluates, then the range walks, then its reading's own statements; the `act` charges nothing for itself.

Found while refusing actors where actors cannot stand, each decided the narrow way and awaiting Eric:

- **Where the refusals are said.** At the object's name, all three: an actor that is not an NPC; an NPC written in the body of something that holds no actors, as for something that holds nothing; and one written directly in the body of a world that holds no actors, pointed to the first place in the tree, as the arrival refusal is. An object told it is not an NPC is not also told where it stands. *Decided 2026-09-23 (73), and built:* the refusal of an actor that is not an NPC is gone; an object composing `sprout.Visitor` is refused at its name in its place, and is not also told where it stands.
- **What is absent decides nothing.** With no visitor kind (refused or absent), an actor is not told it is not an NPC, and is still refused where it cannot stand; one in a container whose kind is absent, or directly in a world whose kind is absent, is told nothing. *Decided 2026-09-23 (73), and built:* the refusal of a declared visitor reads `sprout.Visitor` and not the world's visitor kind, so it is said whatever the world names.
- **In either mode.** Both refusals are refusals at load too, as the refusal of an object in something that holds nothing is.
- **A spawn the compiler can see is wrong.** A spawn of an NPC kind into something whose kind the compiler knows holds no actors is not refused at compile; the spec lists it only as a fault, and it faults when it runs (`holds-no-actors`), checked after a container that holds nothing.
- **An exit to something that does not hold actors.** Not built: exits are not read yet, so B28 refuses it beside the exit on something that is not a place. *Built (B28).*
- **`here`.** The actor's container, which holds actors; an actor found in one that does not is an engine error, which nothing but a stored state can make (Open 75). A move of an actor is between two places, so the notices are spoken for every one.

Found while nesting objects and composing with `is`, each decided the narrow way and awaiting Eric:

- **The spec's leftovers of `in`.** Identifiers and scope still read an object's `in` from inside the world and stated the hiding rule twice, and What absent means kept a row for a container in an object's `in`; both went with this change, since Objects already says a declaration never names its container.
- **An `object` at a file's top level.** Read whole, so what its body gets wrong is said too and nothing in it is taken for the next declaration, and refused by the parser at `object` and its name, with the remedy to move it into the world's braces or its container's. The file is then one that does not compile: at load it reads as absent, as any such file does. The same holds in a library's files, so the second-tier refusal of an object in a library goes; a world in a library is still refused, and what its body holds is not read.
- **An object in a kind's body.** Parsed as a member, and read: every instance of the kind is given a copy (Open 68, and "Found while giving kinds their contents" below).
- **An object that still names its container.** `object cushion is Bench in hall.bench` is read, refused at `in` with the remedy to take the `in` out and write the object inside the braces of what it named, and kept; a path after the `in` that is itself malformed is refused as well.
- **The colon written for `is`.** The kinds after it are read and kept, and the colon is refused at itself with the whole line written with `is` as the remedy; where what follows it is not a kind, that is said too, and the remedy writes `<Kind>`.
- **A path's dot at the end of a line.** A dot followed by a word on a later line ends the path, refused as a dot left over, since in a body the next line is the next member: `visitors arrive at kiln.` does not take the `visitors` below it as a step.
- **A stray `}` in a nested object's body.** It closes the object, and what follows is read by the body around it, where it is kept; only a top-level body names members written after its `}`, since after an object's `}` the members of the body around it follow as a matter of course.
- **Which world's body holds the objects.** The first `world` the world's own files declare, whatever its name; a second is refused (or a gap at load) and what it holds is not read.
- **The arrival refusal beside a refused file.** Said at publish even where another of the world's own files was refused, since the world's body is one block in one file and the refused file cannot be where the place is; two corpus worlds that arrived at a place they never declared now declare it.
- **Orders.** The tree places shallowest first and in the order written within a depth, which is the order its `placed` map keeps and the arrival remedy's "first place" reads; the bundle's objects are listed in the order written, each before what it holds.
- **Nesting depth.** Each object body is one level of the parser's own depth bound, as a block's braces are, so a nest past it is refused once as too deep; the tree itself is placed by a loop.
- **What `sprout check` counts.** Its summary counts every object in the world's body among the world's declarations, so a world says the same number it did when objects stood at the top level.

Found while telling a place's range who came and went, each decided the narrow way and awaiting Eric:

- **Which walk.** The range walk with the place as the asker, as the tree stands after the write, asking `any`: Places and After the move say "in range", and Range is one definition, so no container is asked about `:departed` or `:arrived` by name. The one who moved is left out wherever the walk reaches it.
- **Who reads and who is sent.** A visitor the walk reaches reads the passage; everything else it reaches is sent the message, NPCs included, and so are the place itself and a surface (Open 77). A visitor who moved someone else reads as anyone in range would.
- **In range of both places.** A visitor reads both notices, `leaves` first, and an object is sent both messages, `:departed` first; nothing merges them. With the world refusing, that is whoever the two places share: a place and one nested in it, where the inner one relays.
- **Order.** `:left`, `:entered` and `:moved`, then every `:departed` in walk order, nearest first, then every `:arrived` the same; the notices `leaves`, `arrives`, then `described`, each audience nearest first.
- **A place that writes no `leaves` or `arrives`.** There are no words for that side, so its visitors are sent the message with everything else in its range, and nobody in range is told nothing.
- **What the walks cost.** One step for each node each walk reaches, as every range walk is charged.
- **The engine's two refusals.** `inside_itself` renders with `item`, the thing moved, and `self` the world. The refusal of an actor where actors cannot stand keeps its fixed words, "{item} cannot stand in {to}.", since the spec gives it no line.

Found while reserving the engine's message names, each decided the narrow way and awaiting Eric:

- **Whose messages.** Reserved names says "an authored message"; every library's declaration is refused, the standard library's included, since the engine's messages are the engine's and no library declares them.
- **By name alone.** A declaration is refused whatever it carries, with or without `with`; a handler for one of the eight still binds what the engine passes, since nothing needs declaring for it.

Found while giving kinds their contents, each decided the narrow way and awaiting Eric:

- **Where a content's own mistakes are said.** Once, at the content in the kind's body, whatever holds the kind: an object in a kind that holds nothing, or inside a content that holds nothing, is refused with the words for something in the body of what holds nothing, naming the kind or the content; two of one name in one kind's body are two of one name there, at the second. What is refused is given to no instance.
- **A content made of a kind it is already inside.** `kind Box { contains object inner is Box }` would give every box a box without end, and so would a loop through several kinds. The spec is silent; refused at the content that closes the loop, "`inner` is made of `Box`, which it is already inside, so every `Box` would hold another without end", with the remedy to take it out or make it of kinds that do not compose `Box`, and cut, so what is left is finite in either mode.
- **Where a clash of names is said.** A name the instance's own body also uses, or that two kinds in its closure both give, is refused per declared instance at the second declaration in closure order, with today's words naming the instance's path: at the instance's own object where it is its own, and at the second kind's object where two kinds clash. A kind whose closure clashes and that no declared instance is made of is not refused; a spawn of it makes both, since a spawned instance's contents have no identifiers.
- **What each instance is told about its copies.** The hidden-name warning and the actor refusals are said for each declared instance at the kind's object declaration, naming that instance; words that come out the same for two instances are said once.
- **A content whose kind is absent.** A declared instance's copy is placed without a kind, as a declared object of an absent kind is, so what it holds keeps its place; a spawn makes neither it nor what it holds.
- **What a spawn makes.** The instance, then each content after what holds it, what a holder's kinds give before what its own body holds; each takes a minted id and one arrival, in that order. Each is charged against the turn's spawn budget and counts against the host's live-instance bound, and the spawn makes all of it or, faulting, none. An actor the contents would stand in something that holds no actors faults the spawn as `holds-no-actors`, naming the spawn's container. Nothing is sent for the contents (76, decided as built).
- **How a spawned instance's content is stored.** As made `given`, with the kind whose body writes it and its path there, `{ from: given, kind: printers_shop.Lantern, path: [wick] }`, under a minted id. At load it is read against what that body writes at that path now, and kept dormant where it writes nothing there.
- **What is counted.** The `objects` cap counts each copy a declared instance is given, and `places` each copy that holds actors (78, decided as built). `sprout check`'s summary counts declarations written, where a kind's contents are part of the kind, and not one per instance. The compiler bounds the copies only by the host's `objects` cap, so a world whose kinds nest many contents in many instances is as large as the host lets it be.
- **What a kind's body is checked as.** Each content's body is checked once, against its own anonymous kind composed in the library that wrote it, however many instances are given it, and a content no instance is given is checked all the same.
- **Naming a content from the kind's own bodies.** `wick` inside `Lantern`'s handlers resolves with identifiers inside bodies, B32's; until then it is refused as a name nothing answers to, as a dotted target is. *Built (B32):* `wick` from `Lantern`'s bodies is the copy inside the instance running the body.

Found while refusing a member word as a message's name, each decided the narrow way and awaiting Eric:

- **Whose messages.** As with the engine's names, every library's declaration is refused, the standard library's included, whatever it carries.
- **Its words.** The verb refusal's, with `message` for `verb`: "`:describe` names a member of a kind, so it cannot name a message", and the remedy the engine-name refusal gives, "Choose another word, as in `:rang`."
- **Before a duplicate.** A member word is refused ahead of the check for two of one name, so two `message :prose` in one library are two member-word refusals, not one of those and a duplicate.

Found while ending a body at its first refusal, each decided the narrow way and awaiting Eric:

- **From how deep it ends.** A refused `move` or `act` inside an `if`, however nested, ends the whole body that ran it, not only its block: the body is the unit Moving something names, and `take` never says it took what it could not carry, wherever the `move` stands.
- **What ending leaves standing.** Everything the body did before the refusal stands, writes, spawns, sends and lines alike, and a `destroy self` it already ran still takes effect as the body ends. Only the body that ran the statement ends: the reading's other participants' `do`s, and the composed plays after it on the same participant, still run.
- **Which refusals end it.** A guard's refusal and the engine's own (`inside_itself`, an actor where actors cannot stand) end the body alike, since each is a refusal said to the actor. For `act`, only its consent pass's refusal ends the body; a reading that acted, even one whose own `do` ended at a refused `move`, is done, and the body that performed it goes on.
- **A refused `act` said only to others.** An NPC's `act` refused in a person's command is heard by whoever would hear the NPC's `tell`, not the person, so where nothing else in the command reached the person the world's `nothing_happens` answers them.

Found while giving the host its limits (B43), each decided the narrow way and awaiting Eric:

- **Unsetting a limit that has a figure.** A host that names no figure for a limit gets the spec's default; `null` leaves unbounded only the limits whose default is the host's to say (places, objects, kinds, files, source bytes) and the wall clock, and a `null` for any other is refused at the host's boot. "A limit the host leaves unset is unbounded" is read as a limit with no default, since the tables give the rest a figure a host starts from (Open 81). *Decided 2026-09-24 (81):* as built.
- **What an exception covers.** One exception is for the world, every cap at once, and the world then loads under the larger of each cap it recorded and the host's own, unset being the largest. The host keeps whether it made one beside the caps the world recorded (core's `MicroworldRecord`, `caps` and `excepted`), and the language keys it by nothing.
- **Caps compared, not contents.** A world recorded against a cap larger than the host's is refused at load even when what it holds would fit the host's cap, as the spec says; its remedy names republishing, which checks it against the host's caps and records those.
- **Only at load.** What a world recorded is read only at load; a publish, an excepted world's included, is checked against the host's own caps and records them.
- **Its words.** Once per cap, at the manifest's `name` as the other world-wide caps are: "This world was published allowing 12 exits on one place. This host allows 8, 4 fewer.", or "…with no limit on places in a world. This host allows 40." where it recorded none; the remedy is "Publish it again under this host's limits, or ask the host to make an exception for this world." `sprout check` publishes and never loads, so the words are pinned by `bundle/compile/recorded.spec.ts` rather than a `corpus/bad` world.
- **Under an exception, past even that.** A file past the caps the exception granted reads as absent at load, as any broken file does; a world-wide cap past them refuses, as without one.
- **What the runtime reads under.** An excepted world's stored values are read, and its lists grown, under the caps the exception granted, which that load's bundle records, rather than the host's own.
- **Pending wakes per object.** A runtime budget in the table, 1 by default, counted against one object across turns; what a load does with a stored wake list longer than a lowered figure is B36's. *Built (B36):* kept whole; the cap is on asking, so a `wake` faults until the list is under it (see "Found while building wakes").
- **Budgets are not recorded.** A bundle records its static caps and nothing of the runtime budgets, which apply as the host sets them on the day a turn runs.

Found while keeping a destroyed declared object gone, each decided the narrow way and awaiting Eric:

- **Stored state written before tombstones.** `tombstones` is a required field, so a stored world without it is unreadable; nothing is released, so no store holds one.
- **The stored mark.** A `tombstones` list on the stored world, one declared id each, in code-unit order, kept for good: nothing removes one. The schema refuses a tombstone that is not a declared id, one written twice, and any instance stored under one or inside one; a link or a visitor's last place may still name one, since the absent rules read those. A turn's change set carries the tombstones it adds beside what it upserts and removes.
- **What is tombstoned.** Every declared object a destroy removes: the one destroyed and each declared object inside it, a copy a kind gave it and a dormant declared record included, so a file restored does not bring one back. What was spawned inside it, and what a kind gave a spawn, is removed and leaves no tombstone, since a minted id is never made again anyway.
- **What source adds inside it later.** Load does not make a declared object inside one tombstoned, however it came to be declared there, since what would hold it is gone; only a destroy writes a tombstone, so nothing is written for it.
- **When a name faults.** From the moment the destroy takes effect, for the rest of that turn as well as after it; a binding to the object is not a name, and reads it as it was for the rest of the turn, as Destroying says.
- **Where the fault is read.** `namedObject` (runtime) resolves a declared id and faults for a tombstoned one, `DestroyedReference`, naming the object; a declared id with nothing decoded under it gives nothing, for the absent rules. Nothing a body runs resolves an identifier or a path yet, so no world reaches the fault until B32 resolves them through it; the engine's own paths are Open 82. *Built (B32):* every name a body reads resolves through `namedObject`, so a destroyed declared object faults there. *Decided 2026-09-24 (82):* the engine reads a destroyed declared place as an absent one; B28 and B42 build it.
- **Which object the warning names.** The first declared object, in declared order, that runs the `destroy`, by its path; a `destroy` in a kind is warned about once however many declared objects are made of it, and names the kind the `destroy` is written in. One that no declared object runs, because none is made of its kind or each leaves the play out with `without`, is not warned about.
- **Where the warning looks.** A play's `do`, however deep in an `if`, since that is the only body that may destroy today; handlers, hooks and wakes join as B32 reads them. *Built (B32):* handlers and hooks are read too.
- **Its words.** "`kitchen.taper` is made of `Candle` and is declared in the world, so once it is destroyed it never comes back.", or without the kind for the object's own body, and the remedy "Destroying is meant for what was spawned. To have something come and go, `spawn` it when it should appear; to keep this one, change one of its properties instead." Pinned by `corpus/good/destroy-declared`.

Found while blessing libraries (B44), each decided the narrow way and awaiting Eric:

- **Where the set comes from.** It is the host's, as its limits are: `blessedFrom({ bless, unbless })` starts from `DEFAULT_BLESSED`, the standard library's hash and nothing else, adds and removes hashes, and throws `BlessedError` at the host's boot for anything that is not 64 lower-case hex digits or for unblessing a hash it does not hold. `compileBundle` blesses `DEFAULT_BLESSED` when the host names no set, as it checks against `DEFAULT_LIMITS` when the host names no limits; `sprout check` publishes under that default and takes no set of its own, since an author's machine is not a host.
- **What a load honours.** Exactly the hashes blessed at the world's publish, which the host keeps beside the caps it recorded (lang's `RecordedCaps.blessed`, core's `MicroworldRecord.blessed`, as `blessedIn(bundle)` gives them). The host's set on the day of the load is not read: a library unblessed since publish stays exempt, and one blessed since is still charged, until the world is published again. A load with nothing recorded blesses the host's own set, as it checks the host's own caps. The spec says the exemption is granted at publish and does not say whether a later blessing reaches a published world. *Decided 2026-09-24 (86), the other way:* a load honours the host's set now, both ways; the code catches up.
- **Under an exception.** What was blessed is honoured the same way whether or not the host excepted the world; an exception widens caps and does not touch blessing.
- **A blessed copy under another name.** A library's hash is its files by name and text and nothing else, so a copy of a blessed library vendored under another name is blessed too, as it is the same source.
- **No words of its own.** Blessing adds no refusal: a world past a cap with a library unblessed is refused with the source, file or kind cap's own words, whose remedy already names a blessed library. A bundle's library records `blessed`, and nothing new is said at load when the recorded blessing and the host's current set differ.

Found while giving a person `sprout.Visitor`, each decided the narrow way and awaiting Eric:

- **The library's file.** `sprout/visitor.sprout` holds `kind Visitor is Actor { }` and nothing else, after `sprout/actor.sprout`, as the worked microworld lists it; the library's hash moves with it.
- **Whose kind may act.** The kind that wrote the body must compose `sprout.Actor`, as What it refuses words it, so a behaviour kind meant for an NPC composes `sprout.Actor` itself, `kind Purring is sprout.Actor { … }`, and composes beside `Creature` (where `Creature` restates a property of `sprout.Actor`, Open 84 applies). The refusal reads "Only an actor acts, and `Kettle` does not compose `sprout.Actor`.", the words a play `as actor for` on such a kind is already told. *Decided 2026-09-24 (84):* `Creature`'s restatement supersedes `sprout.Actor`'s; the code catches up.
- **What decides a declared or spawned visitor.** Composing `sprout.Visitor`, however far down, and nothing about the world's visitor kind: an object is refused at its name, "`porter` composes `sprout.Visitor`, what a person is made of, and nothing declares a visitor: each one is a person who arrives.", and a spawn at its kind with "spawns"; `spawn sprout.Visitor` "is what a person is made of". The remedy names the shared kind: "For an NPC, use a kind that composes `sprout.Actor` and not `sprout.Visitor`; to share it with the visitors, write `kind Creature is sprout.Actor { … }` and `kind Person is Creature, sprout.Visitor { }`."
- **A visitor kind that is an actor and not a person.** `visitors are Creature`, where `Creature` composes `sprout.Actor` alone, is told "`Creature` does not compose `sprout.Visitor`, and a world's visitors are made of a kind that does." with the remedy of a new kind composing both, `kind Person is Creature, sprout.Visitor { }`, never of composing `sprout.Visitor` into `Creature`, which would refuse the NPCs made of it. A kind that is no actor is told the same, with `kind Person is sprout.Visitor { … }`.
- **Another library's person.** `visitors are victorian.Gent`, where `Gent` composes `sprout.Visitor`, is refused as any library's kind, and the remedy composes it: `kind Person is victorian.Gent { … }`. A library's kind that is not for a person is told to compose `sprout.Visitor`.
- **The name a remedy suggests.** `Person`, and `Guest` where the kind written is already `Person`, since a world's own `Visitor` is warned (Open 85). *Decided 2026-09-24 (85), the other way:* a kind composing the library kind it hides is not warned; the code catches up.
- **A stored spawn of a person's kind.** A stored instance spawned of a kind composing `sprout.Visitor` stays dormant at load, as one of a kind composing `sprout.World` does, and such a kind is not one a running `spawn` finds; the compiler refuses the `spawn` before either could happen.
- **What `actor` is.** `sprout.Actor`, in every body that binds it (Open 83); no corpus world or fixture read a person's own property through `actor`, so none needed narrowing. *Decided 2026-09-24 (83):* as built.
- **The visitor kind's own body.** Nothing in it is refused for being behaviour here; decision 80 says what it may declare (built below, "Found while keeping a person's kind free of behaviour"); the plays it composes still run, and the fixtures that gave the visitor kind plays their NPCs used now give them to the shared kind.

Found while keeping a person's kind free of behaviour, each decided the narrow way and awaiting Eric:

- **What is refused today.** Of the members decision 80 refuses, the kind body's grammar holds two: a play (`as … for …`), refused at its head, and a consent guard (`depart`, `release`, `accept`), refused at its keyword. A handler, hook, tick, wake or pass rule is not a kind member the parser reads yet (`on :stir` in a kind's body is "A kind is not made of `on`."), and neither is a grammar block or a `describe`; the check is exhaustive over the kind members, so each is decided where it is added (B32's handlers and hooks, B35's ticks and wakes, the pass rules). *Built (B32):* a handler, a hook and a pass rule in the visitor kind's own body are refused at the member, "…so its own body does not answer a message with `on :stir`." (or "watch a property with `changed :stamina`", "say what passes with `pass any`"), with the same remedy (`corpus/bad/visitor-kind-handles`).
- **The words.** One sentence shape for every member: "`Person` is what a person is made of, and a person acts by typing, so its own body does not play `as actor for pull`." (or "guard a move with `depart`"), with the remedy "Write it on a kind `Person` composes: `kind Creature is sprout.Actor { as actor for pull … }` and `kind Person is Creature, sprout.Visitor { }`." The shared kind is always named `Creature` (`Being` where the visitor kind is itself `Creature`), whether or not the visitor kind already composes one of the world's own; a guard's remedy writes its parameters as the author did, `depart (to) …`; and the visitor kind's braces are `{ … }` where its body declares anything else, so the remedy never reads as "empty it" (`corpus/bad/visitor-kind-behaviour`).
- **Only the visitor kind's own body.** What it composes, however far down, and every other kind of the world's, an NPC's included, keep their plays and guards; a kind is not refused for being composed into the visitor kind. The visitor kind still resolves, so the world still has visitors, and nothing else is said about it.
- **In either mode.** Refused at publish and at load alike, as `checkVisitorKind`'s refusals are, and not marked as a policy a later level tightened, since the language has one level.
- **What else the body may say.** `contains` and kind contents (objects in its body) are what a person holds, and are taken; so is `without`, which Open 93 asks about. *Decided 2026-09-24 (93):* `without` is taken, as built.
- **Moved.** `good/roles` (`Person` played `as actor for pull`) and `good/move` (`as actor for board`) now write the play on `kind Creature is sprout.Actor { … }` and declare `kind Person is Creature, sprout.Visitor { }`; their pages are unchanged. No fixture, spec, `sprout init` template or e2e world gave the visitor kind's own body a play or a guard.

Found while giving memory its block, each decided the narrow way and awaiting Eric:

- **The old form.** `:remembers [ … ]` is refused at its `:remembers`, "What an object remembers is written as a `remembers` block, with no colon before `remembers` and its properties in braces.", and the remedy is the block its entries make, `Write \`remembers { :handled false :visits 0 min 0 max 99 }\`.`, where every entry is written `name: …` and its `]` closes before the next member; otherwise the remedy is the general `remembers { :visits 0 min 0 max 99 }`. The list is stepped over whole and its entries are not read, so what is wrong inside one is told once the block is written.
- **Between entries.** Nothing, as between a body's members; a comma is refused with words of its own, "A `remembers` block does not put commas between what it remembers.", and the entries on both sides are kept.
- **An entry with no colon.** `visits 0`, or `visits: 0` as the list wrote it, is refused at the word, "A `remembers` block holds properties, and `visits` is not one.", with "Write `:visits` and its value, as a property is written."; the colon after the word goes with it, and the next entry is read.
- **A block never closed.** It ends where the body's next word-led member or a declaration starts, keeping what it read, and says "This `remembers` block is never closed." there; a `:symbol` after it is read as its entry, since nothing tells a remembered property from an owned one.
- **The member table.** `remembers` is a member word like `contains`, so a word no body reads is answered "It holds its properties, `remembers`, `contains`, …", and a stray `}` names a block's entries as written, `:visits`.
- **A word with a brace after it.** It is never a list's element, a value a refused property steps over, or an object's name, as `remembers {` starts a block; a value missing right before a block is refused as one missing before a declaration is, "… before the next `remembers` block.".
- **A word with a value after it, past a list's `]`.** `faulty "x"` is left for what reads next, as a property that lost its colon, in a body as in a block, rather than named as more of the list.
- **Words that showed the list.** The restatement remedy writes the block, `remembers { :seen true }`; the checker's "Declare it first, as in `remembers { :visits 0 min 0 max 99 }`."; `from` over a remembered property, "Name a property declared outside a `remembers` block."; and a name remembered twice is quoted as written, "`:visits` is remembered twice.".
- **Several blocks in one body.** Allowed, their entries combining (Open 90). *Decided 2026-09-24 (90):* as built.

Found while building events (B32), each decided the narrow way and awaiting Eric:

- **A handler's and a hook's head.** `on :m`, then its parameters in brackets where it names any, then a block; `changed :p` the same. The parameters are positional and named as the author chooses, `_` leaving one unnamed, and `from` and `to` may name one as they may a guard's; empty brackets, a stray comma or another word of the language is refused. A symbol that starts a line of its own after `on`, `changed` or `pass` is the body's next property, not what the head names, as a path's dot at the end of a line is.
- **An engine message's parameters.** Positional, under the author's names, as a guard's are: `on :entered (thing, _)` binds what entered as `thing`. Receiving writes each engine message with the names it passes, and those are what a remedy shows; one past what the message passes is refused, "`:entered` passes `item` and `from`, and nothing else."
- **`actor` and `here` in a handler or a hook.** Not bound, and a read of either is refused, "`actor` is not bound inside `on :stir`: nobody is acting when a message arrives.", unless a parameter takes the name, as `on :arrived (actor, from)` does, since that is how the spec writes it.
- **`say` in a handler or a hook.** Refused in the spec's own words from Diagnostics, whose remedy names `tell`, which B30 brings; until then the remedy names a statement this compiler does not read yet. *Built (B30):* `tell` is read, so the remedy names a statement this compiler reads.
- **`refuse` and `allow` in a handler or a hook.** Refused, "`refuse` answers someone, and nobody waits on `on :unlock_attempt` for an answer.", the remedy naming `if` and a reply by message, as Handlers do not refuse has it.
- **A message a handler or a pass rule names.** One of the engine's by its name, else the library's own message, else the standard library's, as a bare kind is reached; nothing answering is refused, "Nothing declares a message `:illumnating`. Did you mean `:illuminating`?", and at load it is the absent table's `message` row, the handler or rule dropped. Two handlers for one message in one body, or two hooks for one property, are refused at the second, as a guard written twice is.
- **What a hook may watch.** A property the kind that writes the hook holds, composed ones included, and not a remembered one, which is held about each actor; one it does not hold is refused with the one most likely meant. `was` is the property's type; a second parameter is refused.
- **A pass rule's rule.** A condition with only `self` bound, the container asked, refused where it is not a boolean. A pass rule on a kind that holds nothing is accepted and never asked, as a `release` on one is; so is `pass :entered` or any engine message, which the engine delivers directly and never through a container.
- **Pass rules under composition.** Exclusive, per message and for `any`: the composer's own replaces; else the one origin's; two origins are refused at the kind, as written, that brought the second, "`Cabinet` gets `pass any` from both `Glass` and `Oak`, and a container has one rule for each message.", and the first is kept, as a colliding property keeps its first. A kind that replaced a rule it composes still collides where a composer takes both it and what it replaced, as a restated property does.
- **Where a body's names resolve from.** The world's body from the world, and a declared object's own body from the object, as Identifiers and scope has it, the nearest declaration winning. A kind's body has no place in the tree, and nor has the body of an object a kind gives: those resolve first among what the giving kind's body holds, from the content's own place in it outward, which names the copy inside the instance running the body, and then among what the world's body holds, which every instance is inside; the world's name as a first step reaches from anywhere. What is nearer a particular instance is not asked, since a kind's body is checked once for all of them. A real question for Eric: whether a name in a kind's body should instead resolve from each instance's place, which no compile-time check can do for a spawned one. Open 91. *Decided 2026-09-24 (91), the other way, and built:* from each instance's place at run time, nearest first; what the checker knows is Open 180, and what a body declares Open 190. *Decided 2026-09-24 (180):* the proposal, as built.
- **A binding and an object of one name.** The binding: the scope is asked first, so `let lamp = …` or a role called `lamp` hides an object called `lamp`, with no warning; the object is still reached by its path.
- **Identifiers in an expression.** One name only, `lamp.get(:lit)`; a dotted path is written where a thing goes, after `send`, `move`, `spawn … in` and a role of `act`, since `.` in an expression reads a member. An identifier the compile fixes is typed at its object's own kind, so its properties read without `is()`; one in a kind's body that each instance's place decides is of the object type (Open 180).
- **Where a copy is found at run time.** For a declared instance, at its declared path under the instance; for a spawned one, the first instance inside it, breadth-first, made as that kind's copy at that path. A copy moved out of the instance that was given it is no longer found, and the name reaches nothing. A name in a kind's body finds the nearest body outward from the running instance that declares it, and the copy is looked for inside that body's instance (Open 190).
- **Reading through a name out of range.** A fault, `NameOutOfRange`, for a read in an expression and for what a `move`, a `spawn` or an `act` names, as Identifiers and scope says of a `get`; a name that reaches nothing now, a declared object absent at load, faults the same way. A `send` to either does nothing, as a send out of range does.
- **What a `send` and a `broadcast` refuse.** A target that is a value or a set; an engine message, which the engine sends itself, the remedy naming the eight; a message nothing declares, at load the absent table's `message` row, the send then going nowhere; a value where the message carries none, none where it carries one, and one of another type, in the words a `set` of the wrong type is refused in. In a guard or a `permit` both are refused as doing, as `move` is.
- **When a send is made.** Where the statement runs: the target's range, a broadcast's walk and the value are all read then, against the tree as it stands, and what is queued is what was sent to whom. A broadcast is sent to what the walk reaches as held or passed, never the sender and never a surface, and in walk order.
- **The two warnings.** Only the world's own declarations and handlers are warned about; a library's message or handler nothing in this world uses is not. A message counts as sent where any body in the bundle sends or broadcasts it and as handled where any kind answers it; the engine's messages are never warned about. Their words: "Nothing sends `:stir`, so `on :stir` never runs." and "Nothing handles `:creak`, so sending it does nothing."
- **The drain's order.** Breadth-first in insertion order: what the effect pass queued, in body order across its bodies, then what each delivery's handlers queue, each behind everything already waiting. One handler's sends, its hooks among them, are queued in the order its body made them once that handler has ended; a delivery runs every handler the recipient's kind composes for the message, in run order, one after another, each one's sends queued as it ends. The effect pass's own sends are delivered at depth 1, and a handler at depth d sends at d + 1.
- **What an event is.** One delivery that runs at least one handler or hook, charged once however many composed handlers it runs; a delivery to something with no handler for the message costs nothing, since a move's `:arrived` goes to everything in range of the place. A real question for Eric: whether every delivery counts, handled or not, which would let a busy room's arrivals exhaust the budget. Open 92. *Decided 2026-09-24 (92):* as built.
- **Cascade depth.** Checked as a delivery runs, against the host's figure: a chain of twenty handlers, each sending the next, runs at the default, and the twenty-first faults. An `act` in a handler at depth d runs its reading at d + 1, and what that reading sends is the handler's own, delivered at d + 1.
- **A hook, once per change.** Queued by `set`, `adjust`, `add` or `remove` where the value it leaves differs from the one it found, a list by its elements in order; a write that leaves the value as it was queues nothing. Only a property some hook in the kind watches queues one. `remember` never queues a hook, since memory is not watched.
- **What a destroyed object takes with it.** Everything queued to it, everything it sent that is still waiting, and every engine message naming it as its `from`, whether it was destroyed in the effect pass or by a handler; a later send to it is not queued. A delivery to something not live is skipped, and charged nothing.
- **`finally destroy self`.** Built here, since it needs the drain: `finally` then `destroy self` is one statement, standing wherever `destroy self` may, refused with "Only `destroy self` may follow `finally`." otherwise. It marks `self`; once the queue is empty each marked object still there is destroyed in the order marked, as `destroy self` would, and that sends nothing. Written beside `destroy self`, the destroy happens at the body's end and the mark comes to nothing. The warning for destroying a declared object covers it. `finally` is not among the reserved words the spec lists; it is read as a statement's first word, and nothing refuses it as a name. A real question for Eric: whether `finally` joins the reserved words. Open 94. *Decided 2026-09-24 (94):* `finally` is reserved, now in `syntax/reserved.ts`.
- **No actor in a handler.** A refused `move` in a handler or a hook is said to nobody (the effect's `to` is empty), since nobody is acting and the spec's "the actor if there is one" finds none; it is kept among what the drain did, and it ends the body. An `act` there is an NPC's reading, heard as its own is.
- **What the drain is given and gives.** `drain` takes what the effect pass queued, destroyed and marked, and gives back what handlers said, the notices of any actor they moved between places, everything destroyed, and how many events ran. It is not a turn: B34 runs the consent pass, the effect pass, the drain and the views in order, under one transaction. *Built (B34):* `commandTurn`.

Found while giving each kind its file, each decided the narrow way and awaiting Eric:

- **The name.** A kind's name in lower case, `_` between its words, compared exactly against the last part of the file's path (Open 95). *Decided 2026-09-24 (95):* as built.
- **Where the refusal falls.** At the kind's name, for a kind in a file named for no kind: "`PrintedSheet` is declared in `sheets.sprout`, and a kind is declared in the file named for it.", with "Move `kind PrintedSheet` to a file of its own called `printed_sheet.sprout`."; every kind in such a file is refused so, each with its own file. In a file named for one of its kinds, that kind stays wherever in the file it is written, and each other is refused at its name as sharing it: "`Lid` shares `chest.sprout` with `Chest`, and each kind is declared in a file of its own.", with the same remedy. A second `kind Chest` in `chest.sprout` is the name declared twice, and only that is said.
- **The world.** Refused in a kind's file, at the world's name, with "Move the world to a file with no kind in it, such as `world.sprout`." (Open 96). A world in a file with kinds none of which it is named for is not refused: the kinds are, and moving them leaves the world where it is. *Decided 2026-09-24 (96), the other way:* the world is in the file named for the world's name; the code catches up.
- **Which tier.** The first: one file answers the question alone, so an editor's `checkShape` says it. In a whole compile it refuses at publish and warns at load, and the file's declarations stand, so an object of a misplaced kind is not also "Nothing here is a `Chest`." (Open 95). What it refuses under The compiler does not list the rule; the narrow change is to add "a kind in a file not named for it, or beside another kind or the world".
- **The standard library.** Already one kind to a file, each named for it, so its hash did not move.
- **Moved.** Every corpus world, fixture and test world now gives each kind its file, `sprout init` writes `person.sprout` beside `world.sprout`, and a file left holding nothing was removed from its manifest. A bad world's page kept its refusal, at the new file, line and column, except: `bad/kind-cycle` names the same loop from `Chest`, since files are read in name order and `chest.sprout` is now read first; and `bad/finally-not-destroy`, `bad/handler-no-colon` and `bad/send-no-message` each warn that nothing handles their message, since the message's file now compiles and the kind whose handler did not parse is in a file of its own. The runtime's spec fixture gives each kind the file named for it before compiling, keeping its line and column, since what those specs test is not where a kind is written.

Found while reshaping core's records, each decided the narrow way and awaiting Eric:

- **Where the stored shape lives.** In the language, `runtime/stored.ts`, as it was: core's boundary spec lets it import the language, and the adapters may import only core. The language exports its per-record schemas (`StoredInstanceSchema`, `StoredVisitorSchema`, `StoredPropertySchema`) and names a turn's change set `StoredChanges`; core re-exports them, with `codeUnitOrder`, so an adapter validates what it reads back without importing the language.
- **The port's shape.** `state()` reads a world's whole stored state, the `StoredWorld` without its name, which the bundle gives, and `putState(changes)` writes one turn's `storedChanges`, the serial with it. There is no counter a store issues from: a draft issues serials following the one it read, which is the re-run rule, and two racing write turns each read the serial the other left.
- **Reads write nothing.** The heartbeat (`touchActor`), `actorsIn`, and an actor's last seen, narration, last noun and pending lines went with the actor record: a poll writes nothing (The runtime › Turns), who stands where is the tree, and what a turn said is its effects, which the log (B40) keeps.
- **No reset.** Nothing replaces `clearObjects`: the spec has no reset, a tombstone is kept for good and a minted id is never reused, so a world's state is cleared only with the world, by `destroyMicroworld`.
- **Order.** A store hands back instances by id, visitors by visit and tombstones in code-unit order, whatever order a turn gave them; a map's key order is not part of the contract, and the conformance suite compares with keys sorted, so a database that reorders a JSON object's keys is not wrong.
- **Who is forgotten.** `forgetVisitor(visit)` and `exportVisitor(visit)`, by the visit key, in every microworld whose visitor records hold it; the store holds nothing that ties one person's visits in different worlds together, so a host that mints a visit per world calls them once per visit. They replace `forgetActor` and `exportActor`, since an actor id is an instance id and names one world's instance.
- **What forgetting takes.** The visitor record, their instance, everything stored inside it all the way down, and every instance's memory keyed by their instance id, dormant records included; their own instance's memory of others goes with it. The serial and the tombstones are untouched. What they held is Open 98. *Decided 2026-09-24 (98):* removed all the way down, as built.
- **What exporting gives.** Per world: the visitor record, their instance (null where the store lacks one), and each instance's memory of them, by the remembering instance's id; not what they held, which is the world's. Whether memory belongs in it is Open 97. *Decided 2026-09-24 (97):* memory is carried, as built.
- **Forgetting waits on turns.** It writes a world's state, so it takes that world's write lock (the SQL store takes every affected world's in code-unit order, in one transaction; the document store each world's state key in turn), and a turn that read the visitor's memory cannot write it back. A conformance case pins it; like the other two that need a turn to wait on another, PGlite skips it and a real Postgres runs it.
- **The SQL migration.** 0.1.0 shipped `001`, so the stored form is `002`, and `001` is not edited: it drops the object, actor, memory and spawn-counter tables with what they held, which this runtime cannot read, and makes `serial`, `instance`, `memory` (a row per remembering instance per actor, indexed by actor), `visitor` (indexed by visit) and `tombstone`; schema version 2. Host seconds, arrivals and the serial are `bigint`, since host seconds pass a 32-bit integer in 2038.
- **The document layout.** Instances and tombstones are one opaque string in the `state` document, the lock a write turn holds; each visitor a document of its own, written only when a turn changes that visitor; the serial in the `counters` document beside the action and miss sequences.
- **A miss's state.** The room and what was in it, as stored instances, in place of the untyped property records.

Found while building passages (B29), each decided the narrow way and awaiting Eric:

- **Where a `prose` line looks.** Beside the file that writes it, so `prose "mirror.prose"` in `rooms/mirror.sprout` is `rooms/mirror.prose`, and a library's is among its own files. An object's body and the world's may write one, as a kind's may; the worked microworld's `composing_room` does.
- **What a `.prose` file holds.** Passages and comments. Anything else is refused once where it starts and stepped over to the next `passage`, whose words are then read.
- **A `.prose` file not there.** Named by a `prose` line and not a file of the world at all: refused at publish, the absent table's `passage` row at load. Named in the manifest and missing, withheld or broken: that file's gap is the one said, and at load each passage of it a body says is a gap where it is said; at publish nothing more is said. What says an absent passage renders nothing. A `.prose` file no kind points at is warned about, as What it warns about lists.
- **What a slot may name.** `{x.name}` names a passage of `x`'s kind, for any word but `count`; a slot reads no dotted path. A bare name is a binding in reach, else an object named from where the passage is written, as a body's name is.
- **What a condition may be.** Any boolean, `&&`, `||`, `!` and `bound` among it, as a body's `if` takes; `+` and `-` are refused in a slot and in a condition alike, a minus written on a number being the number; and a call that writes or remembers is refused there in words of its own, "A passage only reads, and `set` writes."
- **The loop's own names.** Bound in the loop's body only, and read outside one they are refused with why; a loop variable may not take a name in reach, a `$` name or a word of the language. `$index` counts from 1 (Open 120). A kind filter is written only on a walk of contents: on `of` it is refused with both forms. *Decided 2026-09-24 (120):* from 1, as built.
- **What `{for x in c}` walks.** The direct contents of `c` in contents order, asking no range, as `count` does; `c` must be typed by a kind that holds things, so `here` is narrowed first (Open 126). *Decided 2026-09-24 (126), the other way:* `here` is `sprout.Place`. *Built:* `{for thing in here}` walks it unnarrowed wherever every kind holding actors composes `sprout.Place` (Open 200).
- **Checked against where it is said.** A passage's `self` is the kind that wrote it; the rest of its scope is the scope of the statement that said it, as it stood there, later `let`s not in it. Each composed kind is asked what each body it runs says, so a composer's own line is checked against the bodies of its closure that say it, and a body its `without` leaves out does not count. A name a passage renders that is bound nowhere it is said is refused at the statement that says it, "The passage `full` renders `{target}`, and nothing here is called `target`.", and nothing more is said of the passage from there; a mistake in the passage itself is said once however many places say it. A passage a slot renders is given `actor` and `here` (Open 123); a passage named for one of the engine's lines is the engine's wherever it reaches, with what the engine binds (Open 122); a passage nothing says is checked with `self` alone (Open 125). *Decided 2026-09-24 (122, 123, 125):* as built.
- **How a slot's option is told from a string.** At run time both are text, so the checker records each slot that renders an option, and the bundle carries them (`optionSlots`); the renderer humanises those and writes every other string as it is.
- **Reflow.** Every run of spaces and line breaks is one space and a paragraph has none at its ends; a blank line is a paragraph break where it is written in the source, so a block that renders nothing between two lines makes none; a paragraph with nothing in it is dropped; `\n` is a line break reflow keeps. The first letter of every paragraph, and of every line a `\n` starts, is capitalised, past a quote or a bracket it opens with.
- **What an object is called.** Its name and article as addressing gives them (the grammar block, else the defaults Open 100 records), the article written as it applies, `none` as nothing (Open 121); a visitor is their nickname. To its own reader any object is "you", and the verb's agreement is the author's. *Decided 2026-09-24 (100, 121):* `a` unless written, as built, and written `an` before a vowel. *Built:* the default is `an` before a, e, i, o or u, and a written article as written.
- **Words in quotes.** A one-line passage: a `}` alone is a character, since nothing in quotes can escape one, and a `{` never closed is refused at it. `say` in quotes is held to the host's literal cap, counted after escapes (Open 127). *Decided 2026-09-24 (127), the other way:* `refuse` in quotes is held to the cap too. *Built:* `corpus/bad/refuse-too-long`.
- **Passage depth.** A passage said by name runs one passage deep, and each slot rendering a passage one deeper; words in quotes count none. Output is charged to each reader at rendering, in the characters of what it renders.
- **The engine's fixed words.** An actor moved into what holds no actors reads a one-line passage, `{item} cannot stand in {to}.`, rendered with the actor and where it was to go, so the words name them as any slot does; a fault's stock line, where a world leaves `fault` or `unseen` out, is one too.
- **A slot inside a slot.** Refused once at the inner `{` and not read. A slot left open that takes a passage's `}` is read as far as it runs, so what it reads is said where it stands before the body it took is said never closed (`corpus/bad/passage-slot-unclosed`).
- **`{one of}`.** Chance's (B33): refused as not read, and the choice stepped over to its `{/one of}`. *Built (B33):* read as a block of choices; see "Found while building chance".

Found while building turns (B34), each decided the narrow way and awaiting Eric:

- **Where a turn runs.** The frame and a command turn are the language's (`runtime/turn.ts`, `runtime/command.ts`), plain functions over a committed state: a write turn opens a draft and a budget of its own kind, runs its body, and commits or is abandoned; a poll reads the committed state through a reader with nothing to write with, under the poll's step budget, with no seed. The lock and the transaction are core's (`turns.ts`): a write turn reads the stored state inside the store's transaction, runs, and writes its change set only where it committed, so a fault writes nothing of the world; a poll reads a snapshot with no lock. A turn is a function of what it read and its inputs, so a store that runs its body twice commits it once.
- **What a fault is.** Anything a turn's body throws, parsing and the commit included, abandons it. A rule of the language's names what it is about where it is about one (`LifecycleFault`, `MoveFault`, `ActFault`, `NameOutOfRange`, `DestroyedReference`) and is the world's; anything else is the engine's own, marked for the host (Open 110). A load that cannot read the stored state is not a turn's fault: it throws out of the transaction to the host, as `StoredStateUnreadable` always has. *Decided 2026-09-24 (110):* as built.
- **How a fault is told.** To the actor alone, as a `notice` from the world, in the world's `fault` rendered with `actor` and `here`; bystanders hear nothing, since nothing happened. A world whose composed kinds lack `fault`, or a poll's `unseen`, is told the stock line in fixed words, so a fault is never silent.
- **What a write turn is given.** The seed the host drew, which B33 draws from, and the host's bound on stored instances for the turn, both for the log to record (B40); the host's budgets; and the host's clock, which only the wall-clock backstop reads. `elapsed` is a tick's and a wake's, and B35 and B36 give it. *Built (B35):* a tick is handed the instant it runs, and its `elapsed` is measured from the place's last tick; see "Found while building ticks". *Built (B36):* every write turn is handed the instant it runs, which a `wake` is asked at; a wake's `elapsed` is measured from when it was asked; see "Found while building wakes". *Built (B33):* the seed begins the turn's one stream of draws; see "Found while building chance".
- **The parser's seam.** A command turn takes the words typed and hands them to a `Parser` the host supplies, with who typed them, the turn's state before anything is written and the turn's meter, so parsing is charged to the turn's steps and a command too costly to read faults. It gives back a reading, or a line said in place of one, as an unknown word or a `which` is answered, which commits with nothing changed. An answer said to anyone but the actor, or a reading someone else performs, is the engine's defect and faults. B27 fills the seam. *Built (B27):* `parseCommand`, which answers with the world's `unknown`, `not_here` or `which` as a `notice` to the actor, a `which` carrying the line to type for each candidate; the seam's context carries each visitor's nickname, read from the committed state, and the turn's stream of draws, whose first draws are the parser's.
- **A command from someone not present.** From a visit the world has never seen, or one whose instance is away, it is the host's defect, thrown before the turn opens: there is nothing to abandon and nobody in the world to tell.
- **What a command turn gives back.** Its phases as they are, not one list: the parser's answer; the consent pass's refusal; or what the effect pass did and what the queue did after. Ordering them into one sequence of effects is B38's. *Built (B38):* the phases are kept, and the turn also gives back one sequence of rendered effects; see "Found while building effects".
- **Stale views.** Every committed write turn names every visitor whose instance stands somewhere, in code-unit order of the visit, as one whose view is stale, a refused command and a parser's answer included, since each is a write turn; a faulted turn names none (Open 111). *Decided 2026-09-24 (111):* as built.
- **The set-role cap.** Checked as a reading starts, a typed command's and an `act`'s alike, before either pass: a set role binding more than the host allows one to faults the turn, and not even the actor's part runs.
- **An empty change set.** A committed turn that changed nothing still writes its change set, which is empty and keeps the serial; a store's `putState` of it changes nothing.
- **The other kinds' faults.** The frame abandons a tick, a wake and a maintenance turn as it does a command, and returns the fault; what follows is each kind's own item: a faulted tick dropped (B35, built: see "Found while building ticks"), a faulted wake consumed and logged (B36), which writes after the fault, and a faulted maintenance turn's catch-up abandoned with the visitor admitted (B36). *Built (B36):* a faulted wake's turn is abandoned and the wake then consumed in a turn of its own, and a maintenance turn's catch-up goes on past a faulted part (Decided 140); see "Found while building wakes". A poll that faults yields the world's `unseen` speech; logging it against the object whose `describe` cost too much is B37's. *Decided 2026-09-24 (140):* the other objects' due wakes are still delivered. *Built.*

Found while building ticks (B35), each decided the narrow way and awaiting Eric:

- **Where a tick runs.** The turn is the language's (`runtime/tick.ts`, `tickTurn`), in the frame every write turn runs in; the time a world reads is `runtime/time.ts`, which a wake shares. Running one under the world's lock is core's `runTick`, and when to tick is core's `Ticker` (`ticks.ts`), a round at an instant the host chooses. Nothing reads a clock: the host hands each tick its instant, in whole host seconds.
- **Which places are ticked.** Every place a present visitor stands in, which is the visitor's container, since an actor stands only in something holding actors; each once, in code-unit order of the id. An NPC keeps no place ticking, and an away visitor none. A place inside a place is ticked on its own while someone stands in it, and the place around it is not ticked for them, since their place is the nearest one (Places).
- **How the tick is delivered.** As the engine's `:tick (elapsed)` to the place, queued first in the turn's drain at depth 1, so the queue is still the one thing that delivers: it is one event against the turn's budget where the place answers it, and what the handler sends runs after it, a level deeper. A place with no `:tick` handler still has its turn, which records when it ran and costs no event. The tick reaches the place and nothing else; a `:tick` handler on its contents never runs.
- **`elapsed`.** The seconds from the place's last tick to this one's instant, which the turn then records as the place's last tick. A place's first tick is handed 0 (Open 130). An empty stretch is not forgotten: the next tick spans it (Open 131). *Decided 2026-09-24 (130, 131):* as built.
- **The host's defects.** A tick for something that is not a place, or not in the world, at an instant that is not whole seconds from 0, before the place's last tick, or so far after it that `elapsed` would pass the integer range, is thrown before the turn opens: `elapsed` is supplied truthfully or not at all (The host contract › Time).
- **A place emptied before its tick runs.** A round reads who stands where from the last committed state, with no lock, and a tick turn reads it again under the lock; a place nobody stands in by then is not ticked, and nothing is written.
- **Skip rather than queue.** A round asks for one tick per occupied place, and a place whose tick is asked for and has not yet run, waiting on the world's lock behind a long turn, is skipped by the next round rather than asked for twice; since its last tick is not moved, the next tick that runs carries the missed seconds. What is waiting is kept per world, so one world's slow lock skips none of another's places.
- **A dropped tick.** A tick that faults is abandoned like every write turn: the place's last tick stays where it was, so the next tick folds the dropped interval in, and nobody in the world is told, since nobody acted; its fault goes back to the host (Open 132).
- **`tell` in a tick.** Not yet read (B30); what a tick says today is what its drain says, a refused `move` to nobody and an NPC's reading performed with `act`. *Built (B30):* a tick's `tell` reaches the people in the place ticked.
- **A `:tick` handler on something that is never a place.** Accepted, and never delivered (Open 133). *Decided 2026-09-24 (133), the other way:* warned. *Built,* in `bundle/compile/timed.ts`: at the handler's `:tick`, once, naming the first object that runs it and is not a place, the declared ones in declared order by their path and then each kind a body spawns ("`yard.cat` is not a place, so `on :tick` never runs.", "A spawned `Ember` is not a place, …"), with "Only a place, whose kind writes `contains actors`, is ticked. Move the handler to the kind of the place it stands in, or ask with `wake in 10 minutes` and answer `on :woke (elapsed) { … }` here." A kind nothing is made of is not warned, nor a library's handler, as the other warnings about handlers are not.

Found while building the command parser (B27), each decided the narrow way and awaiting Eric:

- **What a grammar block holds.** `name` and one name in quotes, `article` and one of `a`, `an`, `the` or `none`, and `nouns` and one or more nouns in quotes, in any order. `exit` and `link` are B28's (*built*: see "Found while building exits"), and a line the block is not made of is refused once, the rest of that line stepped over (`corpus/bad/grammar-lines`). The world's own body reads no grammar block, and the world is never a noun's candidate, though range reaches it as a surface: nothing addresses the world.
- **Several grammar blocks in one body.** Their lines combine as one, as several `remembers` blocks do (Open 90); a `name` or an `article` written twice in one body is refused at the second (`corpus/bad/grammar-twice`).
- **An empty name, and an empty noun.** Refused, as an empty phrase is: nothing could type or read one. What it refuses lists neither.
- **A noun that begins with an article.** Not refused, since the spec refuses a name that does; a noun is tried as typed and again without a leading article, so one written so still matches.
- **The nouns cap and the word cap.** The nouns one body writes, however many blocks hold them, are counted against the host's nouns per object, refused at the first past it; a thing's composed nouns are not counted again. Each word of a name or a noun, split on white space, is held to the characters in one noun word (`corpus/bad/grammar-caps`).
- **What a thing without an identifier is called.** A spawn has no identifier, so where nothing in its closure writes a name, it is called by its kind's name humanised in lower case, `MazeCell` as "maze cell"; what a kind's contents give an instance is called by its identifier in the kind's body.
- **What a thing answers to.** Its full name, the last word of it, its identifier humanised (Slots: "Nouns resolve through an object's identifier and the `nouns`"), and every noun its closure writes, the kinds' first. A kind's `name` applies to every instance whose own body writes none, `kind Pebble { grammar { name "pebble" } }`.
- **A visitor.** Called by their nickname, with no article, answering to the whole of it and to the nouns their kind writes; not to its last word, since it was admitted whole, so "Marta B" is not `b`; a visitor kind's written `name` and `article` do not apply to a person.
- **How a line is read as words.** Lower case, split on white space, each comma a word of its own; any other mark is part of the word it touches, so `potatoes!` is one word and `?` is `help`'s. One article or determiner, `a`, `an`, `the`, `my`, `this` or `that`, is optional before a noun and before a value.
- **Where a phrase's slots fall.** The phrase's words must be the line's words where they stand; where they do not pin a slot, an earlier slot takes the longer run first, which is longest-first for nouns and nicknames; the first placement whose every slot fills is the reading.
- **The order phrases are tried in.** The world's own verbs, then the standard library's, then every other library's, each in declared order; the first reading wins; failing one, the first `which` any phrase asked; failing that, `unknown`. The world's own verb shadows a library verb of its name (Open 104). *Decided 2026-09-24 (104):* as built.
- **Disambiguation.** A role's kind narrows first: a thing that answers and does not compose the kind makes the phrase not match, which is `unknown` if no other phrase reads, never an author's refusal. Then a thing whose whole name was typed is preferred to one answering to a noun ("lamp" is the lamp, not the lamp oil that also answers to it); then several left are asked `which`, nearest first, unless they are written alike (Open 101). *Decided 2026-09-24 (101):* the nearest, and a draw from the turn's seed among equally near ones; the code catches up. *Built:* as decided (Open 197).
- **`which` with a re-submitted command.** The parser keeps nothing between turns: the answer carries, for each candidate, the line to type again to mean it, the doubted noun replaced by the candidate's name (`take key` offers `take brass key` and `take iron key`), and in a set role's run only the noun in doubt is replaced. Typed again, a choice asks the same `which` no more.
- **`unreachable`.** Open 102. Nouns resolve only against the visitor's range, walked once by the pass rules; nothing out of it is named, in a reading or in an answer, and a noun nothing in range answers to is `unknown`. Nothing yields `unreachable` yet. *Decided 2026-09-24 (102), the other way:* a noun nothing in range answers to is answered with `not_here`; the code catches up (Open 181). *Built:* `not_here`, binding `actor` and `here` and naming nothing, and `unreachable` is gone (Opens 181, 195, 196). *Decided 2026-09-24 (181, 195, 196):* as built.
- **Set roles.** A comma before `and` is one split; an empty noun in a run, `juggle gong and`, makes the phrase not match. How many things one set role binds is the reading's to check as it starts (B34's set-role cap), so the parser does not check it again.
- **Value roles, and a friendly form for options (decision 74).** A value role takes any words and binds a value only where a participant's `from` hears it now, so nothing a visitor typed reaches the reading otherwise ("carrying nothing of what was typed"): an option spelt as a visitor types it, `old press` for `old_press`, as typed or after a leading article; a whole number as one word of digits, a minus allowed. A value no participant hears leaves the role unbound, and the phrase still matches.
- **Directions and labels.** The closed set, the diagonals written as one word with `ne`, `nw`, `se`, `sw`, and `n`, `s`, `e`, `w`, `u`, `d`; `in` and `out` have no shorter form, and `north-east` is no direction. A direction names the first exit that applies in it; failing that, the words name the exit whose whole label they are, however cased. Which exits apply is the caller's, and B28 supplies them; until then a `go` reading, with its exit bound, is what the parser produces and nothing runs it. *Built (B28):* `parseCommand` asks the actor's place for the exits that apply, at most one in each direction, and a `go` reading runs. *Decided 2026-09-24 (103):* the closed set, as built; the spec's example is fixed.
- **What a line costs.** One step for every candidate a noun is tried against, one for each placement of a phrase's slots and one for each exit looked up, beside the range walks' own; a line too dear to read throws the budget's fault, and nothing else.
- **The word set.** Single words, sorted: each word of every name and noun written, of every object's identifier at every depth, and of the humanised name of every kind a spawn may name that writes no name, beside the directions, their abbreviations, the determiners, `and` and every phrase's words. A kind nothing spawns, the world's and the visitor kind, gives none. Exit labels join it with B28. *Built (B28):* each word of every exit's and link's label a kind or an object writes.
- **What the answers carry.** The world's `unknown`, `not_here` and `which` passages, unrendered, with `actor` and `here` bound, and `candidates` in a `which` as the bindings table says; B29 renders them. *Built (B29):* `renderFor` renders an answer for its reader. A world that composes none of them is an engine error, as for `nothing_happens`.

Found while building exits (B28), each decided the narrow way and awaiting Eric:

- **How a line is written.** `exit`, a direction, a label in quotes, `->` and a destination, an identifier or a dotted path, then optionally `when` and a condition in brackets; `link`, its name and a label, and nothing after, a link naming a place refused ("A link leads nowhere until the world connects it, so it names no place."). A line written wrong is refused once and stepped over to the block's next line, a bracketed condition whole (`corpus/bad/exit-lines`, `corpus/bad/link-lines`).
- **Directions in source.** Written out: an abbreviation is how a visitor types one, and is refused naming the direction it stands for ("`n` is how a visitor types `north`, and source writes the direction out."); a word outside the closed set is refused with the set listed and the remedy of a direction and the rest in the label (Open 103, `corpus/bad/exit-not-a-direction`). Links took the same set (Open 150). *Decided 2026-09-24 (150), the other way:* a link's name is a word of the author's, not a direction. *Built:* the parser reads any lower-case word as a link's name, and the first tier refuses one that is a direction, a reserved word or starts with no letter, a direction first (`corpus/bad/link-not-a-name`). An abbreviation, `n`, is not a word of the closed set and may name a link, since a visitor never types a link's name. A link's name is not in the world's word set.
- **An empty label.** Refused, as an empty name is: a label is what a visitor reads and may type back (`corpus/bad/exit-label-empty`).
- **The exits cap.** The exit and link lines one body writes, however many grammar blocks hold them, against the host's exits per place, refused at the first past it; several guarded exits in one direction each count, and a place's composed exits are not counted again, as a thing's composed nouns are not (`corpus/bad/exits-too-many`).
- **Where an exit may be declared.** On a kind or an object whose composed kind holds actors, refused at the direction of each one written elsewhere ("`Cupboard` is not a place, so nobody stands in it to take a way out."), a mixin kind composed only into places included, since the kind that writes the exit is where it is checked (`corpus/bad/exit-on-non-place`). A kind's exit is checked once, against the kind that wrote it, however many compose it.
- **How exits compose.** One source for each direction (Open 151). *Decided 2026-09-24 (151), the other way:* exits and links are not composed, and a kind's apply only to its own instances. *Built:* a kind has only its own; an object, declared or given by a kind's body, has its own and those of the kinds its `is` names, by direction and by link name, its own replacing theirs, and two of them writing one direction or one link refused at the second as written (Open 240). A kind composing two kinds that write one direction is not refused (`corpus/good/exits-own`). A spawn's ways out are its kind's own.
- **Where an exit leads.** Its destination names a declared object from where the exit is written, as a body's names do, a kind's body naming what it gives and then what the world's body holds; nothing answering is refused in the checker's words with the one most likely meant (`corpus/bad/exit-unknown-target`). It must hold actors where its kind is known, and it may not be the world, whatever the world declares, as visitors never arrive at the world (`corpus/bad/exit-to-non-place`). A destination whose kind is absent at load is accepted, the gap being its kind's, and the exit does not apply while it is absent (the absent table's row).
- **An exit's `when`.** A condition over `self`, the place, true or false, nothing else bound: `actor` and `here` are refused ("`actor` is not bound in an exit's `when`: it is asked of the place, whoever looks."), since a guard is polled for whoever looks. The literal `false` is warned about in the spec's words (`corpus/bad/exit-when`). `chance` in one is B33's to refuse.
- **Which exits apply.** Asked of the actor's place when a command is read, at most one for each direction and each link set: the first whose destination is a live place and whose guard holds, in the order the place's kind answers them. An exit does not apply, rather than faulting, where its destination is absent, a declared place destroyed (Open 82), gone or no longer a place, or where its guard reads through a name out of the place's range (Decision 53) or to a declared object destroyed. An unset link does not apply, nor one whose place is gone. One step is charged for each exit asked, beside its guard's own, so a place too dear to ask faults the turn as any work does. A label is matched only among the exits that apply, so a way that does not apply is never named, even by its label.
- **What `go` does.** The consent pass first, the actor's own part of `go` able to refuse (Open 153; *Decided 2026-09-24:* as built); then the engine's move, the actor moving itself through the exit, as the reading's first effect; a refusal of it by any party is said to the actor as a refused `move` is and ends the pass ("The gate is shut."). Where the move is made, the places either side speak and are sent `:departed` and `:arrived` as for any move between places, the actor's `do` for `go` runs, and the actor reads the new place's description, the `described` notice B31 renders, so `nothing_happens` is not said. A direction or a label that names no exit that applies is the world's `unknown`, as any word it does not know is.
- **A move through an exit.** Asks no range of its destination, since an exit joins one place to another however the tree holds them, and a place is out of range of another while the world refuses; the destination must still be live, and the three guards run as for any move.
- **`act go`.** Still refused (Open 152). *Decided 2026-09-24 (152):* refused, as built; an NPC moves with `move`.
- **`connect`.** A statement where a write may stand, a `do`, a handler or a hook, and refused in a guard or a `permit` as a write is (`corpus/bad/connect-in-guard`). It names a link among the writing body's ways out, by its name (Open 241), refused otherwise with the links it has, and offering to declare it only where the word may name one (`corpus/bad/connect-unknown-link`); its destination is a binding (Open 154; *Decided 2026-09-24:* as built), of object type, refused where its kind is known and holds no actors (`corpus/bad/connect-not-a-binding`). It writes only `self`, replaces where the link led before, and asks no range: the binding was reached where it was bound. A `connect` to what is not a live place when it runs, or on an instance without the link, as a kind's handler on an instance of a kind that composes it is, is a `ConnectFault`, the world's, writing nothing (`corpus/bad/connect-lines` pins how one is read).
- **A link at load.** Kept as stored, a link its kind no longer declares included: it applies nowhere, and a line restored brings it back.
- **What is not built.** The view listing the exits that apply with their labels (B37). *Built (B31):* `help` offers `go` by each exit that applies, and a visitor reads the place they arrive in; see "Found while building descriptions".

Found while building wakes (B36), each decided the narrow way and awaiting Eric:

- **Where a wake runs.** `wake in` is read in `syntax/parse/wake.ts` and checked in `check/wake.ts`; what an object holds is `runtime/wakes.ts` (asking, what is due, catch-up's one per object); a wake turn is `runtime/wake.ts` and catch-up `runtime/maintenance.ts`, both in the frame every write turn runs in. Running them under the world's lock is core's `runWake` and `runMaintenance`; when to deliver a wake, live or as catch-up, is the host's, and core has no scheduler for it (the host reads `dueWakes` of the committed state).
- **Every write turn is handed its instant.** `WriteInputs` carries `now`, in whole host seconds, for a command as for a tick, since a `wake` in a `do` is asked at the turn's instant; the log records it with the seed (B40). Nothing reads a clock.
- **The statement.** `wake in <n> seconds | minutes | hours`, the number a whole number written out and the unit plural (Open 142). It asks for `self`, the only object a body may schedule, and stands where the other effects do: a `do`, a handler, a hook; a guard or a `permit` refuses it as read-only ("`wake` asks for a wake, and a guard only reads and decides."). A wait longer than `elapsed` can carry, past 2,147,483,647 seconds, is refused at the count ("`wake in 600000 hours` waits longer than a wake can.", "Wait at most 596523 hours, and ask again with `wake in` when it arrives if it must be longer."). *Decided 2026-09-24 (142):* as built.
- **The floor and the cap.** A wake is due after what was asked or the host's shortest wake, whichever is longer, fixed when it is asked, so a floor raised later moves no pending wake; the limits table's row for the floor says `raised` rather than `fault`. A `wake` with as many pending as the host allows faults the turn (`WakeFault`, the world's, about the object), as a `spawn` past the host's bound does. A stored list longer than a lowered cap is kept whole.
- **What a wake is.** The serial it was asked under, from the world's one counter, when it was asked and when it is due. An object's list is kept oldest first. `elapsed` is the seconds from when it was asked to the instant it is delivered.
- **What is due, and in what order.** Every wake whose instant has come on an object in the tree, oldest first by when it fell due and then serial (Open 141); a wake on something out of the tree waits (Open 143). A destroyed object's wakes go with it, and with what it held. *Decided 2026-09-24 (141, 143):* as built.
- **A wake turn.** The host names the object and the serial; the turn takes the wake off the list, sends `:woke (elapsed)` queued first at depth 1, and drains, like a tick. A wake no longer pending, or on something no longer in the tree, does not run (`unwoken`), since another turn may have destroyed or consumed it; an instant before it is due is the host's defect, thrown before the turn opens.
- **A `wake` nothing answers, an `on :woke` nothing asks for.** *Decided 2026-09-24 (145):* both warned. *Built,* in `bundle/compile/timed.ts`: a `wake` at the statement, "Nothing here answers `:woke`, so this `wake` comes to nothing.", with "Say what happens when it arrives with `on :woke (elapsed) { … }` in this kind, or take the `wake` out."; an `on :woke` at its `:woke`, "Nothing here asks to be woken, so `on :woke` never runs.", with "Ask with `wake in 10 minutes` in a `do`, a handler or a hook of this kind, or take the handler out." "A kind" is read as every kind in the bundle that runs the body: where one of them brings the other half, as `Kiln is Timer { on :woke … }` brings it to `Timer`'s `wake`, neither is warned, since the spec warns rather than refuses for just that reason. Only the world's own are warned, once each.
- **A faulted wake.** Abandoned like every write turn, and then consumed: a second write turn of the same kind takes the wake alone off the list and commits, and core writes that. The fault goes back to the host; nobody in the world is told, since nobody acted. Logging it is B40's.
- **Catch-up.** One maintenance turn, under one budget: each object's oldest due wake, one per object, oldest first, each a part kept once it has run; a wake one of them asks for waits for live time, as do an object's later due wakes. A part that faults is consumed, and the catch-up goes on to the other objects' wakes (Decided 140). The turn always commits, if only an empty change set, and gives back what it delivered, each wake that faulted with its fault, and what it abandoned; nothing said (Open 144; *Decided 2026-09-24:* as built). Its change set is everything between the state it opened on and the state its last part left (`changesBetween`). *Decided 2026-09-24 (140):* a part that faults does not end the catch-up. *Built.*
- **Past a faulted part.** The parts after it are charged to the same budget and draw on along the same stream: what the faulted part drew stays drawn rather than rewinding, since the stream is taken in the order the turn runs and a replay from the seed faults the same part and draws the same again. Once the figure every part charges is spent — steps, events or the wall-clock backstop — nothing more can run under the budget, so the rest of the catch-up is abandoned and left pending for live time, rather than each faulting and being consumed unrun; a spent figure only some parts charge, spawns, output or a depth, faults only a part that charges it.
- **Admission.** Core's `runMaintenance` never refuses a world's fault, so the host admits the arriving visitor once it resolves; wiring it before entry is B42's. *Built (B42):* core's `runArrival` runs it, committed, before the arrival turn; see "Found while building arrival".
- **The seed.** Every part of a maintenance turn is handed the turn's one seed; drawing from it (B33) must keep one stream across the parts, or the parts draw alike. *Built (B33):* the parts share one stream as they share one budget; see "Found while building chance".

Found while building chance (B33), each decided the narrow way and awaiting Eric:

- **Where it is built.** `{one of}` is read in `syntax/parse/prose.ts` (tags in `prose-tags.ts`) into a `prose-one-of` holding its choices; `chance` and `random` are typed in `check/check/draws.ts`; where a draw is refused, and the reach through passages, is `check/chance.ts`, which `check/blocks.ts`, `check/handlers.ts`, `check/exits.ts`, `check/prose.ts` and `check/passages.ts` consult; the stream is `runtime/draws.ts`; a line's draws are `prose/line-draws.ts`.
- **The forms.** `{one of}…{or}…{/one of}`, the words spaced as a person likes, an `{or}` written directly inside the choice it ends, not inside an `{if}` or a `{for}` within it: "`{or}` separates the choices of a `{one of}`, directly inside it." A choice may be empty. An `{or}` outside a choice is `corpus/bad/or-outside-one-of`. A `{one of}` with one choice is warned at its `{one of}` as it is read, "This `{one of}` has one choice, so it says the same words every time.", with "Write another choice after an `{or}`, or take out `{one of}` and `{/one of}` and keep the words.", and renders that choice, drawing once for it (*Decided 2026-09-24 (175)*, `corpus/good/one-of-one-choice`). One never closed and a `{/one of}` that closes none are refused as `{if}` and `{for}` are (`corpus/bad/one-of-unclosed`), and a close no passage opens now names `{/one of}` beside `{/if}` and `{/for}`. `chance(n)` and `random(n)` take one whole number above zero written out, up to the integer maximum: a number worked out, `0`, a negative number and no number or two are each refused (`corpus/bad/chance-bounds`); `chance(1)` and `random(1)` are accepted. Any other name written with brackets and nothing before it is refused naming the two there are.
- **What a draw is typed as.** `chance(n)` a boolean; `random(n)` an integer from 0 to n − 1 exactly, so `random(6) == 6` is refused as any literal outside a range is, and a `set` of it into a narrower range faults at run time as any value that does not fit does.
- **Where a draw is refused.** In a consent guard, a `permit` and an exit's `when` (three of the spec's four; `describe` is B31's, which passes its own reason as the others do), in a pass rule (Open 170), and in a line a poll says (Open 171), both *Decided 2026-09-24* as built: "`accept` may not use `chance`: a guard is asked as part of a decision it must not change.", with "Roll in a `do`, a handler or a tick, keep what it gave on a property, and read that here." (`corpus/bad/chance-in-guard`); "An exit's `when` may not use `chance`: it is asked to show a visitor the ways out, so a roll would offer a way that vanishes when taken." (`corpus/bad/chance-in-when`). A `do`, a handler, a hook, a tick and a wake may draw, as the worked microworld's cat does.
- **Through passages.** A passage said by name or rendered by a slot from where nothing draws is held to the same rule, and the refusal falls where it is said, as a name it lacks does, since the passage may vary wherever else it is said: "The passage `stuck` uses `{one of}`, and it is said from a `permit`, which may not: …" (`corpus/bad/chance-in-permit-passage`), "… it is rendered from `accept` …" at the slot (`corpus/bad/chance-rendered-in-guard`). The first draw the passage's own words make is named, and nothing more is said of it from there; a passage it renders is followed on, and refused at its own slot. A line the engine says in a poll is refused at the draw itself.
- **The stream.** Each write turn begins one stream from its seed; every draw, in a body or in prose, is taken from it in the order the turn runs, and nothing reads the clock or the platform's generator (Open 174). *Decided 2026-09-24 (174):* the generator is the language's, as built, and the spec names it. A maintenance turn's parts share one stream as they share one budget, so no part repeats another's draws. A poll has none, and neither does the frame a guard, a `permit` or a pass rule runs in, so a draw there is the engine's defect rather than a quiet roll.
- **What a draw costs.** `chance` and `random` are one step, as any leaf is; choosing a `{one of}`'s block is one step, as testing an `{if}` is. `&&` and `||` that decide from their left leave a draw on their right untaken.
- **One line, one draw.** Every reader of a line reads the same choice (Open 172), drawn when the line first renders (Open 173). *Decided 2026-09-24 (172, 173):* as built.

Found while building the three audiences (B30), each decided the narrow way and awaiting Eric:

- **Where it is built.** `say`, `tell`, `tell <x>` and `text` are read in `syntax/parse/speech.ts`, which also reads the words `refuse` takes; where each may stand is `check/audiences.ts`; who reads what is told is `runtime/audience.ts`; and a line rendered once for each reader, an NPC's in its frame, is `prose/heard.ts`'s `renderHeard`. Nothing in a turn renders yet: a turn gives back its lines unrendered with who reads each, and storing the rendered line is B38's. *Built (B38):* every write turn renders what it said once its work is done; see "Found while building effects".
- **How `tell <x>` is told from `tell pulled`.** The word after `tell` names who is told where a dot follows it, or where the words follow it on its own line, in quotes, as a passage's name, or written wrong; a lone word, or one followed by the next line, is a passage said to the place. A word on a later line than `tell` is never who is told.
- **Where each may stand.** `say` in a role's `do`; `tell` in a `do`, a handler and a hook, ticks and wakes among them; neither in a guard or a `permit`, each refused in words of its own ("`tell` speaks, and `accept` only reads and decides.", `corpus/bad/tell-in-guard`). `text` is read everywhere and refused outside a `describe`, "`text` gives a `describe` its words, and this is a `do`." (`corpus/bad/text-outside-describe`); *Built (B31):* a `describe` holds it, and `say` and `tell` are refused in one; see "Found while building descriptions". A handler's `tell actor` and a passage it says that renders `{actor}` are refused in the words a handler withholds `actor` in (`corpus/bad/handler-tells-actor`).
- **Who reads a plain `tell`.** The people directly in the teller's place (Open 162), in contents order, less every participant of the reading it stands in, the actor first among them. Where no reading runs, in a handler, a hook, a tick or a wake, nobody is left out, the teller included where it is a person. A `tell` with nobody to read it is still a line, with no one to read it, as a handler's refused `move` is.
- **What reaches an NPC.** Nothing: only a person reads. Who hears an NPC's `say` is now the people in its place less its participants, so an NPC standing there is no longer among them; the engine's `leaves` and `arrives` notices are unchanged.
- **`tell <x>`.** A binding that is one object (Open 160), read where the statement runs, reaching that person only where they are in the teller's range then (Open 161). *Decided 2026-09-24 (160):* as built. *Decided 2026-09-24 (161), the other way, and built:* only in range, else nowhere and no fault; a teller inside a shut container reaches nobody outside it, and an `on :departed` telling the one who left reaches nobody. Range is asked as a `get` asks it, `pass any`, since a line is not a message and no `pass :m` answers for it; Who hears it's "pass rules do not apply" is read as the plain `tell`'s. A `tell actor` in a person's reading reaches them, so the reading is answered and `nothing_happens` is not said.
- **An NPC's line, framed.** A line an NPC says or is refused in, heard from it, reads to each hearer as the NPC speaking, in fixed words, `The cat says "Mrrp."`: the NPC named for that reader, the line's paragraphs joined into one quotation, capitalised; a line that renders nothing to a reader is not framed and not read. The frame's own characters are charged to each reader's output with the line's. What an NPC tells is not framed: it is narration, as a person's is.
- **The literal cap.** `tell` and `text` in quotes are held to the host's cap on a literal line as `say` is, `tell <x>` included, counted after escapes (`corpus/bad/tell-too-long`), and so is `refuse` (decided 127, `corpus/bad/refuse-too-long`).
- **The no-`say` warning.** At the verb's name: "Nothing that takes part in `pull` ever `say`s anything, so typing it is answered with the world's `nothing_happens`." (`corpus/good/unsaid-verb`), for the world's own verbs with phrases only, and never beside a refusal (Open 163).
- **Catch-up.** Unchanged: a maintenance turn gives back which wakes it delivered and nothing any of them said or told, so a wake's `tell` during catch-up is dropped as Absence says.

Found while building descriptions (B31), each decided the narrow way and awaiting Eric:

- **Where it is built.** `describe { … }` is read in `syntax/parse/describe.ts` as any body's block is; one voice under composition is `declare/describe.ts`; what may stand in one and the scope it has are `check/describe.ts`, with `check/blocks.ts` and `check/audiences.ts`; running it is `runtime/describe.ts`'s `describeFor`, which a poll calls as a command does; the engine's answers to `go`, `look`, `examine`, `inventory` and `help` are `runtime/engine-verbs.ts`, and what `help` offers is `runtime/offers.ts`; a description rendered for its reader is `prose/describe.ts`'s `renderDescription`. A command turn gives back its answers unrendered, beside what it said; storing the rendered line is B38's. *Built (B38):* each answer is an effect of the turn; see "Found while building effects".
- **Where a `describe` may be written.** In a kind's body and an object's, the visitor kind's included, as How members combine says. The world's own body is refused one, "A world is not made of `describe`." (`corpus/bad/describe-in-world`), since nothing looks at the world: it is never in range and never a place.
- **One voice.** Two in one body are refused at the second, "`Lamp` writes `describe` twice." (`corpus/bad/describe-twice`); a composer's own replaces what it composes; two composed sources with none of the composer's own are refused at the kind, as written, that brought the second, "`Press` gets a `describe` from both `Iron` and `Wood`, and a thing has one voice.", with "Write `describe { … }` in `Press`, with the one paragraph both should make." (`corpus/bad/describe-two-sources`). A describe is checked once, against the kind that wrote it.
- **Its scope.** `self` the thing, `actor` whoever looks, typed as `sprout.Actor` so `actor.is(Person)` narrows, and `here` the looker's place, typed as a role's `here` is (Open 200); `let` and `if` as in any body. A passage it says is checked with those bindings, as any said passage is.
- **What it may not do.** `say`: "`say` speaks to the one acting, and a `describe` is read by whoever looks.", with "Write `text` in its place …"; `tell`: "`tell` speaks to the room, and a `describe` is read by whoever looks, and only reads."; `refuse` and `allow`: "`refuse` decides, and a `describe` only says what is there." (`corpus/bad/describe-speaks`). A write, `move`, `spawn` (a `let` of one included), `send`, `broadcast`, `connect`, `act`, `wake` and `destroy`: "`move` moves something, and a `describe` only reads.", with "Move it to a `do` or a handler; a `describe` reads what is there and gives its words with `text`." (`corpus/bad/describe-writes`). `chance`, `random` and `{one of}`, in it and in any passage it says or renders: "A `describe` may not use `chance`: it is run whenever anyone looks, so a roll would change the thing while nobody acts." (`corpus/bad/describe-draws`), the no-draw rule's `describe` variant.
- **No `text`.** A `describe` with no `text` in any branch is refused at its word, "This `describe` has no `text`, so whoever looks at `Lamp` would read nothing." (`corpus/bad/describe-no-text`). A `text` in a branch that may not run counts (Open 212).
- **An absent `.prose` file.** At publish, a `describe` whose every `text` names a passage its kind would have from a `.prose` file that is absent is refused, "This `describe` says nothing while `Mirror`'s `.prose` file is absent: every `text` in it names a passage that file holds." (`corpus/bad/describe-prose-gone`); one with a `text` in quotes, or naming a passage the kind has, is kept. At load each such `text` renders nothing, and the description reads the world's `unremarkable` where nothing is left (Open 212).
- **Its syntax.** Its braces are refused where they are left off, "What `describe` gives goes in braces." (`corpus/bad/describe-no-braces`), and a block never closed ends where the body's next member starts, "`describe` is never closed." (`corpus/bad/describe-unclosed`); the next member is kept either way, and a well-formed member never vanishes beside a defective `describe`, over generated bodies.
- **Running one.** Each statement a step, each expression charged as a body's; each `text` one line, from the thing, carried unrendered with the names in scope where it ran. It reads the state it is given, the turn's after the queue in a command and the committed one in a poll, and draws nothing, so B37 calls `describeFor` with its own budget and renders with no draws.
- **Rendering one.** Each line renders as its own paragraphs, in the order run, as a `described` line; one that renders nothing leaves no paragraph behind. Where every line renders nothing, or the thing has no `describe`, the world's `unremarkable` is read, with `self` the world and `thing` the thing and nothing else bound, as Where types come from gives it: `actor` and `here` are not added, since the table says "only `thing`".
- **What the engine answers, and when.** Once the queue is empty: first every person a move of the turn carried between places reads the place they arrived in, in the order the moves were made, once for each place, and only where they still stand there, so a later move's arrival stands in for an earlier one's; then the command's own answer. An NPC reads nothing. A reading the engine answers is answered, so `nothing_happens` is not said beside it.
- **`look` and `examine`.** `look` describes the looker's container, which is their place, since an actor stands only in what holds actors; `examine` describes the thing its target names. A person examined reads as anything does: "There is nothing special about you." to themselves.
- **`inventory`.** The actor's own kind's `inventory` passage, which `sprout.Actor` writes as the spec's standard library does, said from the actor as a `said` line with `actor` and `here` bound, and checked as an engine line that binds the one acting.
- **`help`.** What `runtime/offers.ts` derives, each reading whose consent pass allows (Open 210): a role a thing fills by each thing in range it fits but the actor and the world, a set role by each such thing alone, `go`'s way by each exit that applies, a tool and a value role left unbound; one thing never fills two roles; each offer is a step, so a range too large to list faults as any work does.
- **`wait`.** Nothing is done, and the reading is answered with `nothing_happens` (Open 211).

Found while giving `here` its kind, each decided the narrow way and awaiting Eric:

- **Where a place is not a `sprout.Place`.** `here` is `sprout.Place` wherever every composed kind in the world that holds actors composes it: every named kind, each library's among them, what a kind gives its instances, and each declared object's own. Where one does not, `here` is the object type, since that kind could be an actor's place, and a read through `here` that needs its kind is refused with a remedy naming the first such kind: "`Wardrobe` holds actors without composing `sprout.Place`, so `here` may be a place that is not one: compose `sprout.Place` into `Wardrobe`, or narrow `here` first with `is()`." (`corpus/bad/here-not-a-place`). Nothing is refused for holding actors in a kind of its own, so no corpus world that writes `kind Room { contains actors }` and reads nothing through `here` moved (Open 200). Where the standard library is absent, `here` is the object type, as `actor` is.
- **Where it is bound.** A role's `permit` and `do`, a passage said from one or rendered by one of its slots, and the engine's lines that bind `here` (`unknown`, `which`, `nothing_happens`, `fault`, `unreachable`), so `{for thing in here}` and `here.count` need no `is()` in any of them (`corpus/good/lamplit`). A handler, a hook and an exit's `when` still withhold it.

Found while building arrival (B42), each decided the narrow way and awaiting Eric:

- **Where it is built.** An arrival is `runtime/arrival.ts`, a departure `runtime/departure.ts`, each a write turn through the frame, of the kinds `arrival` and `departure`, charged to the turn's step budget as any write turn is; what a place says of one actor entering or leaving, which a move between places and an entry share, is `placeEntered` and `placeLeft` in `runtime/move.ts`; what a place a visitor can stand in is, live, not the world and holding actors, is `isPlace` in `runtime/live.ts`. Core's `runArrival` runs catch-up as a maintenance turn and commits it, then the arrival in a transaction of its own; `runDeparture` writes a departure, or the quiet one after a fault.
- **A new visitor.** An instance of the visitor kind, minted as they arrive, with their record keyed by the visit and holding the nickname the host accepted, placed at the arrival place.
- **A returning visitor.** Their instance and record as kept; they come back where they last stood if that is still a place and its `accept` allows, else at the arrival place, and are told `displaced` first only where their last place is gone (Open 224). The nickname they come back with replaces the one kept.
- **Entry.** The place's `accept (item, from)` is asked with the world as `from` (Open 222), the first refusal deciding; then the one write, and `:entered` to the place, `:moved` to the visitor, and `:arrived` across the place's range as for any move between places; the place's `arrives` to the visitors in its range, and its description to the one who came in. Nothing is sent `:left`, since they were in nothing.
- **What the host must not hand over.** A visit already standing in the world, an empty nickname, or one someone present holds is the host's defect, thrown before the turn opens; a nickname held by someone away may be taken, since reservations are soft. The word-set check is the host's.
- **A world that admits no one.** No world, no visitor kind, or no arrival place in source, which `closedByBundle` reads from the bundle alone, so core runs no catch-up for someone who cannot come in; or an arrival place destroyed, absent, or no longer a place, read from the state (Decided 82). Nothing is written, and the host says so outside the world, in fixed words it may replace with its own: "This world is not letting anyone in just now."
- **A refused or faulted arrival.** Neither writes. A refusal is said to the visitor in the place's words, rendered against the world as the refusal found them, standing nowhere, which is never written (Open 224); a fault is told outside the world (Open 220).
- **Displacement.** A visitor whose place is gone, or is no longer a place, is displaced by their next command (Open 223, 225); the world's `displaced`, which binds nothing, is said from the world, then they enter the arrival place. Where the world then admits no one, they are told `displaced` and stay; where the arrival place refuses them, they are told `displaced` and the refusal, and stay.
- **A departure.** No guard is asked, since a person is never held in a world; the visitor leaves the tree with what they carry, and where they stood is kept as their last place (Open 221). They are sent no `:moved`, being out of the tree once they have gone. A departure from a place that is gone sends nothing, since nobody is in its range.
- **Ticks.** A visitor whose place is gone keeps no place ticking until their next turn displaces them, and one inside something that no longer holds actors keeps nothing ticking either.
- **The fault line.** Told to an actor whose place is gone, where `here` cannot be bound, the world's `fault` would render a `{here}` against nothing; the engine's stock line, which binds nothing, is told instead.
- **Catch-up and what the visitor carries.** Unchanged (Open 143): catch-up runs before the visitor is in the tree, so a wake on what they carry waits until they are back, and is then delivered with the whole time.
- **What is not built.** Logging the arrival, the departure and the nickname (B40); the host's nickname checks against the word set; and reporting what load names as stranded, which is the host's.

Found while building effects (B38), each decided the narrow way and awaiting Eric:

- **Where it is built.** An effect's shape, what a turn says unrendered (a line, or a description), and a place's notices as lines are `runtime/effects.ts`; rendering them is `prose/effects.ts`'s `renderEffects`, which renders each line once for each reader with `renderHeard` and each description with `renderDescription`. Since `prose/` sits below the engine, the frame cannot call it by name: `TurnHost.render` hands it in, as `CommandHost.parse` hands in the parser, and every host, core's and the fixtures', passes `renderEffects`. The frame (`writeTurn`, `writeUnder`) asks each turn kind what it said once its body returns, renders it under the turn's own budget and draws, and commits; `Committed.effects` is what the host is given.
- **The shape.** An effect is `{ kind, from, actor, to, visit, paragraphs }`: `kind` the spec's five (`said`, `told`, `refused`, `described`, `notice`); `from` the object it came from, which is whose body said it (`self` when it rendered), the party that refused, the thing described, the place for its notice, or the world for the engine's lines; `actor` the turn's (Open 250); `to` the reader's instance and `visit` the visit that is them, since the host routes by visit and a refused arrival's instance is never written; and `paragraphs` the words rendered for that reader. An NPC's line carries the NPC's framing in its words and its body as `from`.
- **The order.** One sequence per turn, a line's readers one after another in the order the line names them, and `effectsTo(effects, visit)` gives one person's in order. A command: the parser's answer alone; or the consent pass's refusal alone, to the actor; or the effect pass's lines in body order, then the queue's in delivery order, then the engine's answers (each person moved between places reading where they arrived, then the command's own answer). A place's `leaves` and `arrives` are said where the move is made, among the body's lines, so they keep body order. An arrival: the world's `displaced` where it is told, the place's `arrives` to whoever is there, the queue's lines, then the description to the one who came in. A displacement is the same, the refusal after `displaced` where the arrival place refuses, and `displaced` alone where the world admits no one. A departure: the engine's words to the one leaving, the place's `leaves`, then the queue's lines; a departure made quietly after a fault still tells the one leaving. A tick and a wake: the queue's lines, then the place each person a handler moved arrived in, as they read it, which a command's mover reads too. Catch-up says nothing (Absence, Decided 144), and nor does a wake consumed after a fault or a dropped tick.
- **Rendering.** Once the turn's work is done, against the draft then, so a destroyed object is still named (Open 252); under the turn's budget, each reader's words charged to their output and every step to the turn's steps; with `LineDraws` over the turn's stream, so every reader of one line reads one draw (Decided 172, 173). Nicknames are the draft's, so a visitor arriving in the turn is named by the nickname they came with. A line with no reader is not rendered and is no effect (Open 251), and neither is a reader it renders nothing for; a place's notice with nobody in range is not a line at all.
- **The output limit.** More than the host's `output` to the turn's actor throws, and the turn is abandoned as any budget spent abandons it: a command's actor is then told the fault, and an arrival that faults so is not admitted. A reader other than the actor is cut short instead (see "Found while building cost with people").
- **A fault, told.** A command that faults tells its actor the world's `fault`, rendered over the committed state under a fresh budget of the command's kind and a stream begun again from the turn's seed, since the turn's own went with it; where that cannot be rendered, the host's output limit being shorter than it, the stock line is the effect, made without rendering, so a fault is never silent: "Something in this world has gone wrong, and nothing has changed."
- **What the host is given outside the effects.** A world that admits no one, and an arrival that faults, are the host's words outside the world, as before (`NOT_ADMITTING`, `ENTRY_FAILED`); a refused arrival gives its refusal as its one effect, rendered against the world as the refusal found the visitor, which is never written.
- **What is kept beside them.** Each turn's value still holds its phases unrendered (what the parser answered, the consent pass refused, the effect pass and the queue did, the engine answered), since a spec reading them needs no renderer; `EngineAnswer` is now the turn's `Unrendered`, a line or a description.
- **What comes after.** B40 logs `effects` with each turn. B45 adds an extension's effect: a kind of its own beside the five, with a payload its schema validates and a transcript line, not rendered prose, among the same sequence, and the cap on effects per turn (Trust); an extension's `missing` notice on entry is a `notice` in the arrival's sequence. B46's conversation is not an effect and never in the log. B47 decides per client what to send for each effect, by its kind, routed by `visit`. B37's view renders its description with no draws and is not an effect, since a poll is not logged.

Found while building cost with people (B39), each decided the narrow way and awaiting Eric:

- **Where it is built.** What one reader may still be told is `prose/output.ts`'s `charged`, which `renderFor` and an NPC's frame charge through, and the meter's `offer` and `cutShort` are `runtime/budget.ts`'s; the host's bound on a crowd is `runtime/crowd.ts`, asked by `moveInstance` and by an arrival's `enter`, with its figure `peoplePerPlace` among the host's budgets in `bundle/limits.ts`.
- **Whose output faults.** A `RenderContext` names the turn's actor, and only that reader's output past the host's `output` exhausts the budget; anyone else is `offer`ed each line, and the first that would take them past it cuts them short for the rest of the turn, which goes on (Open 270). A description to someone cut short reads nothing, not the world's `unremarkable`. Where a poll renders a description, the one looking is to be named its actor, so one too long for them still faults the poll into `unseen` (B37 renders it).
- **A line too long for a bystander (254, out of Open).** Settled by the spec's text, and built by B39: What this costs says a turn "never faults because of who else was there" and counts output per recipient, so a bystander's output past the figure never faults a turn, where effects had it fault for any reader. `renderEffects` names the turn's actor in its `RenderContext`, none for a tick, a wake or a maintenance turn; a line nobody reads is still not rendered (Open 251). What the bystander reads instead is Open 270.
- **Rendering steps.** Unchanged: every reader's rendering is charged to the turn's steps (Open 271).
- **Where the crowd's figure lives.** Among the host's runtime budgets, as `pendingWakesPerObject` is though it outlives a turn, with no figure in the spec's tables and so `null` by default, which leaves it unbounded; its row in the limit table says a move past it is refused (`move-refused`), not faulted, since a visitor who walks toward a full room has done nothing wrong.
- **Who is counted, and when.** People only, standing directly in the place, the one moving not counted, so a move within the place one already stands in is never refused; an NPC neither counts nor is ever turned away. Asked after the engine's own refusals and before any guard (Open 272); on arrival, before the place's `accept`.
- **Ticks.** Following occupied places was already so: only a place a present visitor stands in is ticked (see "Found while building ticks"), so a tick's cost follows people and nothing here changes it.
- **What is not built.** A client's chip for `go` into a full place is not greyed, since the consent pass does not ask the engine's refusals (B37's view); and logging who was cut short is B40's.

**Decided, and different from what was built.** Each has an issue, so the code catches up rather than the spec drifting.

- A comment is also `/* … */`.
- The escapes are `\"`, `\\`, `\n` and `\{`, in quoted text and in a passage alike; `{{` is gone.
- A world's `version` is semver.
- An enum's options take an optional trailing comma, are capped at 100 by the host, and may not be reserved words; the reserved words are now listed under Lexical rules.
- A default option may be written qualified, `:ward Ward.iron` or `:ward sprout.Ward.iron`, and bare in a restatement, where the type is already known.
- A list's element type may be a list.
- Two lists are not compared with `==`; set and ordered equality are a later level's, and the runtime's `equals` goes.
- There is no nesting cap. The parser still has to bound its own recursion, and whatever it does about that is the compiler's own affair rather than a host limit.
- `chance(n)` is one in n and `random(n)` is 0 to n − 1.
- `sprout.World` is written on every world (`world w is sprout.World { … }`), as that literal and not an unqualified `World`, refused when missing, and refused on anything but a world. Eric restated this on 2026-09-22 after first deciding the opposite: level 1 is explicit wherever it could have defaulted, since a later level can relax a requirement and never add one.
- The manifest has an optional `namespace`, falling back to `name`, and the one `world` declaration repeats `name`; none, two, or another name is refused.
- A static cap exceeded at load refuses the world, unless the host has recorded an exception for it.
- Spawns per world per hour is gone; live instances per world is the host's storage decision, faulting a `spawn` when it is reached, rather than a figure in the table.
- The claim that a `let` costs fewer steps than the reads it replaces is gone; a `let` is charged as a statement.

**Confirmed as built**, with the spec now saying so where it did not: which side of a verb knows the other is the author's choice; an unset host limit is unbounded; blessed libraries are exempt from the file cap as well; a part's language level is declared, not derived; an author is text; a default is always written; `min` and `max` may be written in either order; `[]` is a fine default for a list that wrote its element type; `count` on a list; the list cap is a refusal when written and a fault when grown past; adding a held element to a full list does nothing; where a `let` may be written; `let x = spawn …` waits for B18; a world must say what its visitors are and where they arrive; `pass any (false)` is the language's default; what a world holds is a property like any other; a missing arrival place is the host's to refuse; the diagnostic gutter is the tool's; `$first` and `$last` are boolean, `$index` and `$count` integer; precedence is the conventional order, now written down; a `set` whose value is only known at run time faults rather than being refused.

Two things worth keeping from the earlier sweeps, because they read like holes and are not: two object bindings of different kinds may be compared, since asking whether the mover is the actor is the point; and `chance` and `random`, whose reachability rules are the whole of what makes them safe (built with B33).

One future thought recorded on the way, for the registry discussion in the backlog: a host may one day decline to store a vendored library whose hash matches one it already holds, and serve the shared copy instead.

*Closed on 2026-09-20, against the five reviews:* an open role, a handler's sender and an unfiltered loop variable are of the bare object type and are read only through `is()`; `each` is defined; `:remembers` uses the property declaration syntax; NPCs compose the visitor kind, have a name, read nothing, act with `act`, and do not keep a place ticking. *Decided 2026-09-23 (73), the other way on the first point:* an NPC composes `sprout.Actor` and not `sprout.Visitor`, which only the visitor kind composes.

*Closed: range and visitors are in the world model; `let` is specified; type parameters, stateful alternatives and stored references were all decided against; listing contents needs no convention, since `{thing}` renders an object's name; `look` and `examine` render a `describe`, and `text` is the statement that writes inside one.*

### Decisions

**Type parameters for standard library kinds — decided against.** Roles dissolved the case. A library kind carries the verb, the role and the state it can name itself; the world composes in a second `permit` that reads its own enums. Both run, either may refuse, and neither kind mentions the other's types. Locks, vending and growth stages all split the same way, so the pattern is general: *the library supplies behaviour, the world supplies vocabulary.* Generics would have touched composition, property merge, `is()`, disambiguation and the printer, for a benefit we can no longer name. *Revisit only if the standard library draft produces a kind that must itself read or write a world-typed value.*

**Roles bind objects only — closed by value roles.** A verb's roles were filled only by things in the world, so `ask [target] about [topic]` had no shape: a topic is not an object. That was the dialogue gap the reviews raised, with a precise cause. A role that binds a symbol from a declared enum, or an integer in a range, answers it and covers `set [dial] to [number]` too; the spec's Value roles section is the result. *Closed.*

**Stateful text alternatives — decided against.** Ink's other three forms (sequence, cycle, once-only) each need to remember where they got to, and that counter would be undeclared state: missing from the property map a moderator reads, keyed to a source position that moves when an author adds a paragraph, and multiplied per visitor if it is to mean "the first time *you* saw this."

The explicit form is also more correct, which settled it. A sequence advances on each *render*, and a description renders on every poll, so "the first time" would be spent by a poll caused by somebody else acting in the room. Authors mean the first *visit*, and only a counter they wrote can say that. A counter in a `remembers` block, incremented in `on :entered`, is three lines and says what it means. *Revisit only if authors keep writing the same counter.*

**Weighted variation.** Evennia's ambient messages weight their random selection so a common line and a rare one can share a set. `{one of}` is uniform. *Small, probably worth it.*

**Disambiguation on state.** TADS ranks candidates at runtime through `verify()`, so an open box beats a closed one for an action needing an open container. Our typed slots rank on type only. Static and checkable against dynamic and more expressive. *Deferred until a world wants it.*

**Stored object references — comparison-only stands, with links as the exception.** The prior-art case against us dissolved: Inform's `matching key` holds one specific key, and roles express "any key cut for brass wards" better than a pointer can. Most other reference-wanting cases turn out to be queries or flags — who is It is `:team` on the visitor, not a pointer to a player — and a flag survives its referent being destroyed where a pointer does not.

Only dynamic topology was irreducible, and it is answered by `link` and `connect` rather than by general references. A link's destination is write-only from the language's side: assignable from a binding, never readable into an expression, so no null is observed, nothing dangles, and nothing can `get`, `send` or `each` through it. The map grows and the message graph does not. *Closed.*

**Nickname reservation expiry.** Reclaimable nicknames mean a world's namespace grows with every unique visitor it has ever had. Whether reservations lapse is host policy, but a popular world eventually runs out of good names.

**The eight-role cap.** Untested — the worked example never needed more than two roles on a verb. It is the cap most likely to be wrong in one direction or the other. Like every limit it is now a host default, so being wrong costs a configuration change rather than a language change.

**The step budget's number.** The spec's default is 50,000. LambdaMOO has run foreground tasks on 30,000 for decades, against a language with real loops and recursion. Ours is probably generous; instrument before fixing it, and remember it is the host's number.

**Actor-less turns — decided for a static rule with an escape hatch.** Two of the reviews wanted opposite fixes: refuse `actor` statically wherever a body is reachable from a tick or a wake, or let it be a runtime no-op. Reachability would have forbidden `actor` in every `:entered` handler, since an NPC can arrive on a tick; a runtime no-op is a silent lie. The spec does neither. `actor` is bound by *member kind* — role bodies and `describe` — and never in handlers, hooks, ticks or wakes, which is static, exact and needs no reachability. What handlers lost they get back two ways: `tell <x>` addresses any actor they have bound, and `act` lets an NPC start a reading in which it *is* the actor, so "the cat licks you" is an ordinary verb with a real actor and a real target. The `:entered` idiom becomes `item.adjust(:visits, 1)`, keyed on the thing that arrived rather than on an actor that may not exist.

**Silence — decided against, everywhere.** A reading that says nothing to the actor is answered with `nothing_happens`; the compiler warns about a verb nobody `say`s for; built-ins are library verbs with passages; arrival renders the description; refusals always have text. The reviews' worst transcript had sixteen silent turns, and every one of them now prints something.

**Typing — exact, at the cost of `is()`.** The union rule over role-players was computable and fragile; declaring a kind on every role brought brand kinds back. The bare object type is the third way: an open role is an object, and reading anything through it requires narrowing. One `if` per cross-kind read, and no read is ever unchecked. Messages declare their carried type for the same reason.

**The poll is a turn.** Read-only, snapshot-isolated, own step budget, no seed, no log, cacheable until a committed write turn names the view stale. Making views a by-product of write turns was the cheaper runtime and the worse language: it would have charged a busy room's every action for every bystander's view. A poll that is a turn has a budget the host sets and a fault the author owns.

### Not language questions, but blocking

Author-facing tests and a way to inspect what the parser will accept. Both are real gaps from the reviews, neither is answered by the spec, and both are what an author reaches for the first time something does not work. (The dialogue model is answered by value roles; the view's exported readings are most of what a parser inspector needs to show.)
